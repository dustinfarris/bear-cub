#+title: PLAN — Bear Cub: Good standing
#+date: 2026-09-05

# Mutability: execution state. Story status and implementation observations are recorded here as LOGBOOK entries per the workflow-kit org-conventions skill. By phase close, this file should read as an accurate record of what was actually built.

Design: [[file:DESIGN.org]]

* Batch 1

# Stories in this batch nest below as org TODOs, listed in implementation order; status updates get LOGBOOK entries under the story heading. At phase-close the gate record for this batch lands here as a LOGBOOK entry on the batch heading — its presence marks the batch closed. At most one batch is open (has no gate record) at a time; new batches are appended by later user-stories runs, never over an open one.

Implementation order and why: the shared predicate comes out first (01) so nothing is written against a rule that is about to move; the derivation follows (02) since every surface reads it; admin (03) is sequenced ahead of the kiosk because it states the verdict in words, which makes standing observable while the wordless kiosk treatment is built and QA'd; the design-language ruling (04) lands before the kiosk (05) because CLAUDE.md makes that file binding on kids'-view styling — the ruling must govern the band, not be written afterwards to describe it.

** DONE [[file:stories/story-01-routine-day-status.org][Story 01 — ~Chores.routine_day_status/3~: one routine-day predicate, two implementations]]
:LOGBOOK:
- Note taken on [2026-09-05 Sat 22:38] \\
  Closed out: all 5 ACs and all 4 DoD items ~[X]~. ~Chores.routine_day_status/3~ (~lib/bear_cub/chores.ex~) returns ~%{empty?: chore_ids == [], complete?: …, failed?: …}~ over the same two queries the old code ran, and ~routine_day_contribution/3~ is now a thin wrapper: ~if(complete? and not empty?, do: r, else: 0) + if(failed?, do: -r, else: 0)~. The extraction is behavior-preserving *by construction*, which is why the refactor-safety evidence is as strong as it is: the pre-extraction ~complete?~ was ~chore_ids != [] and Enum.all?(…)~, and the extraction moved only the ~chore_ids != []~ conjunct out to the wrapper as ~not empty?~, leaving ~Enum.all?~ vacuously true on the empty roster exactly as [[file:../decisions.org::#d92][D92]] specifies. The truth table for the points contribution is unchanged at every input. Confirmed by ~test/bear_cub/points_test.exs~ being byte-unchanged (~git diff HEAD~1~ empty, 27 passing standalone) and the pre-existing ~describe "routine_day_contribution/3 (Story 02, D40)"~ block unedited per diff. New tests: ~describe "routine_day_status/3 (Story 01, D94, D92)"~ (4 tests, ~chores_test.exs:869-909~) and the ~+R~-denied empty-roster pin the design called for (~:984-988~), both pure additions per ~git diff~. ~routine_days_by_kid/1~ untouched — two implementations after this story, not three ([[file:../decisions.org::#d94][D94]]). ~mix precommit~ clean, 553 passed, independently re-run by the verifying subagent. \\
  *Carry-forward for Story 02, no design change:* a failed completion in this codebase carries ~undone_at~ *and* ~failed_at~ — the test helper stamps both, matching ~extra_contribution/2~'s failed-before-undone ladder. So a routine-day whose only completion is failed reports ~complete?: false, failed?: true~, while a failed-then-redone one reports ~complete?: true, failed?: true~ (the redo writes a fresh live row; the failed row keeps its stamp). Both pairings are real and both deny standing. The design already states this correctly — "A half can be both incomplete and failed at once", and the ~standing/2~ shape example shows the ~complete?: false, failed?: true~ pairing — so this is recorded here rather than as a body edit, for the session that writes Story 02's failed-half fixtures. \\
  *Test-hygiene clause is vacuous for this story's tests, not skipped:* DESIGN §Testing requires every test to pass fixed local dates and pin ~:routine_windows~. The new tests pass fixed dates as literal ~Date~ sigils (2026-09-04 and 2026-07-10) with nothing derived from the wall clock, and do not pin ~:routine_windows~ because neither function under test reads it — ~routine_day_status/3~ takes its date as an argument and the only ~Routines~ call in the path is ~bonus/0~, which reads the separate ~:routine_bonus~ key. The clause binds the window-dependent kiosk tests in Stories 03 and 05, where it does bite. No deferral recorded against it. \\
  No Deferred discoveries: the diff is confined to the two functions in ~chores.ex~ and the two test additions.
:END:
** DONE [[file:stories/story-02-standing-derivation.org][Story 02 — ~Chores.standing/2~: the two-half standing rule]]
:LOGBOOK:
- Note taken on [2026-09-06 Sun 07:13] \\
  Closed out: all 8 ACs and all 4 DoD items ~[X]~. ~Chores.standing/2~ (~lib/bear_cub/chores.ex:461-483~) is exactly two calls to ~routine_day_status/3~ — ~morning~ at ~local_date~, ~evening~ at ~Date.add(local_date, -1)~ — composed into ~standing? = morning.complete? and not morning.failed? and evening.complete? and not evening.failed?~, returning ~%{standing?:, morning:, evening:}~ per the design's shape. New ~test/bear_cub/chores/standing_test.exs~ (13 tests) covers both-halves-clean, each half incomplete, each half failed-then-redone (verified via a genuine second ~complete_chore~ call after ~fail_completion~, not a reused row), the three empty-roster combinations, an undone completion, two date-leak guards, and two roster-history guards (chore archived today still binds last night via ~archived_on > local_date~; a chore created today is excluded via ~active_from <= local_date~, confirmed to route through the real ~create_chore/3~ path). ~mix precommit~ clean, 566 passed (up from 553), independently re-run twice by the verifying subagent across the AC and DoD passes. \\
  No Deferred discoveries: the diff is confined to ~standing/2~ and its test file, matching the Technical Notes' scope statement. \\
  *Story-file discrepancy, not a design issue:* story-02's AC5 cites D96, but its own "Decision Log entries in scope" list only D91/D92/D94 — D96 is Story 03's admin-display decision, not this one's. Left as-is; not a DESIGN body concern, flagged here for whoever next touches the story file. \\
  *Test-hygiene clause remains vacuous here, same as Story 01:* neither ~standing/2~ nor ~routine_day_status/3~ reads ~:routine_windows~, so the story's Testing excerpt clause about pinning it does not bite until the window-dependent kiosk stories (03, 05).
:END:
** DONE [[file:stories/story-03-admin-standing-row.org][Story 03 — Admin: the standing verdict and which half broke it]]
:LOGBOOK:
- Note taken on [2026-09-06 Sun 07:50] \\
  Closed out: all 7 ACs and all 4 DoD items ~[X]~. ~load/2~ (~lib/bear_cub_web/live/admin/today_live.ex~) gained one key per card — ~standing: Chores.standing(kid, today)~, reusing the ~today~ it already derives, so no clock read was added — and the render places ~<.standing_row kid={kid} standing={standing} />~ between the kid's colored ~<header>~ and the ~sections~ loop. The component branches on ~standing?~: three ~hero-star-solid~ (~size-4~) in a ~text-success~ row with *In good standing*, or a muted *Not in good standing* line above two chips. ~half_mark/1~ is the whole ✗ rule and is deliberately two clauses — ~%{complete?: true, failed?: false}~ → ✓, everything else → ✗ — so "unsatisfied" and "failed" reach the same glyph without the derivation being changed to pre-resolve an atom, exactly as the story's Technical Notes required. New ~describe "standing row (Story 03, D96)"~ (4 tests, ~today_live_test.exs~). ~mix precommit~ clean, 570 passed (up from 566), independently re-run by the verifying subagent across both the AC and DoD passes. \\
  *The failed-then-redone chip is the one test worth naming:* it completes the evening chore under last night's date, fails it, then completes it again — so the half reports ~complete?: true, failed?: true~, the pairing Story 01's carry-forward note predicted — and asserts the chip still reads ✗. That is the AC4 "does not distinguish the two causes" clause under its harder input, not its easy one (a simply-unfinished half would pass on ~complete?: false~ alone and prove nothing about ~failed?~). \\
  No Deferred discoveries: the diff is confined to ~today_live.ex~ (one ~load/2~ key, one render line, one component plus its helper) and the new test describe block. \\
  *Test hygiene — the clause bit here, and not the way Stories 01 and 02 predicted.* Both earlier entries recorded it as "vacuous for this story, binds the window-dependent tests in Stories 03 and 05." The ~:routine_windows~ half stayed vacuous even here: the standing row renders independent of the window by [[file:../decisions.org::#d96][D96]], every assertion in the new block targets ~#standing-*~ only, and nothing in it calls ~active_routine/0~ — so the =docs/learnings.org= [2026-07-17] failure mode (inheriting whichever window the wall clock lands in, including the six-hour Good Night gap) cannot fire. What did bite is the *other* half: ~Admin.TodayLive~ reads ~LocalTime.now()~ at mount with no injection seam, so a LiveView test cannot pass it a fixed local date at all, and the design's "no new test derives its date from the wall clock" is unsatisfiable at this layer. The tests capture ~LocalTime.now()~ once in ~setup~ and derive ~now~/~yesterday~ from that single binding instead. This one *is* a design-text problem rather than a per-story vacuity note, so unlike Stories 01 and 02 it did not stop here: it produced a DESIGN body edit and a repair-class Advisory this pass, and Story 05's embedded excerpt was corrected to match.
:END:
** DONE [[file:stories/story-04-design-language-ruling.org][Story 04 — Design-language ruling: the standing band and ring are not the bonus badge]]
:LOGBOOK:
- Note taken on [2026-09-06 Sun 07:59] \\
  Closed out: all 4 ACs and all 4 DoD items ~[X]~ except DoD's testing item, ~[-]~ deferred as documentation-only (no code, no LiveView flow touched). ~docs/design-language.org~ gained one ruling paragraph under ~* Rulings~ (line 31), "Standing band and ring [2026-09-05]", transcribing the story's Design excerpt and D97 verbatim: the band (full width, ~bg-success~, ~h-14~, three ~hero-star-solid~ at ~size-9~, no text), the ring (~ring-4 ring-inset ring-success~ on the column ~<section>~, banner-to-bottom, overlaying the child-color banner edge), the explicit "reuses both existing signals" statement, and the "scale and pattern only, never hue or glyph" distinction, closing with ~Rationale: [[file:decisions.org::#d97][D97]]~. ~mix precommit~ clean, 570 passed, independently re-run by the verifying subagent across both the AC and DoD passes. \\
  No DESIGN.org body edit: the ruling is a faithful transcription of DESIGN's existing §Kiosk — placement and appearance excerpt with no new information surfacing during implementation, so no Decision Log or Advisory entry was warranted this pass. \\
  No Deferred discoveries: the diff is confined to the one ruling paragraph and the story file's own checkbox/LOGBOOK updates.
:END:
** TODO [[file:stories/story-05-kiosk-band-and-ring.org][Story 05 — Kiosk: the green band and column ring, morning window only]]

* Deferred

# Out-of-scope discoveries land here during implementation instead of being fixed inline. Promoted to v2 candidates at initiative close.
