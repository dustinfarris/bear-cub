#+title: Decision Log
#+startup: showall

Repo-level decision log: one monotonic D-sequence across all initiatives, appended newest last — decision entries never live inside an initiative directory. Entry format (heading + CUSTOM_ID), citation links, chain-qualified pointers, the overrule protocol, and the append-only rule live in the workflow-kit org-conventions skill's Decision Log section. Entries are never edited or deleted; a reversal or overrule is a new entry citing the old.

Historical entries remain in situ, list-format:
# D1–D7   docs/2026-07-09-mvp/PRD.org (§9)
# D8–D26  docs/2026-07-09-mvp/DESIGN.org (§10)
# D27–D38 docs/2026-07-11-extras/DESIGN.org
# D39–D55 docs/2026-07-15-gamification-points/DESIGN.org
# This log begins at D56.

* D56 (2026-07-24) — Night screen: one dark screen, one evening-colored moon, nothing else — supersedes the per-column Good Night message (D32), scopes D43's always-visible badge to waking windows
:PROPERTIES:
:CUSTOM_ID: d56
:END:

The 23:00–05:00 gap renders a single full-kiosk dark screen (~bg-stone-950~) holding one large centered moon (~hero-moon-solid~, colored via ~var(--routine-evening)~) and nothing else: no kid columns, no banners, no events strip, no stale glyph, no copy. ~Messages.good_night/0~ retires with the message it carried — the D37/D38 =Good night=-vs-=You did great today!= copy distinction is moot when night carries no copy at all. Supersedes extras D32's per-column Good Night message block; D32's untappable-lockdown stance stands (nothing on the night screen is interactive; corrections still go to admin). Scopes gamification D43: "the points badge renders unconditionally" now holds within waking windows only — at night the whole per-kid surface is replaced, badge included (the badge's purpose, a kid seeing their total while using the kiosk, has no audience at night). Rejected — restyling the per-column Good Night blocks darker while keeping banners/badges/events: the backlog spec was explicit ("a dark screen with a evening-colored large moon in the center, and nothing else"), and a fridge-mounted tablet at night wants minimum emitted light, which retained white banner bands would defeat. Settles the night-mode backlog item (captured 2026-07-13). → design-language Rulings.

* D57 (2026-07-25) — Rewards live in a new ~BearCub.Rewards~ context; ~BearCub.Points~ composes earnings and spends; ~Chores.points_total/2~ becomes ~Chores.earnings/2~ (raw signed)
:PROPERTIES:
:CUSTOM_ID: d57
:END:

Redemption gets two new modules rather than an extension of ~Chores~. ~BearCub.Rewards~ owns the ~rewards~ / ~redemptions~ tables, every write path, the availability derivation, and its own PubSub topic, and knows nothing about chores or completions. ~BearCub.Points~ is a composition module — the only place in the app that knows both sides — exposing ~balance/2~ (raw signed), ~total/2~ (the D41 floored display figure), and their whole-render map forms. ~Chores.points_total/2~ is renamed ~Chores.earnings/2~ and returns the raw signed earnings unfloored; the floor moves up to ~Points~, where the complete summation lives, and ~Chores~ never learns ~Rewards~ exists. Rejected — putting ~redemptions~ inside ~BearCub.Chores~: a naming lie (a redemption is not a chore), and ~chores.ex~ already carries kids, chores, extras, completions, fails, and the points derivation in ~450 lines. Rejected — letting the points derivation read the ~redemptions~ table directly from ~Chores~: inverts the dependency direction the repo already treats as an invariant for ~Chores~ → ~Calendars~; the moment ~Chores~ imports ~Rewards~, a rewards failure can reach the chore path. Rejected — threading the spend in as an argument (~points_total(kid, date, other_signed_inputs \\ 0)~): keeps the contexts clean but pushes the summation into every LiveView, so a missed caller is a silently wrong balance. Settles rewards/SC-3 and rewards/SC-4 placement. → rewards/Key Behaviors (contexts and dependency direction), rewards/Overview.

* D58 (2026-07-25) — ~rewards~ carries a nullable ~kid_id~ (audience) and a ~repeatable~ boolean (frequency) as orthogonal knobs
:PROPERTIES:
:CUSTOM_ID: d58
:END:

A reward's ~kid_id~ is nullable — NULL means offered to any kid, set means offered to that kid only — and is a *purely administrative audience control*, answering "is this on offer to both kids, or just one?" and nothing more. Repeatability is a separate boolean: repeatable means available again the next local day, one-time means claimable once. The two are never coupled, so a one-time reward offered to both kids ("your own bike" — each may earn one) and a repeatable kid-scoped reward ("an extra bedtime story") are both legal. ~kid_id~ is set programmatically from the form's kid selection, never cast from attrs, matching ~create_chore/2~. This is the nullable-discriminator pattern (D27; ~docs/learnings.org~ [2026-07-12]) applied a third time. Rejected — a family-wide-only catalog: cannot express "the ATM toy is this child's to earn". Rejected — a per-kid-only catalog mirroring ~chores~: forces duplicate rows for every shared reward, and the duplicates then drift in price. Rejected — coupling the knobs (e.g. "one-time implies kid-scoped"): would forbid two legitimate combinations for no gain. Rejected — a ~cooldown_days~ integer (0 same-day, 1 next-day, NULL one-time): strictly more general and would collapse both concepts into one field, but nothing in family use asks for a 3- or 7-day cooldown; ~repeatable~ widens to it additively if it ever arrives. Settles rewards/SC-1, rewards/SC-5 storage. → rewards/Data Model.

* D59 (2026-07-25) — One ~redemptions~ table; state derived from nullable timestamp markers, never a status column
:PROPERTIES:
:CUSTOM_ID: d59
:END:

The request and the spend are one row. ~requested_at~ / ~approved_at~ / ~declined_at~ / ~reversed_at~ are nullable markers, and every state is a query over them: pending, lapsed (requested, unanswered, dated before today), approved, reversed, declined. Contribution is marker-first, mirroring ~extra_contribution/2~'s ~cond~ — reversed → 0, approved → −points, otherwise → 0 — so a reversed row is retained forever as the trail. This is D10's posture and the ~undone_at~ / ~failed_at~ precedent: the row records what happened and when. Rejected — a ~status~ string or ~Ecto.Enum~ column: more legible at a glance, but it stores a value the markers already determine, and any store-plus-derive pair eventually drifts; the timestamps carry strictly more information (they answer *when*). Rejected — a separate ~redemption_requests~ table with the spend as a second table: two tables to express one lifecycle, a join for every history read, and a forked audit trail — the same argument D39 used to reject a ~points_ledger~. Settles rewards/SC-6 verdict states, rewards/SC-7 permanence. → rewards/Data Model, rewards/Key Behaviors (availability).

* D60 (2026-07-25) — The redemption price is snapshotted on the row; the reward name is not
:PROPERTIES:
:CUSTOM_ID: d60
:END:

~redemptions.points~ is a price snapshot taken at row creation — at *request* time for the kid leg (that is the deal the child was shown) and at redeem time for the parent-direct leg. The total is cumulative all-time and purely derived, so reading a live ~rewards.points~ at derivation time would make history mutable: a parent raising a price would retroactively deepen every past spend of it and silently move a child's balance, potentially into debt after the fact. A ~reward_name~ snapshot is deliberately *not* taken — reward rows are archived, never deleted ([[#d68][D68]]), so the join is always available and the name stays a single source of truth; a rename is correctly reflected in history, because a parent renaming a reward is clarifying what it is, not creating a different one. Rejected — snapshotting the price at approval instead of request: lets a price change between ask and answer alter the deal after the child committed to it. Rejected — no snapshot, joining to ~rewards~ for the price: makes a derived cumulative total unstable under ordinary admin edits. Settles rewards/SC-7. → rewards/Data Model.

* D61 (2026-07-25) — One open request per kid per local day, enforced by a partial unique index; lapse is derived and free
:PROPERTIES:
:CUSTOM_ID: d61
:END:

A partial unique index on ~redemptions (kid_id, local_date) WHERE requested_at IS NOT NULL AND approved_at IS NULL AND declined_at IS NULL~ caps a child at one open ask per day — the same technique the shipped ~completions (chore_id, local_date) WHERE undone_at IS NULL~ index already uses. Including ~local_date~ in the key is what makes lapse free: yesterday's unanswered request cannot block today's, with no job and no cleanup, so an unanswered ask simply does not carry over. A racing duplicate hits the index and returns ~{:error, changeset}~, which callers treat as already-asked — the posture ~complete_chore/3~ takes on a racing double-tap. The index has no reward component, so it constrains how many asks a kid has open, never which reward they may ask for: repeatability, kid scoping, and the open-request cap are three independent constraints. Rejected — a scheduled cleanup or expiry job for stale requests: midnight machinery the derived-state doctrine (D10) forbids, buying nothing the dated key does not already give. Settles rewards/SC-6 (an unanswered request does not carry over). → rewards/Data Model (indexes), rewards/Screens (admin approval queue).

* D62 (2026-07-25) — Availability is fully derived, and both predicates are scoped per-kid
:PROPERTIES:
:CUSTOM_ID: d62
:END:

Nothing about availability is stored and no job runs. Over the unreversed approvals for a given reward *and a given kid*: one-time is consumed if any such row exists on any day; repeatable is on cooldown if such a row exists dated today. "One-time" therefore means each kid may claim it once ever, "repeatable" means each kid may claim it once per day, and the next local day arriving is what restores a repeatable reward — the same D10 mechanism by which a completed chore becomes incomplete at midnight. A reversal restores availability for free, since both predicates exclude reversed rows. Pending requests do not consume (no ~approved_at~), so there is no cross-kid race; the surviving same-kid interleaving — ask on the kiosk, parent direct-redeems the same reward for the same child, then approves the open request — is what the approval-time re-check ([[#d63][D63]]) exists to catch. Rejected — global (family-wide) one-time consumption: it reads ~repeatable = false~ as physical scarcity, one object gone the moment anyone takes it, which inverts what ~kid_id~ is for ([[#d58][D58]]) — a parent wanting "each of you can earn one of these" would be pushed into duplicating the reward per kid, exactly the duplicate-and-drift problem the nullable ~kid_id~ exists to prevent, and it would let a card change state in one child's column because of something that happened in the other's. Rejected — a global repeatable cooldown: a family reward taken by one kid would sit dimmed in the sibling's column all day with no legible reason. Rejected — guarding availability at request time with a reserve model: rejected on its own merits under [[#d63][D63]]. Family-scarce rewards (one object claimable once across the whole family) are a distinct concept, deliberately deferred and not to be reintroduced by reading scarcity into ~kid_id~. Settles rewards/SC-5. → rewards/Key Behaviors (availability), rewards/Screens (kiosk card states), rewards/Out of Scope.

* D63 (2026-07-25) — Affordability is strict on both legs against the raw signed balance; no override, no reserve
:PROPERTIES:
:CUSTOM_ID: d63
:END:

One rule, one function, both legs: ~Points.balance(kid, today) >= reward.points~, evaluated against the *true signed sum* and never the floored display figure — which D41 pre-authorizes in as many words. A parent's direct-redeem is bound by exactly the same check as a child's request; there is no override and no IOU. It is checked twice: at request time on the kiosk, where an unaffordable reward renders visibly locked and is not tappable (a rendering rule before it is a validation), and at approval time in admin, where affordability *and* availability are re-validated because both the balance and the catalog move between ask and answer; a failed re-check tells the parent which check failed rather than silently no-opping. Rejected — a parent override redeeming into visible debt (a D49-shape escape hatch): D49's override is legitimate because it nets a penalty in the child's favor against a boundary the child could not cross alone; an affordability override runs the opposite way — it manufactures debt the child never agreed to, on a surface (D41/D43) that structurally cannot display a negative number. Rejected — checking against the floored total ~max(0, balance)~: forgives real debt at spend time and contradicts D41's explicit instruction that a balance-needing feature sums the raw signed rows. Rejected — a reserve/hold model escrowing a pending request's price: creates two balances (true and available) that the always-visible badge (D43) cannot show at once, so the badge would either drop when nothing has been earned or spent, or disagree with what the child can actually afford; the race it would close is closed instead by the approval-time re-check plus the one-open-request index ([[#d61][D61]]) at zero display cost. Settles rewards/SC-4. → rewards/Key Behaviors (affordability), rewards/Screens (kiosk card states, admin).

* D64 (2026-07-25) — Redemption widens hidden debt: the kiosk keeps the floor and explains with a lock glyph; every admin balance shows the true signed figure
:PROPERTIES:
:CUSTOM_ID: d64
:END:

A child at true balance 2 who then takes a fail sits at true −3 while the badge honestly shows 0. Redemption does not create this — D41 accepted it explicitly ("two kids both showing 0 may have different true standings") — but it *widens* it, because a spend is now a routine way to arrive near the floor, so the trade-off is re-ruled here rather than silently inherited. No spend-time guard can close it: affordability ([[#d63][D63]]) already guarantees a spend never drives the balance below zero, so all residual debt comes from fails *after* a spend, by construction outside any spend-time check. The resolution is not to hide less on the kiosk but to hide nothing in admin. The kiosk keeps the floor (D41, D43) and the *lock glyph on an unaffordable card is the pre-reader-legible explanation* — a child who cannot read a negative number can read a lock, and needs no number to act correctly. Admin displays the true signed balance, negative included, wherever it shows one (approval queue, direct-redeem picker, history), because the parent is a reader and the only party who can act on debt. Rejected — surfacing the true negative on the kiosk: contradicts D41/D43's floor and shows a pre-reader a number they cannot act on. Rejected — floored balances in admin for consistency with the kiosk: hides the only signal the party who can act on it needs. Settles rewards/SC-3, rewards/SC-4, rewards/SC-6. → rewards/Key Behaviors (hidden debt), rewards/Screens (kiosk card states, balances in admin).

* D65 (2026-07-25) — The kiosk shop is a per-column ~:rewards~ state opened by a gift button in the banner; one tap to ask, dismiss plus idle auto-return
:PROPERTIES:
:CUSTOM_ID: d65
:END:

A new per-column state ~:rewards~ joins the shipped ~:rows~ and ~:band~: the column *body* (events strip, routine card region, extras) swaps to the reward catalog while the kid-color banner stays, so the points badge is visible while shopping and the column stays identifiable by color at a distance. The sibling's column is untouched. Because the view replaces the routine region rather than sitting beside it, the 5-chore no-scroll budget is untouched; the only permanent addition is the horizontal banner button. The banner becomes ~[completion icon] NAME [★ points] [🎁]~ with the gift button grouped beside the badge — badge and shop are the same economy — and the button has exactly two states, =🎁= idle and =⏳= while this kid has a pending request today. Asking is *one tap, no modal, no confirmation*, with ~phx-throttle~ matching the shipped chore row: the deliberate contrast to D53, which guards actions that are irreversible and punitive, where a request is neither (the parent gate is the protection, a mistaken ask costs a decline) — and a confirmation dialog is the wrong instrument for a pre-reader anyway. There is no kid-side withdraw; the escapes are the parent's decline and the overnight lapse. An explicit ✕ dismisses, and an idle timer (~Process.send_after~, reset on each tap inside the view, cancelled on dismiss) returns the column to normal if the child wanders off; its duration is a module constant tuned at the on-device gate, as ~@collapse_delay_ms~ was. ~:rewards~ membership is a socket-level ~MapSet~ of kid ids like the shipped ~@expanded~ — never persisted. [[#d56][D56]] outranks it absolutely: at night no columns render, so the state is unreachable rather than suppressed. Rejected — a full-screen takeover: bigger cards, but it locks a sibling out of their own chores for as long as the view is open on a shared fridge tablet. Rejected — an always-visible shelf as a fifth grid track: permanently spends vertical space against the no-scroll budget in ~:rows~, where it is tightest. Rejected — using the points badge's own tap as the opener: would preclude the deferred points-stats affordance; the gift button is a distinct gesture and the badge's tap stays free. Settles rewards/SC-2 kiosk leg. → rewards/Screens (kiosk reward view, banner button, night), rewards/Key Behaviors (kiosk interaction).

* D66 (2026-07-25) — Reward cards resolve in one seven-row precedence table, with one dim treatment and three glyphs
:PROPERTIES:
:CUSTOM_ID: d66
:END:

Verdict states and availability states are the same kind of thing — derived from redemption markers plus ~local_date == today~ — so they resolve in one ordered table, first match wins: retired → absent; one-time consumed on an earlier day → absent; pending today → =⏳=; declined today → dim + declined glyph; claimed today → dim + check glyph; unaffordable → dim + lock glyph; otherwise a full-color, tappable card. Claimed outranks unaffordable because after a spend the balance is usually below the price again and "you got this today" is strictly more informative. The three dim states share one layout and differ only by glyph, so a pre-reader learns three glyphs against one layout rather than three layouts, and the pending =⏳= is the same glyph the banner button already carries. The declined row doubles as the *re-ask block*: a declined row does not occupy the one-open-request index slot ([[#d61][D61]]), so without this rendering rule a child could re-tap and re-queue indefinitely — deriving "declined for this reward today" and rendering it untappable closes the loop with no new storage (the D55 day-scope pattern). The reward card surface, the dim+glyph vocabulary, and the =🎁= / =⏳= button are new kids'-view indicators and are *pending design-language recordings*, written into ~docs/design-language.org~ by the story that lands them per the D48 / D54 pattern — the existing "do not add further indicators without a ruling" clause makes this mandatory. Rejected — hiding a consumed one-time reward immediately on approval: a child who saved 500 for the ATM toy would watch it vanish the instant it was granted, which reads as a bug rather than a triumph; it shows as claimed for the rest of the local day, then disappears. Rejected — showing consumed rewards as claimed permanently: dead cards accumulate forever in a catalog a pre-reader scans by glance. Rejected — hiding repeatable rewards while on cooldown: the catalog would change shape day to day with no visible reason. Settles rewards/SC-6. → rewards/Screens (kiosk card states, design-language recordings).

* D67 (2026-07-25) — Decline visibility is in-shop only: the card carries the glyph, the banner has no third state
:PROPERTIES:
:CUSTOM_ID: d67
:END:

The banner gift button has two states only ([[#d65][D65]]); a verdict returns it to =🎁=. A decline is surfaced by the declined glyph on the card ([[#d66][D66]]), which is there whenever the child next looks; an approval needs no banner state at all, because it drops the always-visible points badge by the reward's price — the most legible ambient signal the kiosk has. Human-ruled on family texture, not on engineering cost: a day-scoped third banner state would have been *free* (it reads off ~declined_at~ plus ~local_date == today~ and clears at date rollover, the D55 pattern), but at family scale the "no" is delivered out loud by a parent, and an all-day ambient "no" pinned to the child's own banner would rub it in rather than inform. Rejected — a third banner state after a verdict lands, for exactly that reason. rewards/SC-6's "can see the day's answer to a request they made" is discharged by the card glyph. Settles rewards/SC-6 verdict placement. → rewards/Screens (kiosk banner button, card states).

* D68 (2026-07-25) — ~/admin/rewards~ owns the catalog: ~position~ ordering, archive over deletion, and the direct-redeem action
:PROPERTIES:
:CUSTOM_ID: d68
:END:

A new route and a fifth bottom-nav entry, scaffolded with ~mix phx.gen.live~ mirroring ~ChoreLive.Index~ / ~ChoreLive.Form~ per repo convention; the form carries name, emoji (default =🎁=), price, repeatable, and an optional kid ("Anyone" or a specific child). Ordering is parent-controlled ~position~ with ▲/▼, and ~move_reward/2~ is written from the shipped ~move_chore/2~ as its template — same neighbor swap in a transaction, same silent no-op at the list edge, same broadcast; the ~is_nil/1~ bucketing discipline from ~docs/learnings.org~ [2026-07-12] does not apply, because the reward list is a single flat bucket with no nullable discriminator to bucket by. Removal is ~retired_at~ — *archive, never delete*: an archived reward vanishes from the kiosk catalog and the direct-redeem picker and remains in the history view with its redemptions intact. Direct-redeem lives here as a per-reward action opening a kid picker showing each kid's true signed balance, then a ~data-confirm~ guard (D53), then the spend, bound by the same affordability and availability rules as the kid leg with no override ([[#d63][D63]]). Rejected — a hard delete with FK cleanup: either cascades and destroys the audit trail outright, or nilifies ~reward_id~ and strands redemption rows with no identity — which would then force the ~reward_name~ snapshot [[#d60][D60]] correctly declined, adding storage purely to enable a capability that is not wanted. Settles rewards/SC-1, rewards/SC-2 parent-direct leg. → rewards/Screens (admin catalog).

* D69 (2026-07-25) — Requests and reversal live on ~Admin.TodayLive~; approve is confirmation-guarded and reversal is day-scoped
:PROPERTIES:
:CUSTOM_ID: d69
:END:

A per-kid *Requests* section joins the existing Extras section in today's stacked layout — Today is where a parent already looks, and a request is a day-shaped object like everything else on that screen. Each pending request shows the reward's icon and name, its snapshot price, and the kid's true signed balance. Approve is ~data-confirm~ guarded per D53 (points-affecting, and permanent from tomorrow onward since the reversal control is day-scoped) and re-validates affordability *and* availability before stamping ~approved_at~, naming which check failed on a failure; decline carries no confirmation, because it costs nothing, contributes 0, and its own day-scoping is the undo. Lapsed rows never surface: the queue reads ~requested_at~ set, no verdict, ~local_date == today~ — yesterday's unanswered ask is invisible here by the same predicate that hides it on the kiosk, with nothing to clean up. Reversal lives on the same screen beside the day's redemptions, ~data-confirm~ guarded, and is offered only on redemptions with ~local_date == today~ (D55 day-scoping): today is the correction surface, and a mis-tap gets caught the same day or not at all. Rejected — a reversal reachable from arbitrary history: an editing affordance over the permanent record rather than a same-day correction; the reversal's *effect* stays legible indefinitely in the history view ([[#d70][D70]]), which is where the durable record belongs. Rejected — a dedicated approvals route separate from Today: splits the parent's one daily surface for a handful of rows a day. Settles rewards/SC-2 parent leg, rewards/SC-8. → rewards/Screens (admin approval queue and reversal).

* D70 (2026-07-25) — Redemption history is one flat reverse-chronological list, bounded at 50 with the bound stated on screen
:PROPERTIES:
:CUSTOM_ID: d70
:END:

~/admin/rewards/history~ is a pure read surface over storage that already supports it (append-only rows, price snapshot, ~reversed_at~ marker): one flat reverse-chronological list on its own route, linked from the catalog, each row showing date, kid (name + color chip), reward icon and name, snapshot price, and state — approved / reversed / declined. A reversed row renders its price struck through with a "reversed" marker and the reversal's date, which is where a reversal stays legible days later. Lapsed rows are omitted: a never-answered ask is an abandoned intention, not history, and including it would bury real redemptions in noise. Archived rewards still appear with their name and icon — only the catalog and the direct-redeem picker filter on ~retired_at~. The list is bounded at the 50 most recent entries with no pagination and *says so on screen*, because a silent truncation would read as "this is everything" when it is not; at the stated family volume that spans months, comfortably covering rewards/SC-7's "days afterward" horizon, and pagination is a backlog seed if it ever bites. Rejected — per-kid or per-reward grouped views: at two kids and a few redemptions a week, one list beats navigation and filter UI, and the whole record fits on a phone screen. Settles rewards/SC-7. → rewards/Screens (admin history).

* D71 (2026-07-25) — ~BearCub.Rewards~ owns its own payload-free ="rewards"= PubSub topic
:PROPERTIES:
:CUSTOM_ID: d71
:END:

Every successful ~Rewards~ write broadcasts a bare ~:rewards_changed~ on topic ="rewards"=; ~KioskLive~ and ~Admin.TodayLive~ subscribe to it in addition to ="chores"= and re-fetch, never patching from a payload. This is the shipped ~Calendars.subscribe/0~ / ~:calendars_changed~ shape exactly — ~KioskLive~ already handles two independent topics with two ~handle_info/2~ clauses — so it costs one clause per subscriber and introduces nothing new. Rejected — reusing ="chores"= / ~:chores_changed~: the message would lie (a redemption is not a chore write) and, decisively, broadcasting on the chores topic would require ~Rewards~ to reach into ~Chores~ for the topic name or broadcast helper, inverting the dependency direction [[#d57][D57]] establishes. Rejected — a single generic ="dashboard"= topic: churns shipped, working code across two contexts for no behavioral gain. Settles rewards/SC-2 cross-surface liveness. → rewards/Key Behaviors (live updates).

* D72 (2026-07-25) — The points aggregate rewrite is claimed and lands early, as a pure performance refactor; roster drift is surfaced, not fixed
:PROPERTIES:
:CUSTOM_ID: d72
:END:

The deferred ~points_total/2~ N+1 rewrite is claimed as a batch member of this initiative and lands *before* affordability depends on it. The shipped derivation issues one query per distinct ~(routine, local_date)~ pair plus two per pair — roughly 1,460 queries per kid per render after a year — and runs on every broadcast on every connected surface; redemption makes it load-bearing three ways (the affordability check on every tap and every card render, the badge, and every admin balance display). The target is ~Points.balances(local_date) :: %{kid_id => signed_integer}~ in three grouped queries — extras, routine-days, redemptions — O(1) queries for a whole render, with ~Points.balance/2~ delegating to the map and ~Points.total~ / ~Points.totals~ applying the D41 floor on top. No memoization, no cached totals, no materialized column: the derived-state doctrine (D10) is not negotiable and the aggregate is fast enough that caching would buy nothing. Semantics stay *unchanged*: the claimed item is the query shape, not the derivation's meaning. This preserves a discovered defect — ~routine_day_contribution/3~ evaluates historical routine-days against the kid's *current* roster, so adding a morning chore today retroactively wipes past ~+R~ and deleting one can grant points never earned; redemption raises the stakes, since a roster edit can move a balance after a spend, potentially into debt with no fail involved. Roster drift is captured as its own backlog entry at initiative close: a fix needs its own ruling, and a ~chores.inserted_at~-bounded roster would handle additions but not deletions, so the honest fix is not obviously cheap. The pinning tests must include a roster-drift case — a routine-day evaluated after the roster changed — so behavior preservation is proven on the weird path; a refactor that silently "fixed" drift would be an unruled semantic change smuggled in under a performance story. Rejected — fixing drift inside the rewrite: a semantic change disguised as a refactor, and unruled. Rejected — deferring the rewrite to a later initiative: rewards/SC-9 requires the affordability check be cheap on every tap and stay cheap as months accumulate, which the shipped shape does not satisfy. Settles rewards/SC-9. → rewards/Key Behaviors (points aggregate rewrite, testing), rewards/Out of Scope.

* D73 (2026-07-25) — Lapse is terminal at the write layer: ~approve_redemption/3~ and ~decline_redemption/2~ refuse a lapsed row, not only a verdict-stamped one
:PROPERTIES:
:CUSTOM_ID: d73
:END:

Human-ruled correction, overruling the reading Story 03 shipped under. ~check_open/2~ (called by both ~approve_redemption/3~ and ~decline_redemption/2~) now requires the marker shape *and* ~local_date == today~; a lapsed row (no verdict, dated before today) returns ~{:error, :not_open}~, the same signal an already-answered row returns, since no surface distinguishes the two cases. Rejected — the shipped marker-only guard (a request with no verdict yet is answerable regardless of date, on the reading that [[#d61][D61]]'s lapse only frees the one-open-request slot and does not make the row inert). That reading is overruled: SC-6 ("an unanswered request does not carry over") and D61's own prose state a domain semantic, not a display behavior, so marker-only made them true only at the UI layer; concretely, a lapsed-then-approved row keeps its request-day ~local_date~, so it is born outside [[#d69][D69]]'s day-scoped reversal window (~local_date == today~) — the least-intentional class of approval produces the one spend the admin correction surface can never reach, while still backdating the spend into history via ~spend_totals_by_kid~'s ~local_date <= filter~. No surface exposes lapsed rows today, so the flexibility given up is IEx-level only. Settles rewards/SC-6 (write-layer enforcement, not merely display). → rewards/Screens (admin approval queue and reversal).

* D74 (2026-07-25) — A reward's audience is enforced at the write layer: ~direct_redeem/4~ and ~approve_redemption/3~ refuse a kid outside a kid-scoped reward's ~kid_id~, not only the kiosk's display filter
:PROPERTIES:
:CUSTOM_ID: d74
:END:

Human-ruled correction to Story 04's shipped code, closing its own recorded Deferred discovery by fixing rather than sweeping it. ~kid_id~'s "who this is offered to" ([[#d58][D58]]) was enforced only by ~list_rewards/2~'s kiosk-facing audience filter; neither ~direct_redeem/4~ nor ~approve_redemption/3~ checked it, so the domain permitted redeeming a kid-scoped reward for the wrong kid. A new ~offered?/2~ predicate and ~check_audience/2~ guard land in both write paths' ~with~ chains, checked before availability and before affordability — the most categorical refusal first — returning ~{:error, :not_offered}~ as a third named refusal alongside ~:unavailable~/~:unaffordable~ ([[#d63][D63]]). ~approve_redemption/3~ gets it as defense in depth against the same-kind of drift [[#d62][D62]]'s same-kid interleaving already guards: the kiosk leg is audience-correct by construction today (a kid only ever requests from an audience-filtered catalog), but the domain should not depend on that remaining true — concretely, a reward's audience can narrow between a kid's ask and a parent's approval. The admin direct-redeem kid picker ([[#d68][D68]]) now filters to the reward's audience via the same ~offered?/2~ predicate, so the picker is a rendering of the domain rule rather than a second copy of it. This applies [[#d73][D73]]'s principle — a PRD criterion is a domain semantic, not a display behavior — to rewards/SC-1's "chooses for each whether it is offered to one child or to both." Rejected — Story 04's recorded disposition, leaving the mismatch as display-only enforcement swept to the backlog untriaged: rejected for coherence with D73, not because the exposure is severe — unlike D73's lapsed-approval case, a mis-redemption here carries no trap (it is same-day reversible via [[#d69][D69]]'s reversal control), so this ruling rests on layer-consistency, not on any discovered harm. Settles rewards/SC-1 (write-layer enforcement, not merely display). → rewards/Screens (admin catalog, direct-redeem picker), rewards/Key Behaviors (contexts and dependency direction).

* D75 (2026-07-25) — Request feedback stays in the shop: the tapped card turns pending in place, and the banner gift button becomes an open/close toggle
:PROPERTIES:
:CUSTOM_ID: d75
:END:

Human-ruled from hands-on localhost QA of the shipped batch. A successful request no longer returns the column to its normal state: the tapped card transitions in place to its pending rendering (full-color =⏳=, precedence row 3 of [[#d66][D66]]) and the shop stays open. The shop now closes three ways — the explicit ✕, the banner gift button (which becomes a toggle: tapping =🎁= or =⏳= while the shop is open closes it), and the idle timer, which becomes the primary auto-close now that requesting no longer closes the view; a request tap is an in-view tap and resets that timer like any other. Feedback belongs at the point of action: a child who taps a card and is immediately thrown back to the routine column has to infer that anything happened from a glyph change on a banner they were just moved away from, whereas the card changing under their finger is the acknowledgment, and it is the same rendering they will see when they next open the shop. Rejected — the shipped instant return ([[#d65][D65]]: "after a successful request the view returns to the normal column immediately and the banner button flips to =⏳=, so the pending state lives where the child can see it without standing in the shop"). It still looks attractive because it guarantees the column returns to chores with no further tap, but the idle timer already guarantees that on its own, and the banner =⏳= is a strictly weaker acknowledgment than the card. The rest of [[#d65][D65]] stands unchanged: one tap with no modal or confirmation, the ephemeral socket-level ~MapSet~, ✕ plus idle auto-return, and [[#d56][D56]] outranking the state absolutely. Settles rewards/SC-2 (kiosk leg feedback), rewards/SC-6. → rewards/Screens (kiosk banner button, kiosk card states), rewards/Key Behaviors (kiosk interaction).

* D76 (2026-07-25) — Kid-side withdraw: a fourth nullable marker ~withdrawn_at~, pending-only, no confirmation
:PROPERTIES:
:CUSTOM_ID: d76
:END:

Human-ruled from the same QA round. Tapping a pending card withdraws the request. Storage is a fourth nullable marker, ~redemptions.withdrawn_at~ — the ~undone_at~ / ~failed_at~ / ~declined_at~ pattern ([[#d59][D59]]), never a row deletion, so audit permanence holds and the row records that the child asked and took it back. Withdraw is *pending-only*: an approved or declined row refuses it with the ~{:error, :not_open}~ family, the same guard shape [[#d73][D73]] gave the other write paths, and a withdrawn row is likewise refused by ~approve_redemption/3~ and ~decline_redemption/2~. No confirmation: the action is reversible by simply asking again, which is the one-tap philosophy [[#d65][D65]] already rules for the ask itself, and a confirmation dialog is the wrong instrument for a pre-reader on either leg. A withdrawn row is excluded by ~pending?~ everywhere — the kiosk card falls back to its plain state, the admin queue drops the row live through the existing payload-free broadcast ([[#d71][D71]]) with no new machinery, and the history view omits it exactly as it omits a lapse ([[#d70][D70]]): an intention the child took back is not a redemption record. It contributes 0, the "otherwise" leg of [[#d59][D59]]'s marker-first ~cond~. Rejected — the shipped position ([[#d65][D65]]: "No kid-side withdraw. The escapes are the parent's decline and the overnight lapse"). It still looks attractive as one fewer marker and one fewer state on a surface that must stay pre-reader legible, but in field use a mis-tap leaves a child with an ask they cannot take back and a parent with a queue row to dispose of on the child's behalf — and once several asks may be open at once ([[#d77][D77]]) a mis-tap also locks cards the child can no longer afford to want. Rejected — deleting the withdrawn row: it erases the trail, and every other correction in this app is a marker. Settles rewards/SC-6. → rewards/Data Model, rewards/Screens (kiosk card states), rewards/Key Behaviors (kiosk interaction), rewards/Out of Scope.

* D77 (2026-07-25) — Multiple same-day requests, bounded by a rendering reserve rule; the open-request index is re-keyed to ~(kid_id, reward_id, local_date)~ — supersedes [[#d61][D61]]'s one-open-request cap
:PROPERTIES:
:CUSTOM_ID: d77
:END:

Human-ruled from the same QA round. A kid may hold several pending requests at once, provided their points cover the whole set. The bound is a *rendering* rule, not a ledger hold: the kiosk lock condition (precedence row 6 of [[#d66][D66]]) becomes ~balance − (sum of this kid's pending request prices) < reward.points~, with ~BearCub.Rewards~ exposing the pending-commitment read (e.g. ~pending_total/2~). ~Points.balance/2~ is untouched and a pending row still contributes 0 ([[#d59][D59]]). The reserve considered and rejected in the design era was a *domain* hold escrowing the price against the balance itself, and *that rejection stands* ([[#d63][D63]]): the always-visible badge (D43, via [[#d64][D64]]) still shows the true floored total and never an available-after-commitments figure, and the lock glyph remains the pre-reader-legible explanation for "not right now" — a child needs no number to act correctly. The approval-time re-check remains the domain gate and is order-safe: approving one ask can make a sibling ask's re-check fail ~:unaffordable~, which is expected rather than a defect and is already carried by the named-refusal display [[#d63][D63]] requires. The partial unique index is *replaced, not extended*: one open ask per ~(kid_id, reward_id, local_date)~ WHERE ~requested_at IS NOT NULL~ and all three of ~approved_at~ / ~declined_at~ / ~withdrawn_at~ ([[#d76][D76]]) are NULL. The UI prevents same-reward duplicates by construction — a pending card's tap is now a withdraw ([[#d76][D76]]) — so the index catches races only, the posture [[#d61][D61]] already took toward a racing duplicate; keeping ~local_date~ in the key keeps lapse free exactly as before. SQLite cannot alter an index in place, so the migration drops and recreates it. Rejected — keeping one open ask per kid per day ([[#d61][D61]]): overturned by field use, since a child who wants two things must ask, wait for an answer, and only then ask again, which at family answer latency means the second ask usually never happens that day. Rejected — a domain-level hold escrowing pending prices against the balance: ledger purity, on [[#d63][D63]]'s own reasoning, which is why the bound lives in the rendering rule instead. Rejected — unbounded multi-request with no reserve rule at all: mutually exclusive asks pile into the parent's queue, most of them to be declined for an affordability limit the child was never shown. Supersedes [[#d61][D61]]'s one-open-request-per-kid cap; [[#d61][D61]]'s derived-and-free lapse property is retained intact. Settles rewards/SC-4 (display bound), rewards/SC-6. → rewards/Data Model (indexes), rewards/Screens (kiosk card states, kiosk banner button, admin approval queue), rewards/Key Behaviors (availability, affordability).
