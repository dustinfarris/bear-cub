#+title: Story 02 — Aggregate rewrite: ~Points.balances/1~ in grouped queries
#+date: 2026-07-25

Design: [[file:../DESIGN.org]]

* Design

Verbatim from DESIGN.org for this story's scope.

** The points aggregate rewrite (claimed batch member)

The problem, sized: ~points_total/2~ issues one query for distinct ~(routine, local_date)~ pairs, then calls ~routine_day_contribution/3~ — two more queries — per pair. After a year of use that is roughly 2 routines × 365 days ≈ 1,460 queries per kid, per render, and it runs on every ~:chores_changed~ broadcast on every connected surface. Correctness is unaffected today, which is why it was deferred. Redemption makes it load-bearing three ways: the affordability check runs on *every tap* and on every reward-card render, the badge reads it, and admin reads it for every balance display. It lands *early in the batch*, before affordability depends on it (SC-9).

The target shape: ~Points.balances(local_date) :: %{kid_id => signed_integer}~ — O(1) queries for an entire render, not O(kids × days). Three grouped queries:

1. *Extras* — one aggregate over ~completions~ joined to ~chores~ where ~routine IS NULL~, summing a ~CASE~ over ~failed_at~ / ~undone_at~ (the shipped ~extra_contribution/2~ ladder expressed in SQL), grouped by ~kid_id~.
2. *Routine-days* — one query grouping completions by ~(kid_id, routine, local_date)~ computing a live-completion count and an any-failed boolean, joined against a per-~(kid_id, routine)~ chore-count subquery; ~complete? = chore_count > 0 AND live_count == chore_count~, ~failed?~ from the boolean, then the capped ~+R~ / ~−R~ arithmetic.
3. *Redemptions* — one aggregate summing ~points~ over rows with ~approved_at IS NOT NULL AND reversed_at IS NULL~, grouped by ~kid_id~.

~Points.balance(kid, local_date)~ delegates to the map for a single kid, so tests and single-kid admin reads keep an ergonomic entry point. ~Points.total~ / ~Points.totals~ apply the D41 floor on top.

There is *no memoization, no cached totals, no materialized column* — the derived-state doctrine (D10) is not negotiable here, and the aggregate is fast enough that caching would be complexity buying nothing.

Discovered property — *roster drift*, surfaced but not fixed: ~routine_day_contribution/3~ loads the kid's *current* chore roster for the routine and requires every one of those chores to have a live completion on the evaluated date. Historical routine-days are therefore evaluated against today's roster: adding a morning chore today retroactively makes every past morning-day incomplete, wiping all historical ~+R~; deleting one can retroactively complete past days and grant points that were never earned. This ships today, and the badge already moves when a parent edits chores. Redemption raises the stakes: a roster edit can move a balance *after* a spend, potentially into debt with no fail involved.

Semantics stay *unchanged* in the rewrite — it is a pure performance refactor, proven behavior-preserving by tests — and roster drift is captured as its own backlog entry at initiative close. The claimed item is the query shape, not the derivation's semantics.

The pinning tests *must include a roster-drift case* — a routine-day evaluated after the roster changed — so behavior preservation is proven on the weird path and not merely the happy one. A refactor that silently "fixed" drift would be an unruled semantic change smuggled in under a performance story.

** Testing and the on-device gate (excerpt)

- *Unit* — ... the ~Points.balances~ aggregate *pinned against the current per-kid implementation*, including the roster-drift case.

- *Time* — every test pins ~:routine_windows~ per ~docs/learnings.org~ [2026-07-17] rather than inheriting the wall clock, and all new derivations take a local-date argument. Day-rollover behavior (lapse, cooldown reset, verdict-state expiry) is tested by *passing a different local date*, never by mocking a clock.

** Decision Log entries in scope

- *[[file:../../decisions.org::#d72][D72]] (2026-07-25) — The points aggregate rewrite is claimed and lands early, as a pure performance refactor; roster drift is surfaced, not fixed:* The deferred ~points_total/2~ N+1 rewrite is claimed as a batch member of this initiative and lands *before* affordability depends on it. The shipped derivation issues one query per distinct ~(routine, local_date)~ pair plus two per pair — roughly 1,460 queries per kid per render after a year — and runs on every broadcast on every connected surface; redemption makes it load-bearing three ways (the affordability check on every tap and every card render, the badge, and every admin balance display). The target is ~Points.balances(local_date) :: %{kid_id => signed_integer}~ in three grouped queries — extras, routine-days, redemptions — O(1) queries for a whole render, with ~Points.balance/2~ delegating to the map and ~Points.total~ / ~Points.totals~ applying the D41 floor on top. No memoization, no cached totals, no materialized column: the derived-state doctrine (D10) is not negotiable and the aggregate is fast enough that caching would buy nothing. Semantics stay *unchanged*: the claimed item is the query shape, not the derivation's meaning. This preserves a discovered defect — ~routine_day_contribution/3~ evaluates historical routine-days against the kid's *current* roster, so adding a morning chore today retroactively wipes past ~+R~ and deleting one can grant points never earned; redemption raises the stakes, since a roster edit can move a balance after a spend, potentially into debt with no fail involved. Roster drift is captured as its own backlog entry at initiative close: a fix needs its own ruling, and a ~chores.inserted_at~-bounded roster would handle additions but not deletions, so the honest fix is not obviously cheap. The pinning tests must include a roster-drift case — a routine-day evaluated after the roster changed — so behavior preservation is proven on the weird path; a refactor that silently "fixed" drift would be an unruled semantic change smuggled in under a performance story. Rejected — fixing drift inside the rewrite: a semantic change disguised as a refactor, and unruled. Rejected — deferring the rewrite to a later initiative: rewards/SC-9 requires the affordability check be cheap on every tap and stay cheap as months accumulate, which the shipped shape does not satisfy. Settles rewards/SC-9. → rewards/Key Behaviors (points aggregate rewrite, testing), rewards/Out of Scope.

* User Story

As a kid tapping a reward card, I need the check of what I can afford to be answered from a couple of queries rather than one per day of history, so that the kiosk stays fast on every tap and stays fast after months of completions have piled up.

* Acceptance Criteria

** [X] The Story-01 pinning suite passes UNMODIFIED — not one expected value, fixture, or assertion is edited to accommodate the rewrite. (SC-9, D72)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:33] \\
  Verified: ~git diff~ / ~git status~ show ~test/bear_cub/points_test.exs~ untouched throughout the rewrite. ~mix test test/bear_cub/points_test.exs~ → ~Result: 27 passed~, 0 failures, reproduced across multiple runs including after the ~routine_days_by_kid/1~ subquery revision. Independently confirmed by the ~story-verifier~ subagent audit.
:END:
** [X] ~Points.balances(local_date)~ returns ~%{kid_id => signed_integer}~ for every kid in one pass, computed by grouped aggregate queries rather than per-kid, per-routine-day derivation. (SC-9, D72)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:33] \\
  ~lib/bear_cub/points.ex~'s ~balances/1~ now builds the map from ~Chores.earnings_by_kid/1~ (two grouped queries) merged over ~Chores.list_kids()~, rather than looping ~balance/2~ per kid. Verified by the unmodified pinning suite's ~"whole-render map forms (D57)"~ describe block and a new ~chores_test.exs~ test ("matches earnings/2 for each kid") asserting pointwise equivalence with the old per-kid derivation.
:END:
** [X] The extras leg is a single aggregate over ~completions~ joined to ~chores~ where ~routine IS NULL~, summing the shipped ~extra_contribution/2~ ladder (failed → ~−points~, undone → 0, live → ~+points~) grouped by ~kid_id~. (SC-9, D72)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:33] \\
  ~lib/bear_cub/chores.ex~'s ~extras_by_kid/1~ (private) is one ~Repo.all~ over ~Completion~ joined to ~Chore~ where ~is_nil(ch.routine)~, ~group_by: ch.kid_id~, selecting a ~fragment~-based ~SUM(CASE WHEN failed_at ... WHEN undone_at ... ELSE points END)~ mirroring ~extra_contribution/2~'s ~cond~ ladder exactly (failed_at checked first). Verified via the story-verifier subagent's direct telemetry probe (1 query emitted) and the unmodified extras pins.
:END:
** [X] The routine-days leg is a single query grouping completions by ~(kid_id, routine, local_date)~ against a per-~(kid_id, routine)~ chore-count subquery, applying ~complete? = chore_count > 0 AND live_count == chore_count~ and the capped ~+R~ / ~−R~ arithmetic — two fails in a routine-day still contribute a single ~−R~. (SC-9, D72, D40)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:33] \\
  Initially shipped as two ~Repo.all~ calls (a chore-count map plus the per-day aggregate) merged in Elixir — a story-verifier audit flagged this as a CONCERN against the AC's literal "single query" text. Rewritten to use Ecto's ~subquery/1~: ~routine_days_by_kid/1~ now issues exactly one ~Repo.all~, joining the chore-count query in SQL (~join: cc in subquery(chore_counts), on: cc.kid_id == ch.kid_id and cc.routine == ch.routine~). The row-reduce arithmetic (~row.chore_count > 0 and row.live_count == row.chore_count~; capped ~±R~ via a ~MAX(CASE ...)~ any-failed flag) is unchanged and re-verified by a follow-up story-verifier audit. Confirmed against the unmodified roster-drift and two-fails-one-penalty pins.
:END:
** [X] The query count for a whole-render balance read does not grow with the number of days of history — verified by exercising the aggregate over a fixture spanning many routine-days and asserting the derived values, with the query shape stated in the code, not merely intended. (SC-9, D72)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:33] \\
  New test in ~test/bear_cub/chores_test.exs~, describe ~"earnings_by_kid/1 (Story 02, D72)"~: ~"the query count for a whole-render read does not grow with days of history"~ attaches ~:telemetry~ to ~[:bear_cub, :repo, :query]~, compares the query count for a 1-day fixture against a 60-day fixture on the same kid/chore, and asserts equality — a real assertion that would fail under the old N+1 shape. Passes in isolation and as part of the full 74-test file run.
:END:
** [X] ~Points.balance/2~ delegates to the map for a single kid, and ~Points.total/2~ / ~Points.totals/1~ apply the ~max(0, …)~ floor on top of it. (SC-3, D72, D41)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:33] \\
  ~lib/bear_cub/points.ex~: ~balance/2~ now calls ~balances/1~ then ~Map.fetch!(kid.id)~ rather than calling ~Chores.earnings/2~ directly; ~total/2~ stays ~max(0, balance(kid, local_date))~; ~totals/1~ maps ~balances/1~ through ~max(0, balance)~ per kid. Verified by the unmodified pinning suite (all four functions exercised) and by direct code read.
:END:
** [X] No memoization, no cached total, and no materialized column is introduced — the balance stays fully derived on read. (D10, D72)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:33] \\
  ~grep -rniE "cachex|persistent_term|:ets\.|ETS\."~ over ~chores.ex~/~points.ex~ returns nothing. No new schema field, migration, or process-based cache was introduced; ~earnings_by_kid/1~ and ~balances/1~ recompute from ~completions~/~chores~ on every call.
:END:
** [X] Roster-drift behavior is unchanged: the Story-01 roster-drift pin produces the same value after the rewrite as before it. (D72)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:33] \\
  The ~"roster drift (D72)"~ describe block in the unmodified pinning suite (asserting through both ~balance/2~ and ~balances/1~) passes unchanged: ~routine_days_by_kid/1~'s chore-count leg counts the kid's current roster via the ~Chore~ join, exactly reproducing ~routine_day_contribution/3~'s drift behavior (adding a chore retroactively wipes a past ~+R~; deleting one retroactively grants one).
:END:
** [X] The kiosk points badge and every admin balance display show exactly what they showed before this story. (SC-3, D41)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:33] \\
  ~git diff --stat~ against the pre-story tree shows only ~lib/bear_cub/chores.ex~, ~lib/bear_cub/points.ex~, and ~test/bear_cub/chores_test.exs~ changed — no file under ~lib/bear_cub_web/~ was touched. Since ~Points.total/2~/~Points.totals/1~'s signatures and outputs are unchanged (per the pins above), every caller (kiosk badge, admin balance reads) is unaffected by construction.
:END:

* Definition of Done

** [X] All acceptance criteria have been either met or have had a documented removal or deferral
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:36] \\
  All 9 Acceptance Criteria above are marked ~[X]~ with LOGBOOK evidence; none deferred or removed. Confirmed by a dedicated story-verifier subagent audit of the file state.
:END:
** [X] Tests written and passing for the acceptance criteria in this story
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:36] \\
  ~mix test test/bear_cub/points_test.exs~ → ~27 passed~ (unmodified pinning suite); ~mix test test/bear_cub/chores_test.exs~ → ~74 passed~, including the new ~"earnings_by_kid/1 (Story 02, D72)"~ describe block (equivalence test, absent-kid test, and the telemetry-based query-count-invariance test). Full suite ~mix test~ → ~307 passed~, reproduced across three consecutive runs.
:END:
** [X] Code follows style guidelines in CLAUDE.md
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:36] \\
  ~%Kid{}~/~%Date{}~ struct guards present on all new/changed public functions; ~@doc~ present on every public function (~balance/2~, ~total/2~, ~balances/1~, ~totals/1~, ~earnings_by_kid/1~); private helpers (~extras_by_kid/1~, ~routine_days_by_kid/1~) correctly left undocumented. No ~String.to_atom~ on user input; no cache/ETS/persistent_term usage. ~mix format --check-formatted~ exits 0. Confirmed by story-verifier subagent review.
:END:
** [X] The repo's declared ~Verify command:~ (CLAUDE.md; default ~mix format --check-formatted~ and ~mix test~) passes without errors
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:36] \\
  ~mix precommit~ (~compile --warnings-as-errors~, ~deps.unlock --unused~, ~format~, ~test~) final line: ~Result: 307 passed~, 0 failures, no compile warnings, no unused deps, no formatting diffs. Reproduced clean across three consecutive runs after the test database was restored from an errant mid-session ~MIX_ENV=test mix ecto.reset~ (which had run ~priv/repo/seeds.exs~ against the test db outside any sandbox transaction — unrelated to this story's code, fixed by a plain drop/create/migrate with no seeding).
:END:

* Technical Notes

The Design excerpt above is the source of truth; DESIGN.org is broader context.

- Depends on Story 01: the module boundary (~BearCub.Points~, ~Chores.earnings/2~) and the pinning suite both come from there, and this story's acceptance rests on those pins passing untouched.
- *The pins are the contract.* If a pin fails, the rewrite is wrong — do not adjust the pin. Editing a pin to make the rewrite pass is precisely the smuggled semantic change D72 forbids.
- The design names three grouped queries. Two of them — extras and routine-days — are implementable here. The third, the redemptions aggregate, has no table to query until Story 03's migration lands, so the spend term contributes nothing in this story and Story 03 adds the third leg alongside the table it reads.
- ~R~ comes from ~BearCub.Routines.bonus/0~; keep it out of the SQL as a parameter, not a literal.
- Watch the SQLite/Ecto expression surface: the ~CASE~ ladder and the boolean any-failed aggregate need to work under ~ecto_sqlite3~; prefer ~fragment/1~ over cleverness, and keep the resulting arithmetic in Elixir where it is easier to read against the shipped ladder.
- Pin ~:routine_windows~ in every test per ~docs/learnings.org~ [2026-07-17]; day-dependent behavior is exercised by passing a different local date.
- Roster drift stays as-is. Its backlog entry is written at initiative close, not here.
- Deferred discovery: ~mix ecto.reset~'s alias (~mix.exs~) chains ~ecto.drop~ → ~ecto.setup~, and ~ecto.setup~ runs ~priv/repo/seeds.exs~ — so running it with ~MIX_ENV=test~ permanently inserts the placeholder ~"Kid A"~/~"Kid B"~ seed data straight into the test database, outside any sandbox transaction. It reproduced as a run of spurious cross-test failures (leaked kid/chore rows, id drift) during this story's own verification, confirmed unrelated to the rewrite by reproducing it identically against a ~git stash~ of pre-story code. Fix used: ~MIX_ENV=test mix ecto.drop~ then ~ecto.create~ then ~ecto.migrate~ directly, skipping the seeds step. Out of scope here — a repo-tooling footgun, not a Points/Chores defect.
