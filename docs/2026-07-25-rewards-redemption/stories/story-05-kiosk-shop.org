#+title: Story 05 — Kiosk shop: the ~:rewards~ column state, banner button, and card precedence
#+date: 2026-07-25

Design: [[file:../DESIGN.org]]

* Design

Verbatim from DESIGN.org for this story's scope.

** Kiosk — the reward view

A new per-column state ~:rewards~, beside the shipped ~:rows~ and ~:band~. The column *body* — the events strip, the routine card region, and extras — swaps to the reward catalog; the kid-color banner stays, so the points badge is visible while shopping and the column remains identifiable by color at a distance. The sibling's column is untouched and fully usable.

Because ~:rewards~ replaces the routine region rather than sitting beside it, *the 5-chore no-scroll budget is untouched*. The only permanent addition to the column is the banner button, which is horizontal.

** Kiosk — the banner button

The banner becomes ~[completion icon] NAME [★ points] [🎁]~ — the gift button grouped with the points badge on the right, because the badge and the shop are the same economy and read as one unit.

The button has exactly *two* states: =🎁= idle, and =⏳= while this kid has a pending request today. A verdict returns it to =🎁=.

Decline visibility is *in-shop only*: the declined glyph rides the card (precedence row 4) and is there whenever the child next looks, while the banner returns to =🎁=. An approval needs no banner state either — it drops the always-visible points badge by the reward's price, the most legible ambient signal the kiosk has.

The points badge's own tap gesture stays *unbound* — the gift button is a distinct gesture, so the deferred points-stats affordance is not precluded.

** Kiosk — card states, one precedence table

Verdict states and availability states are the same kind of thing — derived from redemption markers plus ~local_date == today~ — so they resolve in one ordered table rather than two overlapping systems. Evaluated top-down, first match wins:

| # | condition                                                        | rendering               | tappable |
|---+------------------------------------------------------------------+-------------------------+----------|
| 1 | ~retired_at~ set                                                 | absent                  | —        |
| 2 | one-time, this kid consumed it, and that redemption is not today | absent                  | —        |
| 3 | this kid has a pending request for this reward today             | =⏳= pending treatment  | no       |
| 4 | this kid was declined for this reward today                      | dim + declined glyph    | no       |
| 5 | this kid claimed this reward today                               | dim + check glyph       | no       |
| 6 | ~Points.balance(kid, today) < reward.points~                     | dim + lock glyph        | no       |
| 7 | otherwise                                                        | full-color card         | *yes*    |

Notes on the ordering and the choices:

- *Claimed (5) outranks unaffordable (6)* — after a spend the balance is usually below the price again, and "you got this today" is strictly more informative than "you can't afford this".
- *Rows 4–6 share one dim treatment and differ only by glyph* — declined, check (claimed today), lock (can't afford). A pre-reader learns three glyphs against *one* layout rather than three layouts, and the pending row's =⏳= is the same glyph already carried by the banner button. Extending the existing "not now" vocabulary costs no new visual grammar (SC-6).
- *Consumed one-time rewards show as claimed for the rest of the local day, then disappear.* A child who saved 500 for the ATM toy should not watch it vanish the instant it is granted — that reads as a bug rather than a triumph. Showing "claimed" permanently is the other failure: dead cards would accumulate forever in a catalog a pre-reader scans by glance.
- *Repeatable rewards on cooldown are dimmed, not hidden* — a catalog that changes shape day to day with no visible reason is the least explicable thing a shelf can do.
- *Row 4 doubles as the re-ask block.* A declined row does not occupy the one-open-request index slot (it has ~declined_at~ set), so without this rendering rule a child could re-tap the same reward immediately and re-queue it indefinitely. Deriving "declined for this reward today" and rendering it untappable closes the loop with no new storage — the D55 day-scope pattern.

** Kiosk — night (D56)

D56 outranks ~:rewards~ *absolutely*. Between 23:00 and 05:00 the kiosk renders one dark screen with one moon and nothing else — no columns, therefore no banner, no gift button, no reward view, and nothing rewards-related that could be tapped. Because columns simply do not render at night, this needs no special case: the state is unreachable rather than suppressed. An idle timer that happens to fire at night is a harmless no-op re-render.

** Kiosk — design-language recordings

The reward card surface, the dim+glyph vocabulary for the three unavailable reasons, and the =🎁= / =⏳= banner button are all *new kids'-view indicators*. Per the D48 / D54 pattern they are *pending design-language recordings* — written into ~docs/design-language.org~ by the story that lands them, not at design time. The existing "do not add further indicators without a ruling" clause on the completion badge makes this mandatory, not optional.

** Kiosk interaction

One tap to ask — *no modal, no confirmation*, with ~phx-throttle~ matching the shipped chore row. This is the deliberate contrast to D53: D53 guards actions that are *irreversible* and punitive, and a request is neither — the parent gate *is* the protection, and a mistaken ask costs a decline. A confirmation dialog is also the wrong instrument on this surface, since a pre-reader cannot read it.

No kid-side withdraw. The escapes are the parent's decline and the overnight lapse.

Dismiss and idle auto-return: an explicit ✕ closes the view. In addition, an idle timer returns the column to its normal state if the child wanders off — a ~Process.send_after~ armed on open, reset on each tap inside the view, and cancelled on dismiss. The duration is a module constant tuned at the on-device gate, exactly as ~@collapse_delay_ms~ (currently 400ms) is; this design deliberately does not pin a number, though ~30s is the starting guess.

After a successful request the view returns to the normal column immediately and the banner button flips to =⏳=, so the pending state lives where the child can see it without standing in the shop.

State is *ephemeral*: ~:rewards~ membership is a socket-level ~MapSet~ of kid ids, exactly like the shipped ~@expanded~ — never persisted, cleared by the timer, and cleared on the routine boundary re-render.

** From hidden debt

- *Kiosk keeps the floor* (D41, D43). The *lock glyph on an unaffordable card is the pre-reader-legible explanation* — a child who cannot read a negative number can read a lock. No number is needed for the child to act correctly (SC-3, SC-6).

** Testing (excerpt)

- *LiveView integration* — kiosk: open, dismiss, idle auto-return, request, and each of the seven card states ...

- *Time* — every test pins ~:routine_windows~ per ~docs/learnings.org~ [2026-07-17] rather than inheriting the wall clock, and all new derivations take a local-date argument. Day-rollover behavior (lapse, cooldown reset, verdict-state expiry) is tested by *passing a different local date*, never by mocking a clock.

** Decision Log entries in scope

- *[[file:../../decisions.org::#d65][D65]] (2026-07-25) — The kiosk shop is a per-column ~:rewards~ state opened by a gift button in the banner; one tap to ask, dismiss plus idle auto-return:* A new per-column state ~:rewards~ joins the shipped ~:rows~ and ~:band~: the column *body* (events strip, routine card region, extras) swaps to the reward catalog while the kid-color banner stays, so the points badge is visible while shopping and the column stays identifiable by color at a distance. The sibling's column is untouched. Because the view replaces the routine region rather than sitting beside it, the 5-chore no-scroll budget is untouched; the only permanent addition is the horizontal banner button. The banner becomes ~[completion icon] NAME [★ points] [🎁]~ with the gift button grouped beside the badge, and the button has exactly two states, =🎁= idle and =⏳= while this kid has a pending request today. Asking is *one tap, no modal, no confirmation*, with ~phx-throttle~ matching the shipped chore row: the deliberate contrast to D53, which guards actions that are irreversible and punitive, where a request is neither. There is no kid-side withdraw; the escapes are the parent's decline and the overnight lapse. An explicit ✕ dismisses, and an idle timer (~Process.send_after~, reset on each tap inside the view, cancelled on dismiss) returns the column to normal if the child wanders off; its duration is a module constant tuned at the on-device gate, as ~@collapse_delay_ms~ was. ~:rewards~ membership is a socket-level ~MapSet~ of kid ids like the shipped ~@expanded~ — never persisted. [[file:../../decisions.org::#d56][D56]] outranks it absolutely: at night no columns render, so the state is unreachable rather than suppressed. Rejected — a full-screen takeover (locks a sibling out of their own chores on a shared fridge tablet); an always-visible shelf as a fifth grid track (spends vertical space against the no-scroll budget); using the points badge's own tap as the opener (would preclude the deferred points-stats affordance). Settles rewards/SC-2 kiosk leg. → rewards/Screens (kiosk reward view, banner button, night), rewards/Key Behaviors (kiosk interaction).

- *[[file:../../decisions.org::#d66][D66]] (2026-07-25) — Reward cards resolve in one seven-row precedence table, with one dim treatment and three glyphs:* Verdict states and availability states resolve in one ordered table, first match wins: retired → absent; one-time consumed on an earlier day → absent; pending today → =⏳=; declined today → dim + declined glyph; claimed today → dim + check glyph; unaffordable → dim + lock glyph; otherwise a full-color, tappable card. Claimed outranks unaffordable because after a spend the balance is usually below the price again and "you got this today" is strictly more informative. The three dim states share one layout and differ only by glyph, so a pre-reader learns three glyphs against one layout rather than three layouts. The declined row doubles as the *re-ask block*: a declined row does not occupy the one-open-request index slot (D61), so without this rendering rule a child could re-tap and re-queue indefinitely. The reward card surface, the dim+glyph vocabulary, and the =🎁= / =⏳= button are new kids'-view indicators and are *pending design-language recordings*, written into ~docs/design-language.org~ by the story that lands them per the D48 / D54 pattern — the existing "do not add further indicators without a ruling" clause makes this mandatory. Rejected — hiding a consumed one-time reward immediately on approval (a child who saved 500 for the ATM toy would watch it vanish the instant it was granted); showing consumed rewards as claimed permanently (dead cards accumulate forever); hiding repeatable rewards while on cooldown (the catalog would change shape day to day with no visible reason). Settles rewards/SC-6. → rewards/Screens (kiosk card states, design-language recordings).

- *[[file:../../decisions.org::#d67][D67]] (2026-07-25) — Decline visibility is in-shop only: the card carries the glyph, the banner has no third state:* The banner gift button has two states only (D65); a verdict returns it to =🎁=. A decline is surfaced by the declined glyph on the card (D66), which is there whenever the child next looks; an approval needs no banner state at all, because it drops the always-visible points badge by the reward's price — the most legible ambient signal the kiosk has. Human-ruled on family texture, not on engineering cost: a day-scoped third banner state would have been *free*, but at family scale the "no" is delivered out loud by a parent, and an all-day ambient "no" pinned to the child's own banner would rub it in rather than inform. Rejected — a third banner state after a verdict lands, for exactly that reason. rewards/SC-6's "can see the day's answer to a request they made" is discharged by the card glyph. Settles rewards/SC-6 verdict placement. → rewards/Screens (kiosk banner button, card states).

* User Story

As a kid at the fridge, I need to open a shop from my own banner, see at a glance which rewards I can have right now and which I cannot, and ask for one with a single tap — so that I can spend what I have saved without reading a word or waiting for anyone to hand me the tablet.

* Acceptance Criteria

** [X] The kid-color banner renders as ~[completion icon] NAME [★ points] [🎁]~, with the gift button grouped beside the points badge, and the points badge's own tap gesture stays unbound. (SC-2, SC-6, D65)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by ~lib/bear_cub_web/live/kiosk_live.ex~'s banner markup (right-hand group: points badge then gift button, in that DOM order) and the LiveView test =AC-1: banner renders the gift button grouped beside the points badge; the badge's own tap stays unbound= (~test/bear_cub_web/live/kiosk_live_test.exs~), which asserts both elements exist and ~refute has_element?(view, "#points-badge-#{kid_a.id}[phx-click]")~.
:END:
** [X] Tapping the gift button puts that column into a ~:rewards~ state: the column body — events strip, routine card region, and extras — swaps to the reward catalog while the kid-color banner and its points badge stay. (SC-2, SC-6, D65)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by the ~build_column/10~ ~state~ ~cond~ (adds ~shopping? -> :rewards~ ahead of ~:band~/~:rows~) and the template's ~:if={state != :rewards}~ / ~:if={state == :rewards}~ sibling wrapper divs, which swap the whole body together. Test =AC-2: tapping the gift button swaps the column body to the reward catalog, keeping the banner and points badge= asserts the reward card and points badge are present while ~#chores-{kid.id}~ is gone.
:END:
** [X] The sibling's column is untouched and fully usable while one column is shopping, and a card's state is always evaluated for the kid whose column it is. (SC-5, D62, D65)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by test =AC-3: the sibling's column is untouched and fully usable while one column is shopping= (sibling's ~#chores-{kid.id}~ present and a chore-tap on it still completes normally while the other column shops) and by test =AC-3: a card's state is always evaluated for the kid whose column it is — a declined kid_a leaves the same shared reward available to kid_b=, which declines one shared reward for ~kid_a~ and asserts ~kid_b~'s card for the identical reward still reads ~data-card-state=available~ — added during closeout verification to give this clause direct evidence rather than code-inspection alone, since every query in ~BearCub.Rewards~ (~pending_today?/3~, ~declined_today?/3~, ~spends_query/2~) filters on ~kid_id~.
:END:
** [X] The gift button has exactly two states — =🎁= idle and =⏳= while this kid has a pending request today — and any verdict, approve or decline, returns it to =🎁=. (SC-6, D65, D67)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by two tests: =AC-4: the gift button shows 🎁 idle, ⏳ once pending, and returns to 🎁 after a decline verdict= and =AC-4: an approve verdict also returns the gift button to 🎁=. The template only ever emits ~🎁~ or ~⏳~ (no third glyph), and both verdict paths flip ~Rewards.any_pending?/2~ back to ~false~, driving the button back to idle.
:END:
** [X] Reward cards resolve through the seven-row precedence table, top-down, first match wins: retired absent; one-time consumed on an earlier day absent; pending today =⏳= and untappable; declined today dim + declined glyph, untappable; claimed today dim + check glyph, untappable; unaffordable dim + lock glyph, untappable; otherwise full-color and tappable. (SC-5, SC-6, D66)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by ~BearCub.Rewards.card_state/4~ (single ~cond~, first-match-wins, in exactly this order) with its own unit tests in ~test/bear_cub/rewards_test.exs~ (=card_state/4 — the seven-row precedence table, first match wins (D66)=, rows 1-7 each pinned), plus the LiveView =row 1= through =row 7= tests in ~kiosk_live_test.exs~, each asserting the rendered ~data-card-state~ and tappability (~phx-click~ presence only on ~:available~). A closeout audit initially flagged that pending (row 3) was dimmed identically to rows 4-6, contrary to this AC's own wording ("pending today =⏳= and untappable" is worded separately from the three "dim + glyph" rows) — fixed so the dim CSS class applies only to ~:declined~/~:claimed~/~:locked~, with the =row 3= test now asserting ~refute has_element?(view, "...opacity-45")~ and rows 4/5/6 asserting the opposite.
:END:
** [X] Claimed-today outranks unaffordable, so a reward just claimed shows the check glyph rather than the lock even though the balance has since dropped below its price. (SC-6, D66)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by ~rewards_test.exs~'s ="row 5 — claimed today outranks unaffordable (D66's own ordering note)"= and ~kiosk_live_test.exs~'s ="row 5 — a reward claimed today renders the claimed glyph, dimmed, untappable, and outranks unaffordable"=, both spending a kid to exactly the reward's price then asserting ~:claimed~, not ~:locked~.
:END:
** [X] A one-time reward claimed today still shows on that kid's shelf as claimed and disappears from the next local day onward; a repeatable reward on cooldown is dimmed, never hidden. (SC-5, SC-6, D66)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by ~kiosk_live_test.exs~'s ="row 2 — a one-time reward consumed on an earlier day renders absent"= (one-time, redeemed yesterday, absent today) together with ~rewards_test.exs~'s row-2 unit test (claimed today, absent tomorrow), and by the two repeatable-reward LiveView tests — claimed/dimmed the day of the spend (="row 5 ... claimed glyph, dimmed..."=) and available again, never dimmed, the next local day (="row 5 — a repeatable reward on cooldown yesterday is available again (not dimmed, not absent) today"=). Day-rollover exercised via the ~yesterday()~ date-shifted write, never clock mocking.
:END:
** [X] A declined card is untappable for the rest of that local day, so a child cannot immediately re-ask for the reward they were just declined. (SC-6, D66, D61)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by ~kiosk_live_test.exs~'s ="row 4 — a declined request renders the declined glyph, untappable for the rest of the day (the re-ask block)"= (no ~phx-click~ on the declined card) and ~rewards_test.exs~'s row-4 unit test, which also confirms the card returns to ~:available~ the next local day.
:END:
** [X] Affordability on the kiosk is a rendering rule: the lock glyph is the whole explanation, and no negative number is ever shown to the child. (SC-3, SC-4, SC-6, D63, D64)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by ~kiosk_live_test.exs~'s ="row 6 — an unaffordable reward renders the lock glyph, dimmed, untappable, with no negative number ever shown"=, asserting the locked card carries no ~phx-click~ and ~refute render(view) =~ "−"~ (the unicode minus used elsewhere for penalties/negatives never appears). ~build_catalog/2~ only ever interpolates ~reward.points~ (a positive price) into the template; the raw signed ~Points.balance/2~ feeds only the internal ~card_state/4~ comparison, never the DOM.
:END:
** [X] Tapping an available card asks for it in one tap — no modal, no confirmation — with ~phx-throttle~ matching the shipped chore row; the view returns to the normal column immediately and the banner button flips to =⏳=. (SC-2, SC-6, D65)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by =AC-10: tapping an available card asks in one tap — no confirmation — returns to the column immediately, and flips the banner to pending=: one ~render_click~ on the reward card leaves ~#rewards-{kid.id}~ gone, ~#chores-{kid.id}~ back, the gift button showing ~⏳~, and ~Rewards.any_pending?/2~ true. ~phx-throttle="1000"~ on the reward card matches the shipped chore row's value (also asserted by the =row 7= test). No modal markup exists anywhere in the reward-shop template.
:END:
** [X] There is no kid-side withdraw control anywhere in the view. (SC-6, D65)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by =AC-11: there is no kid-side withdraw control anywhere in the view=, asserting no "withdraw" text renders and a pending card carries no ~phx-click~; code review of the reward-shop template confirms its only controls are the dismiss ✕ and reward cards themselves.
:END:
** [X] An explicit ✕ dismisses the view, and an idle timer returns the column to its normal state on its own; the timer is armed on open, reset on each tap inside the view, cancelled on dismiss, and its duration is a module constant. (SC-6, D65)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by four tests: =AC-12a= (dismiss closes the view), =AC-12b= (a direct ~{:rewards_idle, kid_id}~ message closes it), =AC-12c= (dismiss cancels the timer — a stale idle message afterward is a no-op), and =AC-12: the idle timer resets — a tap inside the view (re-tapping the gift button) cancels and re-arms it=, added during closeout verification since "reset on each tap" had no prior test evidence. That last test reads ~:sys.get_state(view.pid).socket.assigns.rewards_timers[kid_a.id]~ before and after a second gift-button tap, asserts the timer reference changed, and asserts ~Process.read_timer(first_ref) == false~, proving the old timer is actually cancelled (via ~arm_rewards_idle_timer/2~'s call to ~cancel_rewards_idle_timer/2~) rather than merely superseded. ~@rewards_idle_ms~ (~lib/bear_cub_web/live/kiosk_live.ex~) is the module constant, ~30_000~ (30s), commented as tuned at the on-device gate.
:END:
** [X] ~:rewards~ membership is a socket-level ~MapSet~ of kid ids, never persisted, and is cleared by the timer and on the routine boundary re-render. (D65)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by code inspection — ~:rewards~ is assigned as a ~MapSet.new()~ in ~mount/3~ and only ever mutated via ~assign/3~ (no schema, no migration, no DB write) — plus test =AC-13: :rewards membership is cleared by the routine boundary re-render= (a ~:boundary~ message closes an open shop) and =AC-12b= (the idle timer closes it). No persistence claim gets its own negative test, matching the precedent already set by the shipped ~@expanded~ assign, which is untested for non-persistence the same way.
:END:
** [X] At night the reward view is unreachable rather than suppressed: no columns render between 23:00 and 05:00, so there is no banner, no gift button, and nothing rewards-related to tap; an idle timer firing then is a harmless no-op. (D56, D65)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by =AC-14: at night the reward view is unreachable — no columns, no banner, no gift button — and a stray idle message is a harmless no-op=: with the night window forced active, ~#night-screen~ renders, no ~"gift-button"~ text appears anywhere in the page, and sending ~{:rewards_idle, 999_999}~ leaves the night screen intact with no crash — the state is unreachable by construction (no columns render, so ~handle_event("open-shop", ...)~ has no button to fire from) rather than specially suppressed.
:END:
** [X] The reward view contains no link or navigation to ~/admin~. (CLAUDE.md kiosk/admin fence)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified by =AC-15: the reward view contains no link or navigation to /admin=: with the shop open and a reward card rendered, ~refute render(view) =~ "/admin"~ passes.
:END:
** [X] ~docs/design-language.org~ records this story's three new kids'-view indicators as Rulings: the reward card surface, the dim+glyph vocabulary for the three unavailable reasons, and the =🎁= / =⏳= banner button. (SC-6, D66, D48/D54 pattern)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified — ~docs/design-language.org~'s ~* Rulings~ section carries three ~[2026-07-25]~ entries: "Reward card surface" (reuses the extras tokens, no child-color border), "Reward card dim+glyph vocabulary" (rows 4-6 share ~opacity-45~, row 3/pending stays undimmed with its own =⏳=), and "Banner gift button" (two glyphs, always tappable, no third banner state), each with a ~Rationale:~ link to D65/D66/D67. The dim+glyph wording was corrected during closeout (originally said "rows 3-6") to match the fix recorded against the precedence-table AC above.
:END:
** [X] LiveView integration tests cover open, dismiss, idle auto-return, a successful request, and each of the seven card states, with ~:routine_windows~ pinned and day-dependent states exercised by passing a different local date. (repo convention, design §Testing, ~docs/learnings.org~ [2026-07-17])
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:00] \\
  Verified — ~test/bear_cub_web/live/kiosk_live_test.exs~'s ="kiosk reward shop (Story 05, D65-D67)"= describe block (91 tests total in the file) pins ~:routine_windows~ via ~morning_active()~ with ~on_exit~ restoration in its ~setup~, and covers open (=AC-2=), dismiss (=AC-12a=), idle auto-return (=AC-12b=, =AC-12c=, and the reset test), a successful request (=AC-10=), and all seven card-state rows. Day-dependent states (one-time consumed yesterday, repeatable cooldown yesterday) are exercised via a ~yesterday()~ helper passing a shifted local date into the write path, never via clock mocking. ~mix precommit~ (compile --warnings-as-errors, deps.unlock --unused, format, test) is green at 410 passed.
:END:

* Definition of Done

** [X] All acceptance criteria have been either met or have had a documented removal or deferral
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:04] \\
  Verified by a story-verifier audit of the Acceptance Criteria section above: all 17 items are ~[X]~ with LOGBOOK entries whose cited test names and file locations were spot-checked directly (grep-confirmed against ~test/bear_cub_web/live/kiosk_live_test.exs~ and ~test/bear_cub/rewards_test.exs~), and the three ~docs/design-language.org~ Rulings and ~docs/decisions.org~ D65/D66/D67 entries were confirmed present, not merely referenced.
:END:
** [X] Tests written and passing for the acceptance criteria in this story
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:04] \\
  Verified — ~mix test test/bear_cub_web/live/kiosk_live_test.exs~ (91 passed) and ~mix test test/bear_cub/rewards_test.exs~ (60 passed) both green in isolation; full suite ~mix test~ 410 passed. Test-to-AC-language mapping spot-checked (e.g. the "no negative number" clause backed by ~refute render(view) =~ "−"~; the "claimed outranks unaffordable" clause backed by a test at both the unit and LiveView layers).
:END:
** [X] Code follows style guidelines in CLAUDE.md
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:04] \\
  Verified against CLAUDE.md's Elixir/Phoenix/LiveView conventions by reading both touched files in full: ~<.icon>~ used throughout (no raw Heroicons calls), correct HEEx ~{...}~ vs ~<%= %>~ interpolation, ~class={[...]}~ list syntax with parenthesized ~if~/~&&~, stable DOM ids exactly per the Technical Notes (~#gift-button-{kid.id}~, ~#reward-card-{reward.id}-{kid.id}~, ~#dismiss-shop-{kid.id}~), no struct map-access misuse, no bare ~DateTime.utc_now()~ in domain code (~BearCub.Rewards~ takes ~local_now~/~local_date~ throughout). One pre-existing pattern noted, not a new violation: the reward list — like the sibling chore/extras/events lists already in this LiveView — renders via plain ~:for~ rather than a LiveView stream; carried forward rather than introduced by this story. Recorded below as a Deferred discovery.
:END:
** [X] The repo's declared ~Verify command:~ (CLAUDE.md; default ~mix format --check-formatted~ and ~mix test~) passes without errors
:LOGBOOK:
- Note taken on [2026-07-25 Sat 15:04] \\
  Verified — ~mix precommit~ (~compile --warnings-as-errors~, ~deps.unlock --unused~, ~format~, ~test~) green at 410 passed, reproduced across multiple runs by both the implementing session and the DoD-verification subagent; ~mix format --check-formatted~ and ~mix compile --warnings-as-errors~ independently confirmed clean.
:END:

* Technical Notes

The Design excerpt above is the source of truth; DESIGN.org is broader context.

- Depends on Story 03 (the request write path, the availability and affordability derivations, and the ="rewards"= topic) and on Story 04 (a real catalog to render, so this view can be exercised against data a parent actually created). Balances come from ~Points~ (Stories 01–02).
- *Read ~docs/design-language.org~ before touching styling* — its invariants and rulings are binding, and this story adds three Rulings to it. The recordings are ACs, not follow-up work.
- ~:rewards~ joins the shipped per-column state machine beside ~:rows~ and ~:band~; the ~MapSet~ assign parallels the shipped ~@expanded~ / ~@pending_collapse~ pattern in ~lib/bear_cub_web/live/kiosk_live.ex~.
- The idle timer follows the shipped ~@collapse_delay_ms~ precedent: a module constant, ~Process.send_after~, and a ~handle_info/2~ clause. The design deliberately does not pin the duration (~30s is the starting guess) — it is tuned at the on-device gate that closes this batch.
- ~KioskLive~ subscribes to ="rewards"= in addition to ="chores"= — one extra ~handle_info/2~ clause, re-fetching rather than patching from a payload (D71).
- The card precedence table is one ordered resolution, not two systems: implement it as a single first-match function returning the card's state atom, so the seven integration tests each pin one row of the table.
- Stable DOM ids for the tests: ~#gift-button-{kid.id}~, ~#reward-card-{reward.id}-{kid.id}~ (or equivalent), and a dismiss control id. Test against elements, never raw HTML.
- The 5-chore no-scroll budget is untouched by construction — the view replaces the routine region. The banner button is the only permanent addition, and confirming the budget still holds with it present is part of the on-device gate at batch close.
- *Deferred discovery, caught by the DoD-verification subagent, not fixed inline:* the reward list — like the sibling chore/extras/events lists already shipped in this LiveView — renders via a plain ~:for~ over an assign rather than a ~Phoenix.LiveView~ stream, which CLAUDE.md's LiveView guidelines call for on collections. Given the fixed, small cardinality here (bounded by the no-scroll design budget) this is unlikely to cause the memory/runtime issues the streams rule targets, but it is a real style-guideline deviation that predates this story and was carried forward rather than introduced by it. Not this story's job to fix a pattern set by earlier stories across four lists at once; swept to ~docs/backlog.org~ at initiative close.
