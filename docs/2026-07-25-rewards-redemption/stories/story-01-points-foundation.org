#+title: Story 01 — Points foundation: ~BearCub.Points~, ~Chores.earnings/2~, and the pinning suite
#+date: 2026-07-25

Design: [[file:../DESIGN.org]]

* Design

Verbatim from DESIGN.org for this story's scope.

** Contexts and dependency direction

Two new modules.

~BearCub.Rewards~ — a context owning the ~rewards~ and ~redemptions~ tables, the request / approve / decline / direct-redeem / reverse write paths, the availability derivation, and its own PubSub topic. It knows nothing about chores or completions.

~BearCub.Points~ — a composition module, the only place in the app that knows both earnings and spends:

#+begin_src elixir
Points.balance(kid, local_date)   # raw signed integer; may be negative
Points.total(kid, local_date)     # max(0, balance) — the D41 display figure
Points.balances(local_date)       # %{kid_id => signed} for a whole render
Points.totals(local_date)         # %{kid_id => floored} for a whole render
#+end_src

~Chores.points_total/2~ becomes ~Chores.earnings/2~ and returns the *raw signed* earnings, unfloored — the floor moves up to ~Points~, where the complete summation lives. ~Chores~ never learns that ~Rewards~ exists.

#+begin_src
Points.balance = Chores.earnings + Rewards.spend_total
                 └─ D42's completions_earnings   └─ D42's other_signed_inputs
#+end_src

Cost to shipped code, stated plainly: the kiosk's ~points: Chores.points_total(kid, today)~ call, admin balance reads, and the existing points-derivation tests all move to ~Points~. This is a rename plus a re-point, not a rewrite, and the aggregate rewrite touches the same function anyway.

** From the Overview

Redemption also makes the points total load-bearing: it is read on every reward-card render, on every tap that checks affordability, and on every admin balance display. The shipped ~points_total/2~ issues on the order of one query per routine-day of history, which is why PRD SC-9 requires the check be cheap and /stay/ cheap as months accumulate. The deferred aggregate rewrite is therefore claimed here as a batch member and lands early, before affordability depends on it — as a pure performance refactor with semantics provably unchanged.

** From the points aggregate rewrite — the pinning requirement

Discovered property — *roster drift*, surfaced but not fixed: ~routine_day_contribution/3~ loads the kid's *current* chore roster for the routine and requires every one of those chores to have a live completion on the evaluated date. Historical routine-days are therefore evaluated against today's roster: adding a morning chore today retroactively makes every past morning-day incomplete, wiping all historical ~+R~; deleting one can retroactively complete past days and grant points that were never earned. This ships today, and the badge already moves when a parent edits chores. Redemption raises the stakes: a roster edit can move a balance *after* a spend, potentially into debt with no fail involved.

Semantics stay *unchanged* in the rewrite — it is a pure performance refactor, proven behavior-preserving by tests — and roster drift is captured as its own backlog entry at initiative close. The claimed item is the query shape, not the derivation's semantics.

The pinning tests *must include a roster-drift case* — a routine-day evaluated after the roster changed — so behavior preservation is proven on the weird path and not merely the happy one. A refactor that silently "fixed" drift would be an unruled semantic change smuggled in under a performance story.

** Testing and the on-device gate (excerpt)

Per repo convention, LiveView integration tests are part of story ACs wherever a flow is touched, regardless of the mvp DoD.

- *Unit* — ... and the ~Points.balances~ aggregate *pinned against the current per-kid implementation*, including the roster-drift case.

- *Time* — every test pins ~:routine_windows~ per ~docs/learnings.org~ [2026-07-17] rather than inheriting the wall clock, and all new derivations take a local-date argument. Day-rollover behavior (lapse, cooldown reset, verdict-state expiry) is tested by *passing a different local date*, never by mocking a clock.

** Decision Log entries in scope

- *[[file:../../decisions.org::#d57][D57]] (2026-07-25) — Rewards live in a new ~BearCub.Rewards~ context; ~BearCub.Points~ composes earnings and spends; ~Chores.points_total/2~ becomes ~Chores.earnings/2~ (raw signed):* Redemption gets two new modules rather than an extension of ~Chores~. ~BearCub.Rewards~ owns the ~rewards~ / ~redemptions~ tables, every write path, the availability derivation, and its own PubSub topic, and knows nothing about chores or completions. ~BearCub.Points~ is a composition module — the only place in the app that knows both sides — exposing ~balance/2~ (raw signed), ~total/2~ (the D41 floored display figure), and their whole-render map forms. ~Chores.points_total/2~ is renamed ~Chores.earnings/2~ and returns the raw signed earnings unfloored; the floor moves up to ~Points~, where the complete summation lives, and ~Chores~ never learns ~Rewards~ exists. Rejected — putting ~redemptions~ inside ~BearCub.Chores~: a naming lie (a redemption is not a chore), and ~chores.ex~ already carries kids, chores, extras, completions, fails, and the points derivation in ~450 lines. Rejected — letting the points derivation read the ~redemptions~ table directly from ~Chores~: inverts the dependency direction the repo already treats as an invariant for ~Chores~ → ~Calendars~; the moment ~Chores~ imports ~Rewards~, a rewards failure can reach the chore path. Rejected — threading the spend in as an argument (~points_total(kid, date, other_signed_inputs \\ 0)~): keeps the contexts clean but pushes the summation into every LiveView, so a missed caller is a silently wrong balance. Settles rewards/SC-3 and rewards/SC-4 placement. → rewards/Key Behaviors (contexts and dependency direction), rewards/Overview.

- *[[file:../../decisions.org::#d72][D72]] (2026-07-25) — The points aggregate rewrite is claimed and lands early, as a pure performance refactor; roster drift is surfaced, not fixed* (pinning half): Semantics stay *unchanged*: the claimed item is the query shape, not the derivation's meaning. This preserves a discovered defect — ~routine_day_contribution/3~ evaluates historical routine-days against the kid's *current* roster, so adding a morning chore today retroactively wipes past ~+R~ and deleting one can grant points never earned; redemption raises the stakes, since a roster edit can move a balance after a spend, potentially into debt with no fail involved. Roster drift is captured as its own backlog entry at initiative close: a fix needs its own ruling, and a ~chores.inserted_at~-bounded roster would handle additions but not deletions, so the honest fix is not obviously cheap. The pinning tests must include a roster-drift case — a routine-day evaluated after the roster changed — so behavior preservation is proven on the weird path; a refactor that silently "fixed" drift would be an unruled semantic change smuggled in under a performance story. Rejected — fixing drift inside the rewrite: a semantic change disguised as a refactor, and unruled. Settles rewards/SC-9.

* User Story

As the parent maintaining this app, I need the points total to live behind one composition module with its current behavior captured as tests, so that the affordability rule about to be built on top of it reads from a single honest source, and the performance rewrite that follows can be proven to have changed nothing.

* Acceptance Criteria

** [X] ~BearCub.Points~ exists and exposes ~balance/2~ (raw signed, may be negative), ~total/2~ (~max(0, balance)~), ~balances/1~ (~%{kid_id => signed}~), and ~totals/1~ (~%{kid_id => floored}~), each taking a local date. (SC-3, SC-4, D57)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:04] \\
  ~lib/bear_cub/points.ex~: ~balance/2~ (:27), ~total/2~ (:36), ~balances/1~ (:44), ~totals/1~ (:53). All four guard on ~%Date{}~, so a local date is structurally required — a caller cannot forget it. Raw-signed negativity is proven, not assumed: ~points_test.exs~ asserts ~Points.balance(kid, ~D[2026-07-10]) == -7~ (test /total\/2 floors a negative balance at zero while balance\/2 stays signed/) and ~balances/1 == %{kid_a.id => 9, kid_b.id => -4, kid_c.id => 0}~ (test /balances\/1 returns every kid's raw signed balance keyed by kid id/). Signatures match the story's Design excerpt block verbatim. Per Technical Notes, ~balances/1~ is the sanctioned per-kid loop for now; the grouped-query rewrite behind the same signature is Story 02.
:END:
** [X] ~Chores.points_total/2~ is renamed ~Chores.earnings/2~ and returns the raw signed earnings, unfloored; the ~max(0, …)~ floor is applied only in ~Points~. (SC-3, D57, D41)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:04] \\
  ~lib/bear_cub/chores.ex:420~ ~def earnings(%Kid{} = kid, %Date{} = local_date)~, body ending ~extra_sum + routine_sum~ — no ~max/2~ anywhere in the file. The only two floors in the app are ~points.ex:37~ and ~points.ex:56~. Verified a genuine rename rather than a rewrite: ~git diff lib/bear_cub/chores.ex~ is exactly two hunks, both inside the old ~points_total~ (its ~@doc~ and its tail); ~extra_contribution/2~ and ~routine_day_contribution/3~ are byte-unchanged. Old tail ~max(0, extra_sum + routine_sum + other_signed_inputs)~ with ~other_signed_inputs = 0~ → new tail ~extra_sum + routine_sum~, so ~Points.total/2~ is pointwise identical to the shipped ~points_total/2~. Pinned by test /Chores.earnings\/2 is raw signed and unfloored — the floor lives only in Points/: ~Chores.earnings(kid, d) == -6~ while ~Points.total(kid, d) == 0~.
:END:
** [X] Every shipped call site of the old name reads through ~Points~ instead — the kiosk column's ~points:~ assign and the admin balance reads — and the existing points-derivation tests are re-pointed at ~Chores.earnings/2~ / ~Points~ with their expected values unchanged. (D57)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:04] \\
  Met, with the AC's premise corrected in the doing. *The "admin balance reads" clause was wrong as written*: no admin LiveView displays a balance today. The only ~points~/~balance~ hits under ~lib/bear_cub_web/live/admin/~ are ~today_live.ex:212~ (a button ~title~ string) and ~chore_live/form.ex:96~ (the per-chore points input) — neither is a balance. The design's cost paragraph ("the kiosk's ~points:~ call, admin balance reads, and the existing points-derivation tests all move to ~Points~") was describing surfaces this batch has yet to build: admin balance displays arrive with their owning stories — Story 04's direct-redeem kid picker, Story 06's approval-queue rows, Story 07's history view — each specifying ~Points.balance/2~ per [[file:../../decisions.org::#d64][D64]]. The clause is therefore *vacuous, not deferred*, and no phantom deferral is recorded against it. \\
  What was actually there and is now re-pointed: exactly one shipped call site, ~lib/bear_cub_web/live/kiosk_live.ex:226~, now ~points: Points.total(kid, today)~ with ~alias BearCub.Points~ at :8; and one real admin-side reference, the badge assertion in ~test/bear_cub_web/live/admin/today_live_test.exs:207,215~, re-pointed to ~Points.total/2~. Nothing was left behind: ~grep -rn "points_total" lib test priv~ returns zero code hits. Re-point with values unchanged verified mechanically — the old ~ChoresTest~ ~points_total/2~ describe block (~git show HEAD:test/bear_cub/chores_test.exs~, lines 593–690) diffed against its new home in ~points_test.exs~ after substituting ~Chores.points_total~ → ~Points.total~ differs only in the describe header and the ~import~ line; all six test bodies and every expected literal are identical. The three in-place re-points at ~chores_test.exs:684,687,690~ keep 25/15/20.
:END:
** [X] ~BearCub.Chores~ contains no reference to ~BearCub.Rewards~ or to redemptions; the spend term of ~Points.balance~ is empty in this story and is filled by Story 03. (D57)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:05] \\
  ~grep -rni "reward\|redemption\|redeem" lib/bear_cub/chores.ex lib/bear_cub/chores/~ exits 1 — no matches. The old ~@doc~ prose naming the "D42 redemption seam" and the ~other_signed_inputs = 0~ line were both removed with the rename; the replacement ~@doc~ describes the boundary without naming the module on the other side of it ("~Chores~ knows nothing of the spend side"). Dependency direction is one-way: ~Chores~ has no ~alias~ or call to ~Points~, only a docstring pointer at ~chores.ex:418~. Spend term empty: ~points.ex:61~ ~defp spend_total(%Kid{}, %Date{}), do: 0~, commented as the [[file:../../decisions.org::#d42][D42]] seam Story 03 fills. Pinned by test /balance\/2 equals Chores.earnings\/2 while no spends exist/.
:END:
** [X] The kiosk points badge and every admin balance display show exactly what they showed before this story — no behavior change anywhere. (SC-3, D41, D57)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:05] \\
  Two independent lines of evidence. Algebraic: per the AC-2 diff analysis, ~Points.total(kid, d) = max(0, extra_sum + routine_sum + 0)~ is the shipped ~points_total/2~ expression exactly, and the kiosk assigns ~Points.total~ (not ~Points.balance~), so the badge value is identical for every input. Behavioral: the pre-existing badge tests carrying *hardcoded literal* expected values were not touched and still pass — ~kiosk_live_test.exs:1294~ ("0" before any completion), :1304 ("12"), :1315 ("0" for a below-zero signed total, SC-4), :1323/:1327 ("0" → "12" on live update). The ~kiosk_live.ex~ diff is two lines (an ~alias~ and the call swap): no template, class, or DOM-id change. No admin balance display exists to change — see the AC-3 entry. Full suite ~mix precommit~ 304 passed.
:END:
** [X] A pinning suite captures the current per-kid derivation's outputs as tests: extras contributions (live / undone / failed), routine-day ~+R~ / ~−R~ with the penalty capped, cumulative-all-time accumulation across dates, and the aggregate floor at zero. (SC-9, D72)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:05] \\
  ~test/bear_cub/points_test.exs~, 27 tests, all passing. Every required category is covered by a named test: extras live (/a live extra completion adds its own points value/), undone (/an undone extra completion contributes nothing/), failed (/a failed extra completion subtracts its own points value/, plus /a failed extra redone the same day nets back to the earned value/); routine-day ~+R~ (/a fully-complete routine-day adds exactly R regardless of chore count/, /an incomplete routine-day adds nothing/, /morning and evening are separate routine-days on the same date/); ~−R~ *capped* (/two fails in one routine-day still cost a single -R (the penalty is capped)/ — two failed completions, asserting ~20 - Routines.bonus()~, i.e. exactly one ~−R~); cumulative-all-time (/routine-days accumulate across dates, and only up to the given local date/ asserting 0 / R / 2R / 3R / 3R across five dates, /extras accumulate across dates alongside routine-days/, /one kid's completions never leak into another kid's balance/); aggregate floor (/total\/2 floors a negative balance at zero while balance\/2 stays signed/ — ~balance == -7~, ~total == 0~ — and /a kid with no completions scores zero/). The six shipped ~points_total/2~ worked examples moved in re-pointed at ~Points.total/2~ with expected values unchanged, and the ~balances/1~ / ~totals/1~ map forms are pinned alongside. The suite is demonstrated to *bite*, not merely to pass: mutating ~balances/1~ to ~balance(&1, local_date) + 1~ kills 5 tests, independently reproduced by the verifying subagent.
:END:
** [X] The pinning suite includes a roster-drift case — a routine-day evaluated after the kid's chore roster changed — pinning the shipped behavior as it is, not as it ought to be. (D72)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:05] \\
  ~describe "roster drift (D72) — pinned as shipped, not as it ought to be"~ covers *both* directions: /adding a routine chore today retroactively wipes a past routine-day's +R/ (2026-07-10 fully complete, evaluated from 2026-07-20 → ~Routines.bonus()~; add a third morning chore; the same date now returns 0) and /deleting a routine chore retroactively completes a past routine-day, granting +R/ (0 → ~Routines.bonus()~ after deleting the never-completed chore). A comment block names them as a known defect and forbids repairing it in Story 02. \\
  Pinned, not repaired — verified three ways: the drift mechanism at ~chores.ex:383-400~ (roster query with no date bound, then ~Enum.all?~ over the *current* ~chore_ids~) is byte-unchanged from ~HEAD~; both pinned values are non-negative, so the removed ~max(0, …)~ cannot mask a divergence; and the assertions encode the defect itself — a drift fix would make the post-edit value ~Routines.bonus()~ and fail the test, so repair cannot be smuggled past them. \\
  Tightened during close-out on the verifying subagent's finding: the story's Design excerpt (§Testing) requires "the ~Points.balances~ aggregate pinned … including the roster-drift case," and both tests initially asserted only through ~balance/2~. Each now asserts through ~balance/2~ *and* ~balances/1~ on both sides of the roster edit. This is load-bearing for Story 02, not tidiness: [[file:../../decisions.org::#d72][D72]]'s target has ~balances/1~ become the grouped query with ~balance/2~ delegating to it, so pinning only the singular form would have left the grouped query's drift path — the exact weird path D72 requires proven — unpinned. Mutation-checked: both drift tests fail by name when ~balances/1~ is perturbed.
:END:
** [X] Every test in the pinning suite pins ~:routine_windows~ rather than inheriting the wall clock, and any day-dependent expectation is exercised by passing a different local date, never by mocking a clock. (design §Testing, ~docs/learnings.org~ [2026-07-17])
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:05] \\
  ~points_test.exs~ has a *module-level* ~setup~ (outside every ~describe~, so it runs for all 27 tests) doing ~Application.fetch_env!~ → ~on_exit~ restore → ~put_env~ with ~morning: {~T[05:00:00], ~T[17:00:00]}, evening: {~T[17:00:00], ~T[23:00:00]}~ — the same fetch/~on_exit~/~put_env~ idiom as ~kiosk_live_test.exs~ and ~today_live_test.exs~, per ~docs/learnings.org~ [2026-07-17]. No clock is reachable from the suite: grepping ~LocalTime~, ~now()~, ~Date.utc_today~, ~DateTime.utc_now~, ~Mox~, and ~mock~ returns only the *comment* "never by mocking a clock". Every datetime is built by the local ~la/2~ helper from an explicit ~~D[…]~ literal. Day-rollover is exercised purely by argument: the cumulative test asserts five different local dates against one fixed dataset, the roster-drift tests evaluate 2026-07-10 history from 2026-07-20, and /the map forms agree with the per-kid forms/ loops over two dates.
:END:

* Definition of Done

** [X] All acceptance criteria have been either met or have had a documented removal or deferral
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:06] \\
  All 8 ACs ~[X]~ with per-item evidence above; none required removal or deferral. One documented disposition rather than a silent check: AC-3's "and the admin balance reads" clause rested on a false premise — no admin LiveView displays a balance today, so the clause is vacuous rather than unmet or deferred. The premise correction, the one real admin reference that *was* re-pointed, and the stories that own the future admin balance displays are recorded in AC-3's own LOGBOOK entry and mirrored to the Plan. Verified across two subagent audit passes; the second re-verified AC-6/AC-7 after the ~balances/1~ drift-pin tightening and confirmed no regression elsewhere.
:END:
** [X] Tests written and passing for the acceptance criteria in this story
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:06] \\
  ~mix test test/bear_cub/points_test.exs~ 27 passed; full suite 304 passed, 0 failures. The pinning suite is this story's primary deliverable per Technical Notes, and it is proven to bite rather than merely to pass — an independent mutation run by the verifying subagent killed 5 tests including both roster-drift pins by name. Behavioral ACs each map to named passing tests (AC-1, AC-2, AC-5 via the untouched literal-valued badge tests, AC-6, AC-7, AC-8). Stated for the record: *AC-3 and AC-4 are structural, not behavioral, and carry no test* — "no shipped call site of the old name remains" and "~Chores~ contains no reference to ~Rewards~" are evidenced by ~grep~ plus a clean ~mix compile --warnings-as-errors~, which is the appropriate instrument; a test asserting that a module's source lacks a string would be worse than the grep. A durable automated guard on the ~Chores~ ⇏ ~Rewards~ dependency direction would be a separately-ruled item, not a Story 01 gap — recorded as a Deferred discovery.
:END:
** [X] Code follows style guidelines in CLAUDE.md
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:06] \\
  Audited against the repo CLAUDE.md's Elixir, Test, Phoenix, and Ecto sections plus the design invariants. One module per file; no map-access syntax on structs (~Points.balances(date)[kid.id]~ is a plain ~Map~); no ~String.to_atom/1~; no ~is_~-prefixed non-guard predicates; date/time from stdlib only with no new deps (~mix deps.unlock --check-unused~ exit 0); ~#~ comment rather than ~@doc~ on the private ~spend_total/2~; no ~Process.sleep/1~ or ~Process.alive?/1~ in tests; ~:routine_windows~ pinned per ~docs/learnings.org~ [2026-07-17]; ~alias BearCub.Points~ inserted in alphabetical order in ~kiosk_live.ex~; no ~.md~ files created. Design invariants hold: nothing stored (D10 — no cached total, no materialized column, no reset machinery), all four public functions take a local date, no ~DateTime.utc_now()~ in domain code. ~mix format --check-formatted~ exit 0. Two style edits made during close-out on subagent findings: the misplaced ~@moduledoc~ (after ~use~) became a ~#~ header comment, and the drift tests gained their ~balances/1~ assertions. The "scaffold with ~mix phx.gen.*~" convention does not apply — ~BearCub.Points~ is a pure composition module with no schema, migration, or PubSub wiring for a generator to produce.
:END:
** [X] The repo's declared ~Verify command:~ (CLAUDE.md; default ~mix format --check-formatted~ and ~mix test~) passes without errors
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:06] \\
  This repo declares ~mix precommit~ as its ~Verify command:~ (CLAUDE.md §Weight class), aliased at ~mix.exs:102~ to ~["compile --warnings-as-errors", "deps.unlock --unused", "format", "test"]~. Run green: exit 0, *304 passed*, 0 failures. Independently re-run twice by the verifying subagent, which also confirmed ~precommit~'s write-capable steps rewrote nothing (md5 sums for ~points.ex~, ~chores.ex~, ~points_test.exs~, and ~mix.lock~ identical before and after; ~git status --short~ unchanged). The run emits ~Exqlite.Connection … disconnected~ ~[error]~ log lines from LiveView sandbox teardown; these are ~Logger~ output, not failures, and were confirmed present on a stashed baseline of untouched test files — pre-existing, not attributable to this story, recorded as a Deferred discovery.
:END:

* Technical Notes

The Design excerpt above is the source of truth; DESIGN.org is broader context.

- No dependencies on earlier stories in this batch — this is the batch's first story. Story 02 depends on it: the pinning suite is the baseline proof that the aggregate rewrite preserves behavior, so it must exist first.
- The pinning suite is *this story's primary deliverable*. It is not incidental test coverage: Story 02's rewrite is accepted or rejected against it, and per D72 it must pass unmodified there.
- ~Points.balances/1~ and ~totals/1~ may be implemented in this story as a straightforward per-kid loop over the existing derivation — the point of this story is the module boundary, the rename, and the pins. The grouped-query rewrite behind the same signatures is Story 02.
- The rename is a rename plus a re-point, not a rewrite: ~Chores.earnings/2~ keeps the shipped derivation body, minus the ~max(0, …)~ wrapper which moves up to ~Points.total/2~.
- Roster drift is *pinned, not fixed*. If the pinning test's expected value looks wrong, that is the point — write the value the shipped code actually produces and leave a comment naming it as the D72 roster-drift case. Do not repair it here or in Story 02; it is a backlog entry at initiative close.
- LiveView surfaces are touched only insofar as they call the renamed function; no user-visible flow changes, so no new integration tests are owed beyond keeping the shipped ones green.

** Deferred discoveries

Surfaced during close-out; not fixed inline. Carried to the Plan's Deferred section by ~/workflow-kit:update-design~.

- Deferred discovery: ~test/bear_cub_web/live/admin/today_live_test.exs~ derives its date argument from the wall clock — ~Points.total(ctx.kid_a, DateTime.to_date(LocalTime.now()))~ in /a fail live-updates the kiosk: the chore reverts and the points badge drops/. Pre-existing; this story only swapped the module name on that line. It is *not* a ~docs/learnings.org~ [2026-07-17] violation — that test does pin ~:routine_windows~ — but the date itself is still wall-clock-derived rather than passed in.
- Deferred discovery: no automated guard exists on the ~Chores~ ⇏ ~Rewards~ dependency direction, which CLAUDE.md treats as an invariant alongside ~Chores~ ⇏ ~Calendars~. Today it holds by ~grep~ at review time. A durable check would need its own ruling on instrument and placement.
- Deferred discovery: ~mix precommit~ emits ~Exqlite.Connection … disconnected~ ~[error]~ log lines from SQLite sandbox teardown in LiveView suites. Cosmetic ~Logger~ noise, not failures; confirmed pre-existing on a stashed baseline and unrelated to this story.
