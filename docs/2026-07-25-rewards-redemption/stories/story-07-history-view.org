#+title: Story 07 — Redemption history: ~/admin/rewards/history~
#+date: 2026-07-25

Design: [[file:../DESIGN.org]]

* Design

Verbatim from DESIGN.org for this story's scope.

** ~/admin/rewards/history~ — the redemption record

A parent must be able to answer "did this child redeem this reward?" days later (SC-7). The storage already supports it — append-only rows, price snapshot, ~reversed_at~ marker — so this is purely a read surface.

It is *one flat reverse-chronological list*, its own route, linked from ~/admin/rewards~. Each row: date, kid (name + color chip), reward icon + name, snapshot price, and state (approved / reversed / declined). A reversed row renders its price struck through with a "reversed" marker and the reversal's date — this is where a reversal stays legible days later.

- At family scale — two kids, a handful of rewards, a few redemptions a week — one list beats per-kid or per-reward views: no navigation, no filter UI, and the whole record fits on a phone screen. Per-kid grouping can be added later if the list ever gets long enough to need it.
- *Shows approved, reversed, and declined; omits lapsed.* A never-answered ask is an abandoned intention, not history, and including it would bury real redemptions in noise.
- *Archived rewards still appear* with their name and icon — the row is retained, and only the catalog and picker filter on ~retired_at~.
- *Bounded, visibly.* The list shows the 50 most recent entries with no pagination, and *says so on screen* — a silent truncation would read as "this is everything" when it is not. At the stated family volume 50 entries spans months, comfortably covering SC-7's "days afterward" horizon; pagination is a backlog seed if it ever bites.

** Balances in admin

Every admin surface that shows a balance shows the *true signed* figure, negative included (see Key Behaviors → hidden debt).

** From the Data Model — what makes this a pure read surface

A ~reward_name~ snapshot is deliberately *not* taken: reward rows are kept forever (archive, never delete), so the join is always available and the name stays a single source of truth. A rename is correctly reflected in history — a parent renaming a reward is clarifying what it is, not creating a different one.

** Testing (excerpt)

- *LiveView integration* — ... admin: ... archive-hides-from-catalog-but-not-history ...

** Decision Log entries in scope

- *[[file:../../decisions.org::#d70][D70]] (2026-07-25) — Redemption history is one flat reverse-chronological list, bounded at 50 with the bound stated on screen:* ~/admin/rewards/history~ is a pure read surface over storage that already supports it (append-only rows, price snapshot, ~reversed_at~ marker): one flat reverse-chronological list on its own route, linked from the catalog, each row showing date, kid (name + color chip), reward icon and name, snapshot price, and state — approved / reversed / declined. A reversed row renders its price struck through with a "reversed" marker and the reversal's date, which is where a reversal stays legible days later. Lapsed rows are omitted: a never-answered ask is an abandoned intention, not history, and including it would bury real redemptions in noise. Archived rewards still appear with their name and icon — only the catalog and the direct-redeem picker filter on ~retired_at~. The list is bounded at the 50 most recent entries with no pagination and *says so on screen*, because a silent truncation would read as "this is everything" when it is not; at the stated family volume that spans months, comfortably covering rewards/SC-7's "days afterward" horizon, and pagination is a backlog seed if it ever bites. Rejected — per-kid or per-reward grouped views: at two kids and a few redemptions a week, one list beats navigation and filter UI, and the whole record fits on a phone screen. Settles rewards/SC-7. → rewards/Screens (admin history).

- *[[file:../../decisions.org::#d60][D60]] (2026-07-25) — The redemption price is snapshotted on the row; the reward name is not* (history half): A ~reward_name~ snapshot is deliberately *not* taken — reward rows are archived, never deleted (D68), so the join is always available and the name stays a single source of truth; a rename is correctly reflected in history, because a parent renaming a reward is clarifying what it is, not creating a different one. Settles rewards/SC-7.

* User Story

As a parent, I need a single screen that answers "did this child redeem this reward?" days after the fact — including whether I later reversed it — so that the record of what was spent survives the day it happened without becoming something I can edit.

* Acceptance Criteria

** [X] ~/admin/rewards/history~ is its own route, linked from ~/admin/rewards~, and renders one flat reverse-chronological list. (SC-7, D70)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:25] \\
  ~lib/bear_cub_web/router.ex~ registers ~live "/rewards/history", RewardLive.History~ in the ~/admin~ scope; ~RewardLive.Index~'s header carries a ~#view-history~ link to it. ~RewardLive.History~ renders one ~<ul id="history">~ ordered by ~Rewards.list_redemption_history/1~ (desc by verdict date). Covered by test "links to the redemption history" and "renders one flat reverse-chronological list..." in ~test/bear_cub_web/live/admin/reward_live_test.exs~.
:END:
** [X] Each row shows the date, the kid's name with their color chip, the reward's icon and name, the snapshot price, and the state — approved, reversed, or declined. (SC-7, D70)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:25] \\
  ~history_row/1~ in ~lib/bear_cub_web/live/admin/reward_live/history.ex~ renders all six fields. Verified per-state by tests "each row shows the date, kid name and color chip..." (approved) and "a declined row shows the date, kid, reward icon and name, price, and declined state" — both assert every field individually.
:END:
** [X] A reversed row renders its price struck through with a "reversed" marker and the reversal's date. (SC-7, SC-8, D70)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:25] \\
  The price ~<span>~ takes the ~line-through~ class when ~Rewards.reversed?/1~; a second line renders "reversed" plus ~format_date(@redemption.reversed_at)~, distinct from the row's primary (approval) date. Test "a reversed row shows the struck-through price, a reversed marker, and the reversal date" asserts ~.line-through~ on the price, the "Reversed" label, and the reversal date differing from the approval date.
:END:
** [X] Approved, reversed, and declined rows all appear; lapsed rows — requested, never answered — are omitted. (SC-6, SC-7, D70)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:25] \\
  ~Rewards.list_redemption_history/1~ selects rows where ~approved_at~ or ~declined_at~ is set, which naturally excludes both pending and lapsed rows (neither carries a verdict) without needing to distinguish them. Unit tests in ~test/bear_cub/rewards_test.exs~ cover inclusion of all three verdict states and exclusion of pending/lapsed; LiveView test "omits a lapsed (never-answered) request" confirms the same at the screen.
:END:
** [X] A redemption of a reward that has since been archived still appears, with that reward's name and icon; a reward renamed since the redemption shows under its current name. (SC-7, D60, D68, D70)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:25] \\
  No ~reward_name~ snapshot exists on ~Redemption~; the query preloads ~:reward~, so a rename or archive is reflected live. Unit test "an archived reward's redemption still appears, joined to its current name" and LiveView test "a redemption of a since-archived reward still appears under its current name and icon" both rename then archive before asserting the current name and icon render.
:END:
** [X] The list shows the 50 most recent entries with no pagination, and says so on screen. (SC-7, D70)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:25] \\
  ~list_redemption_history/1~ defaults its ~limit~ argument to 50; the template's subtitle reads "Showing the last 50 redemptions — no pagination". Test "shows at most the 50 most recent entries and states the bound on screen" creates 51 redemptions, asserts exactly 50 rows render (newest present, oldest absent), and asserts the on-screen text.
:END:
** [X] The history screen offers no way to edit, delete, or reverse a redemption — reversal is Today's day-scoped control only. (SC-7, SC-8, D69, D70)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:25] \\
  ~RewardLive.History~'s template contains no buttons, forms, or ~phx-click~ handlers — only navigation links. Test "offers no way to edit, delete, or reverse a redemption" refutes any ~button~ or ~[phx-click]~ within ~#admin-rewards-history~ and the specific ~#reverse-redemption-<id>~ id.
:END:
** [X] LiveView integration tests cover the row content for each state, the reversed row's struck-through price and reversal date, lapsed omission, an archived reward's redemption still listed, and the stated 50-entry bound. (repo convention, design §Testing)
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:25] \\
  ~test/bear_cub_web/live/admin/reward_live_test.exs~'s "history" describe block covers all named cases: full row content for approved and declined states, the reversed row's strike-through and reversal date, lapsed omission, an archived reward's redemption still listed under its current name, and the 50-entry bound with its on-screen statement — verified via ~workflow-kit:story-verifier~ (initial pass flagged the declined-state test as a subset of the approved one; extended to check the full field set and re-verified as met).
:END:

* Definition of Done

** [X] All acceptance criteria have been either met or have had a documented removal or deferral
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:27] \\
  All 8 ACs above are marked ~[X]~ with LOGBOOK entries naming concrete evidence; none deferred or removed.
:END:
** [X] Tests written and passing for the acceptance criteria in this story
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:27] \\
  ~mix test test/bear_cub/rewards_test.exs test/bear_cub_web/live/admin/reward_live_test.exs~ → 96 passed, 0 failures. One test per AC clause in the "history" describe block, plus context-level coverage in ~rewards_test.exs~'s ~list_redemption_history/1~ describe block.
:END:
** [X] Code follows style guidelines in CLAUDE.md
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:27] \\
  ~RewardLive.History~ uses ~<Layouts.admin flash={@flash} active={:rewards}>~, ~<.link navigate={...}>~ (no deprecated ~live_redirect~), correct curly-brace interpolation, DOM ids on all key elements. ~list_redemption_history/1~ preloads ~:kid~ and ~:reward~ per the Ecto guideline. Verified via ~workflow-kit:story-verifier~ spot-check of all four changed/new files.
:END:
** [X] The repo's declared ~Verify command:~ (CLAUDE.md; default ~mix format --check-formatted~ and ~mix test~) passes without errors
:LOGBOOK:
- Note taken on [2026-07-25 Sat 16:27] \\
  ~mix precommit~ (~compile --warnings-as-errors~, ~deps.unlock --unused~, ~format~, ~test~) exits 0 with ~Result: 441 passed~, no compile warnings, ~mix.lock~ untouched. The ~Exqlite.Connection ... disconnected~ log lines during the run are accepted noise per repo convention, not a failure.
:END:

* Technical Notes

The Design excerpt above is the source of truth; DESIGN.org is broader context.

- Depends on Story 03 (the rows and their markers), Story 04 (the catalog this route is linked from, and the archive control the archived-still-shows test needs), and Story 06 (reversals to render).
- Purely a read surface: one query with the reward and kid preloaded, ordered newest first, limited to 50. No writes, no controls beyond navigation.
- Preload the reward and kid associations in the query — the rows reference them in the template (CLAUDE.md Ecto guideline).
- The bound is stated in the UI, not just enforced in the query; a test asserts the on-screen statement, not only the row count.
- The kid color chip reuses the shipped kid-color treatment; this is an admin surface, so ~docs/design-language.org~ (kids' view) does not govern it and no new Ruling is owed.
- This story completes the design's ~archive-hides-from-catalog-but-not-history~ integration test, whose catalog half landed in Story 04.
