#+title: PLAN — Bear Cub: Rewards / Redemption
#+date: 2026-07-25

# Mutability: execution state. Story status and implementation observations are recorded here as LOGBOOK entries per the workflow-kit org-conventions skill. By phase close, this file should read as an accurate record of what was actually built.

Design: [[file:DESIGN.org]]

* Batch 1

# Stories in this batch nest below as org TODOs, listed in implementation order; status updates get LOGBOOK entries under the story heading. At phase-close the gate record for this batch lands here as a LOGBOOK entry on the batch heading — its presence marks the batch closed. At most one batch is open (has no gate record) at a time; new batches are appended by later user-stories runs, never over an open one.

Ordering rationale (human-set, load-bearing): 01 → 02 is forced by the design's own sequencing — the aggregate rewrite lands before affordability depends on it ([[file:../decisions.org::#d72][D72]]), and the pinning suite is the proof of behavior preservation, so it must exist first. 03 lands the whole rewards domain headless, so every write path and derivation is unit-proven before any surface calls it. 04 → 05 → 06 is chosen so each UI story can be exercised against real data from the one before it: the catalog gives the kiosk shop something to render, and the kiosk gives the approval queue real requests to answer. 07 reads the record the previous three created.

Batch close — the on-device gate checklist from the design's §Testing (a manual human checklist per repo convention: never CI'd, never skipped): the reward view rendering in Fully Kiosk's Android WebView; the gift button's tap target at fridge distance and at a child's height; the dim+glyph states distinguishable at a glance across the kitchen; the idle-timer duration tuned there, not at a desk; and explicit confirmation that ~:rows~ still meets the 5-chore no-scroll budget with the banner button present.

** DONE [[file:stories/story-01-points-foundation.org][Story 01 — Points foundation: ~BearCub.Points~, ~Chores.earnings/2~, and the pinning suite]]
:LOGBOOK:
- Note taken on [2026-07-25 Sat 12:07] \\
  Closed out: all 8 ACs and all 4 DoD items ~[X]~. ~BearCub.Points~ (~lib/bear_cub/points.ex~) exposes ~balance/2~ / ~total/2~ / ~balances/1~ / ~totals/1~, all guarding on ~%Date{}~; ~Chores.points_total/2~ became ~Chores.earnings/2~ with the ~max(0, …)~ floor moved up to ~Points~, verified a pure rename (the ~git diff~ is two hunks, both inside the old function; ~extra_contribution/2~ and ~routine_day_contribution/3~ byte-unchanged), so ~Points.total/2~ is pointwise identical to the shipped derivation. ~balances/1~ is the per-kid loop the story's Technical Notes sanction; the grouped-query rewrite behind the same signature is Story 02. The pinning suite (~test/bear_cub/points_test.exs~, 27 tests) is the story's primary deliverable and is proven to bite, not merely to pass — mutating ~balances/1~ kills 5 tests including both roster-drift pins by name, independently reproduced by the verifying subagent. ~mix precommit~ clean, 304 passed. \\
  *AC-3's premise was wrong as written, and is corrected rather than silently checked.* The AC required every shipped call site of the old name to read through ~Points~ — "the kiosk column's ~points:~ assign and the admin balance reads". There are no admin balance reads: no admin LiveView displays a balance today. The design's cost paragraph was describing surfaces this batch has yet to build — admin balance displays arrive with their owning stories (04's direct-redeem kid picker, 06's approval-queue rows, 07's history view), each specifying ~Points.balance/2~ per [[file:../decisions.org::#d64][D64]]. The one real admin-side reference was the kiosk-badge assertion in ~test/bear_cub_web/live/admin/today_live_test.exs~, re-pointed to ~Points.total/2~; nothing was left behind (~grep -rn "points_total" lib test priv~ returns zero code hits). The clause is *vacuous, not deferred* — no phantom deferral is recorded against it, and the AC is met. No DESIGN.org pivot: the design's prose is a forward-looking cost estimate for the initiative as a whole, not a claim about shipped code, so this is an implementation-time observation recorded here rather than a body edit or Advisory. \\
  One tightening during close-out, on the verifying subagent's finding: the roster-drift tests initially pinned only ~Points.balance/2~, while the story's Design excerpt (§Testing) requires "the ~Points.balances~ aggregate pinned … including the roster-drift case". Both tests now assert through ~balance/2~ *and* ~balances/1~ on each side of the roster edit. Load-bearing for Story 02, not tidiness: [[file:../decisions.org::#d72][D72]]'s target has ~balances/1~ become the grouped query with ~balance/2~ delegating to it, so pinning only the singular form would have left the grouped query's drift path unproven — the exact weird path D72 requires covered. Three Deferred discoveries recorded in the story's Technical Notes.
:END:
** TODO [[file:stories/story-02-aggregate-rewrite.org][Story 02 — Aggregate rewrite: ~Points.balances/1~ in grouped queries]]
** TODO [[file:stories/story-03-rewards-domain.org][Story 03 — Rewards domain, headless: schema, write paths, derivations, PubSub]]
** TODO [[file:stories/story-04-admin-catalog.org][Story 04 — Admin catalog: ~/admin/rewards~, form, ordering, archive, direct-redeem]]
** TODO [[file:stories/story-05-kiosk-shop.org][Story 05 — Kiosk shop: the ~:rewards~ column state, banner button, and card precedence]]
** TODO [[file:stories/story-06-approval-queue.org][Story 06 — Approval queue and reversal on ~Admin.TodayLive~]]
** TODO [[file:stories/story-07-history-view.org][Story 07 — Redemption history: ~/admin/rewards/history~]]

* Deferred

# Out-of-scope discoveries land here during implementation instead of being fixed inline. Promoted to v2 candidates at initiative close.

- Roster drift ([[file:../decisions.org::#d72][D72]]) — ~routine_day_contribution/3~ evaluates historical routine-days against the kid's *current* chore roster, so a roster edit retroactively moves past ~+R~ and can move a balance after a spend. Pinned as-is by Story 01 and preserved unchanged by Story 02, per D72. Swept to ~docs/backlog.org~ at initiative close.
- Fulfillment marker on redemptions (design §Out of Scope) — "act if double-buy or forgot-to-buy occurs in family use". Swept to ~docs/backlog.org~ at initiative close as an untriaged entry.
- History pagination (D70) — the list is bounded at 50 with the bound stated on screen; pagination is a backlog seed if it ever bites.
