#+title: PLAN — Bear Cub MVP
#+date: 2026-07-11

# Mutability: execution state. Story status and implementation observations are recorded here as LOGBOOK entries per the workflow-kit org-conventions skill. By phase close, this file should read as an accurate record of what was actually built.

Design: [[file:DESIGN.org]]

* Batch 1 — Phases 1–3 (pre-kit)
:LOGBOOK:
- Note taken on [2026-07-11 Sat 10:09] \\
  Completed before kit adoption. On-device QA was performed per PRD §7 at each phase (1–3). No stories exist for this batch and none are being retroactively reconstructed — the record of work is git history plus DESIGN.org §10 decision log entries D8–D24.
:END:

* Batch 2 — Phase 4: Calendar overlay
:LOGBOOK:
- Note taken on [2026-07-11 Sat 13:30] \\
  Inserted Story 05 (monthly RRULE expansion + graceful degradation on unsupported =FREQ=) into this batch, immediately after Story 01, per human authorization. The authorization's original evidence ("7 MONTHLY/YEARLY recurring events, 2 per kid calendar") was checked against =tmp/real-feeds/*.ics= before writing anything and found inflated: scoped strictly to =VEVENT= blocks (excluding each calendar's =VTIMEZONE= boilerplate DST-transition =RRULE=s, which also use =FREQ=YEARLY= and were being counted as if they were family events), the real feeds contain exactly one =FREQ=MONTHLY= =VEVENT= (=child-b.ics=) and zero =FREQ=YEARLY= =VEVENT=s. Human chose to proceed with the corrected evidence, scoping Story 05 to the one real =MONTHLY= event plus a graceful-degradation AC for unsupported =FREQ= (closing the =CaseClauseError= gap Story 01's closeout audit found), rather than the originally stated 7-event/blocker framing. See story-05-monthly-yearly-rrule.org Technical Notes for full detail.
:END:

** DONE [[file:stories/story-01-ics-parser-spike.org][Story 01 — ICS parser spike]]
:LOGBOOK:
- Note taken on [2026-07-11 Sat 12:44] \\
  Closed out: all 4 ACs and all 4 DoD items =[X]=. =icalendar= 1.1.3 was evaluated and rejected — it silently drops =RECURRENCE-ID= (no struct field, no parser clause), a hard fail against the D16 correctness bar — and =BearCub.Calendars.ICS= was hand-rolled instead (=lib/bear_cub/calendars/ics.ex= + =lib/bear_cub/calendars/ics/instance.ex=), fixture-tested against six sanitized fixtures derived from the family's real feeds (=test/support/fixtures/ics/=), 7/7 tests passing. See the story's Technical Notes for full evidence; this decision needs a DESIGN.org D-entry (handled in this update-design pass).
:END:
** DONE [[file:stories/story-05-monthly-yearly-rrule.org][Story 05 — Monthly RRULE expansion & graceful degradation on unsupported FREQ]]
:LOGBOOK:
- Note taken on [2026-07-11 Sat 13:51] \\
  Closed out: all 4 ACs and all 4 DoD items =[X]=. =expand_monthly_byday/4= added to =lib/bear_cub/calendars/ics.ex=, dispatched from =expand_rrule/3= for =FREQ=MONTHLY;BYDAY=<ordinal><weekday>=; verified against a new sanitized fixture (=monthly_recurring_event.ics=, derived from the real =child-b.ics= =MONTHLY= =VEVENT=) covering 10 in-window occurrences across the Mar/Nov DST transitions with both =EXDATE=s honored. =expand_rrule/3='s =case= gained a catch-all closing Story 01's flagged =CaseClauseError= gap — an unsupported =FREQ= (e.g. =FREQ=YEARLY=) now returns zero occurrences for that event instead of raising. =mix test= → 131 passed. DESIGN.org updated with D26 + a paired Advisory (§6 correctness-bar scope clarification); see this pass's DESIGN.org changes.
:END:
** DONE [[file:stories/story-02-calendars-schema-admin.org][Story 02 — Calendars schema, context, and admin CRUD]]
:LOGBOOK:
- Note taken on [2026-07-11 Sat 14:14] \\
  Closed out: all 5 ACs and all 4 DoD items =[X]=. =BearCub.Calendars= context (list/get/create/update/delete/change_calendar, PubSub subscribe/broadcast on ="calendars"=) and =Admin.CalendarLive.Index/Form= added at =/admin/calendars=(=/new=|=/:id/edit=); admin bottom tab bar gained a 4th "Calendars" destination. The calendars schema/migration already existed (scaffolded alongside kids/chores/completions pre-kit); this story added only the context and LiveViews, hand-written directly in the =Index/Form= shape matching =KidLive=/=ChoreLive= rather than re-invoking =mix phx.gen.live= against an existing schema — same target shape §4's implementation directive calls for, reached without the generator step since there was nothing left for it to scaffold. 24 new tests (context + LiveView CRUD flows + redaction-in-rendered-HTML + PubSub live-update), =mix test= → 151 passed, =mix precommit= clean. No DESIGN.org pivot: implementation matches §1/§4 verbatim (module naming already anticipated by §4's "Index/Form... superseding this doc's earlier single-module names").
:END:
** DONE [[file:stories/story-03-calendar-refresh-pipeline.org][Story 03 — Calendar refresh pipeline]]
:LOGBOOK:
- Note taken on [2026-07-11 Sat 14:44] \\
  Closed out: all 7 ACs and all 4 DoD items =[X]=. =BearCub.Calendars.Refresher= added (=lib/bear_cub/calendars/refresher.ex=) — the app's only periodic GenServer, supervised conditionally in =lib/bear_cub/application.ex= (disabled in test). =BearCub.Calendars= gained the fetch/parse/cache pipeline: =hydrate_cache/1= (boot-time, cache-only), =refresh_calendar/2=/=refresh_all/1= (live fetch via =Req=, per-calendar failure containment for transport/HTTP/parse errors, change-gated ="calendars"= broadcast), =stale?/2= (pure staleness check), =today_events/2= (ETS-backed, blends a kid's calendars with family calendars). =Calendar.cache_changeset/2= added, kept separate from the admin-facing changeset. =:req= promoted from transitive to a direct =mix.exs= dependency per CLAUDE.md. 25 new tests (=test/bear_cub/calendars_test.exs= + =test/bear_cub/calendars/refresher_test.exs=), =mix test= → 166 passed, =mix precommit= clean. One implementation-time correction caught in self-review: =handle_success= originally called =DateTime.utc_now()= directly, violating the "no buried clock reads in domain code" invariant — fixed to thread the caller-supplied =local_now= through instead. No DESIGN.org pivot: implementation matches §6 verbatim (GenServer name, fetch/parse/serve/hygiene/staleness-threshold behavior all as specified).
:END:
** DONE [[file:stories/story-04-kiosk-events-strip.org][Story 04 — Kiosk events strip]]
:LOGBOOK:
- Note taken on [2026-07-11 Sat 15:31] \\
  Closed out: all 8 ACs and all 4 DoD items =[X]=. =Calendars.today_events/2= (=lib/bear_cub/calendars.ex=) extended to tag each event =family?= (from the source calendar's =kid_id=), clip midnight-spanning events to the local day window (=clipped_start?=/=clipped_end?=), and sort all-day events ahead of timed ones; added =Calendars.any_stale?/1= (global staleness check, D4). =KioskLive= now subscribes to the ="calendars"= PubSub topic, renders the blended per-kid events strip (kid-color dot vs. family neutral chip + house glyph, midnight-clip rendering "until 2:00 PM"), and shows a =#calendar-stale-glyph= clock icon when any calendar is stale. Two rounds of story-verifier audit: the first found a coverage gap (no LiveView test combined a personal + family event to prove blended chronological rendering end-to-end — closed by adding one); the second (Definition of Done pass) caught 4 stale line-number citations in the story's own LOGBOOK entries (drifted after a formatter reflow), corrected in place. Along the way, fixed 4 pre-existing tests in =calendars_test.exs= and =refresher_test.exs= that emitted unasserted =Logger.warning= noise — wrapped in =ExUnit.CaptureLog.with_log/capture_log= instead. 24 new tests, =mix test= → 184 passed, =mix precommit= clean. No DESIGN.org pivot: implementation matches §5/§6 verbatim. One Deferred discovery carried below: the story's Technical Notes assumed the socket-down "dim dot" already existed from an earlier phase; it does not — DESIGN.org §5's D4 corner-glyph restyle of the stock Phoenix disconnect banner into a dim dot was never actually implemented.
:END:

* Deferred

- From Story 01, implemented by Story 05: =FREQ=MONTHLY= RRULE expansion (=BYDAY=<ordinal><weekday>= form) and graceful degradation on unsupported =FREQ= — no longer deferred; see [[file:stories/story-05-monthly-yearly-rrule.org][Story 05]] and DESIGN.org D26.
- From Story 01, still deferred: =FREQ=YEARLY= expansion (Story 05 only degrades gracefully on it, does not expand it — zero real =YEARLY= =VEVENT=s observed in the family's feeds, scoped strictly to =VEVENT= blocks and excluding =VTIMEZONE= boilerplate), plus =COUNT=, =BYMONTHDAY=, =BYSETPOS=, =BYMONTH=. The family's real feeds also contain =FREQ=DAILY= events with large =INTERVAL= values and no =COUNT=/=UNTIL= — already handled correctly by the existing safety-cap bound, not a correctness gap. If a future real-feed fetch surfaces a recurrence shape the module still can't expand, extend =expand_rrule/3= rather than reaching for a library (D25 rules out =icalendar= categorically for this project).
- From Story 01: recurring all-day events (=RRULE= combined with =VALUE=DATE=) are unhandled — no required fixture combines them, and none were observed in the family's real feeds.
- From Story 02: =Admin.CalendarLive.Index= renders its list with plain =<li :for={calendar <- @calendars}>= rather than a LiveView =stream/3=, matching the pre-existing =KidLive.Index=/=ChoreLive.Index= pattern (not a regression introduced by this story). CLAUDE.md's LiveView guidelines call for streams on all collections; whether to retrofit all three admin index views to streams is an open repo-wide question, not scoped to any single story.
- From Story 03: a full =mix test= run occasionally logs a non-fatal =[error] Exqlite.Connection ... disconnected: ** (DBConnection.ConnectionError) client ... exited= line (no test failures — =Result: 166 passed= either way). Only observed in full-suite runs, not when running the calendar test files in isolation; likely a benign race between a =start_supervised!=-owned =Refresher= process and the Ecto Sandbox owner during another test's teardown. Not investigated further; worth a look if it recurs or worsens. Still reproduces post-Story-04 (now =184 passed= either way), unrelated to that story's changes — confirmed by running the unmodified pre-Story-04 tree and seeing the same intermittent log.
- From Story 04: the socket-down corner glyph is still the stock, unstyled Phoenix =phx-disconnected= flash banner ("We can't find the internet") in =lib/bear_cub_web/components/layouts.ex= — DESIGN.org §5's D4-described restyle into a small dim corner dot (matching the new staleness clock glyph's placement and treatment) was never actually implemented in an earlier phase, despite Story 04's Technical Notes having assumed it was. Purely cosmetic (the functional disconnect-banner mechanism already works); pick up whenever kiosk visual polish is scheduled.
