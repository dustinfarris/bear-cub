#+title: Story 01 — Chore lifetime columns: the ~add_chore_lifecycle~ migration and its backfill
#+date: 2026-09-04

Design: [[file:../DESIGN.org]]

* Design

Verbatim from DESIGN.org for this story's scope; org links are repointed one level up so they resolve from ~stories/~.

** Data Model

Three new columns on ~chores~. No new tables, no new indexes — the existing ~[:kid_id, :routine, :position]~ covers these at family scale.

| Column         | Schema field   | Type                               | Meaning                                                                            |
|----------------+----------------+------------------------------------+------------------------------------------------------------------------------------|
| ~active_from~  | ~:active_from~ | ~date~, not null                   | Local date the chore became live; set at creation from the caller's local datetime  |
| ~archived_on~  | ~:archived_on~ | ~date~, nullable                   | Local date it was archived; ~nil~ means live. One-way — never cleared               |
| ~is_recurring~ | ~:recurring?~  | ~boolean~, not null, default false | Extras only: comes back every day instead of retiring after one completion          |

The boolean is named ~is_recurring~ in the database and ~recurring?~ in the schema. Ecto's ~field/3~ ~:source~ option carries the mapping, and Ecto rewrites ~ch.recurring?~ to the real column in query expressions:

#+begin_src elixir
field :recurring?, :boolean, source: :is_recurring, default: false
#+end_src

*Hazard.* A raw ~fragment(...)~ bypasses that translation and must spell ~is_recurring~. ~extras_by_kid/1~ already contains such a fragment, so any future reference to the flag from inside one is a live trap.

~active_from~ and ~archived_on~ are *local dates, not UTC timestamps*: they are compared against ~completions.local_date~ inside the points derivation, and mixing the two date spaces there is exactly how day-edge bugs get in. This is a deliberate divergence from the ~rewards.retired_at~ sibling, which is a UTC timestamp — rewards never compare their marker against a historical date. Both fields are set by the context from a local datetime the caller passes: ~archive_chore/2~ takes one as ~complete_chore/3~ and ~archive_reward/2~ already do, and ~create_chore/2~ gains one, becoming ~create_chore/3~ ([[file:../../decisions.org::#d86][D86]]). The standing "all time logic takes a local datetime argument" invariant holds, with no clock read in domain code.

The column is ~archived_on~, not ~retired_at~: "retired" is already taken in chore vocabulary for the *derived* state of a one-off extra completed on a prior day (D28/D29), and that meaning survives this change. Rewards' own function is already ~archive_reward/2~ despite its ~retired_at~ column, so "archive" is the verb the repo actually uses; chores take the verb and name the column to match.

~Chore~ also gains a *virtual* field ~shows_in~ (a plain string, not a predicate), which drives the admin select and from which the changeset derives ~routine~ and ~recurring?~ — see §Screens / Interfaces.

~completions~ is untouched: same table, same ~(chore_id, local_date)~ partial unique index, same undo / fail / on-behalf machinery. An archived chore keeps every completion row it ever had, which is the whole point.

Rationale: [[file:../../decisions.org::#d80][D80]], [[file:../../decisions.org::#d82][D82]].

** Migration and backfill

One migration, ~add_chore_lifecycle~:

- ~add :is_recurring, :boolean, null: false, default: false~
- ~add :archived_on, :date~ (nullable)
- ~add :active_from, :date, null: false, default: "1970-01-01"~

SQLite can add a ~NOT NULL~ column only with a constant default, so ~active_from~ arrives as an epoch floor — the *conservative* direction, since "live since forever" can only ever grow a historical roster, never shrink one and fabricate a bonus. No table rebuild is needed; nothing here alters an existing column.

The same migration backfills the accurate value:

#+begin_src sql
UPDATE chores
   SET active_from = MIN(DATE(inserted_at),
                         COALESCE((SELECT MIN(local_date) FROM completions
                                    WHERE chore_id = chores.id),
                                  DATE(inserted_at)))
#+end_src

*The ~MIN~ of the two is load-bearing.* If a backfilled ~active_from~ ever landed *after* a day the chore was already being completed, that day's roster would shrink and hand out a ~+R~ that was never earned. Taking the earliest of "created" and "first completed" makes that impossible, and absorbs the UTC-versus-local skew in ~DATE(inserted_at)~ for evening-created chores.

** Testing (excerpt)

*Test hygiene*: every test passes fixed local dates and pins ~:routine_windows~ (~docs/learnings.org~ [2026-07-17]); no new test derives a date from the wall clock.

** Decision Log entries in scope

- [[file:../../decisions.org::#d80][D80]] — Chore lifetime is two local-date columns, ~active_from~ / ~archived_on~; naming and date space diverge from ~rewards.retired_at~ deliberately, and the backfill takes the ~MIN~ of created and first-completed.
- [[file:../../decisions.org::#d82][D82]] — Extras may repeat daily via ~is_recurring~ / ~recurring?~ (the column this story lands; its behavior is Story 05).

* User Story

As a parent, I need every chore to carry the local date it became live, a place to stamp the date it ends, and a truthful backfilled value for the chores we already have — so that later changes to today's roster can never reach backwards and move points a kid already earned.

* Acceptance Criteria

** [X] Every chore that existed before this change carries an ~active_from~ no later than both the local date it was created and the earliest ~local_date~ it was ever completed on, so no historical roster can shrink and hand out a bonus that was never earned. (SC-3, SC-5, D80)
:LOGBOOK:
- Note taken on [2026-09-04 Fri 20:59] \\
  Met only after a mid-story design correction. The excerpt's SQL backfill was implemented verbatim and fails this criterion: ~DATE(inserted_at)~ is UTC, so a chore created after 17:00 local and never completed is stamped a day late — confirmed against SQLite, which returned =2026-07-11= for a chore created 2026-07-10 19:00 local. The ~MIN~ rescues that only when a completion already exists on the earlier local day, so [[file:../../decisions.org::#d80][D80]]'s "absorbs the UTC-versus-local skew" claim holds for completed chores only. Ruled by the human [2026-09-04]: convert per row into the configured timezone. The backfill is now ~AddChoreLifecycle.backfill/1~ (Elixir, ~DateTime.shift_zone!~ then ~Enum.min~ against the earliest completion date). Pinned in-repo by "an evening-created chore never completed is live from its local creation day" and by the table-driven "the local creation day is recovered across offsets, DST and inserted_at spellings", which covers a PST evening, both sides of each DST transition, a year boundary, and the space-separated and microsecond ~inserted_at~ spellings — an audit had these cases passing only in a throwaway harness, so they were brought into the suite where a regression will be noticed. Verified to bite: dropping the timezone conversion fails both tests (=pst-evening became live on the wrong day=), and collapsing the ~MIN~ fails "a chore completed before its created date becomes live on that earlier day". Separately confirmed end-to-end by running the real ~Ecto.Migrator~ over hand-seeded pre-lifecycle rows, which is also how a second defect surfaced: a migration runs with the app unstarted, so ~shift_zone!~ raised =:time_zone_not_found= until the backfill started ~:tzdata~ itself. Landed at ~/workflow-kit:update-design~ as [[file:../../decisions.org::#d87][D87]], the §Migration and backfill body edit, and an Advisory carrying the superseded SQL.
:END:
** [X] A chore created from this point on carries an ~active_from~ equal to the local date it was created on, taken from a local datetime supplied by the caller rather than from a clock read inside domain code. (SC-3, SC-5, D80)
:LOGBOOK:
- Note taken on [2026-09-04 Fri 20:59] \\
  ~create_chore/3~ (~lib/bear_cub/chores.ex:109~) sets ~active_from~ with ~Ecto.Changeset.put_change(:active_from, DateTime.to_date(local_now))~, alongside ~position~ and never cast — ~Chore.changeset/2~ still casts only ~[:routine, :name, :icon, :points]~. The clock read sits at the web edge: ~LocalTime.now()~ at ~admin/chore_live/form.ex:59~. Pinned by ~"create_chore/3 stamps active_from from the caller's local datetime"~ (19:30 local, so it also pins that the *local* date is taken, not a UTC-derived one) and ~"create_chore/3 never casts active_from from attrs"~; a constant value there fails those two along with the form and re-run-guard pins — four in total — and ~BearCub.Chores~ contains no clock read. \\
  A first audit found this criterion missed on one production path: ~priv/repo/seeds.exs~ builds chores by hand and so fell through to the column default, stamping =1970-01-01= on a fresh ~mix ecto.setup~ deploy. Fixed in scope — seeds now set ~active_from~ on the struct from ~LocalTime.now()~, exactly as they already set ~position~ (a seed script is an edge, so the clock read belongs there per [[file:../../decisions.org::#d86][D86]]) — and pinned by a new ~seeds_test.exs~ case, "seeded chores are live from the local date they were seeded", which failed at =1970-01-01= before the change. \\
  A second audit then showed the web-edge half unpinned: swapping ~LocalTime.now()~ for ~DateTime.utc_now()~ in the form left the whole suite green, against the repo convention that a touched flow gets a LiveView test. Closed with "a chore created through the form is live from today's local date" in ~chore_live_test.exs~, which submits ~#chore-form~ and asserts the created chore's ~active_from~ against a bracket taken around the submit — flake-free at a midnight boundary, unlike comparing against a second clock read. Verified to bite: with the form mutated to ~DateTime.utc_now()~ it fails. Both this guard and the seeds one are inherently time-of-day dependent, since a UTC read only diverges from the local date after 17:00 local; that is the cost of the clock read living at the edge, per [[file:../../decisions.org::#d86][D86]].
:END:
** [X] Every chore that existed before this change is still live and still one-off after the migration: ~archived_on~ is ~nil~ and ~is_recurring~ is false on every backfilled row. (SC-5, D80, D82)
:LOGBOOK:
- Note taken on [2026-09-04 Fri 20:59] \\
  Hand-seeded pre-lifecycle rows all came out of a real ~Ecto.Migrator~ run at ~archived_on = nil~, ~is_recurring = 0~, and the already-migrated dev database shows the same across all 16 of its rows. Pinned by ~"every backfilled chore is still live and still one-off (D82)"~. \\
  That test alone was not honest evidence for the recurrence half, and the subagent proved it: rows built through ~chore_fixture~ pass ~Ecto~, which spells ~is_recurring~ out on every INSERT because of the schema default, so flipping the migration to ~default: true~ left the whole suite green. Closed with ~"the lifecycle columns land with the declared types, nullability and defaults"~, pinning the DDL that a genuinely pre-lifecycle row actually depends on. Verified to bite: with the migration flipped to ~default: true~ and the test database re-migrated, it failed =left: {"INTEGER", 1, "true"}=; restored, 14 pass.
:END:
** [X] Every kid's derived point total is identical immediately before and immediately after the migration runs — the columns land without moving a single score. (SC-5, D80)
:LOGBOOK:
- Note taken on [2026-09-04 Fri 20:45] \\
  Measured across the real migration by the ~story-verifier~ subagent, which is the load-bearing evidence: on a pre-lifecycle database with 2 kids, 5 chores (including an evening-created one, a PST one and an extra) and 10 completions of which one was failed, ~Points.balance/2~ and ~Points.total/2~ were snapshotted for every kid across 2026-07-07..2026-07-13, ~Ecto.Migrator~ ran, and the snapshot was retaken — identical, with 11 of 14 samples non-zero. Structurally corroborated: ~lib/bear_cub/points.ex~ is untouched and no production code outside the schema, ~create_chore/3~ and the migration mentions the new columns, so the derivation cannot see them yet. The in-suite pin ~"no kid's derived points move when the backfill runs"~ brackets the backfill over a multi-day fixture; it guards later stories rather than carrying evidentiary weight today, since nothing yet reads the columns for a mutation to disturb.
:END:
** [X] ~archived_on~ accepts a local date and ~is_recurring~ a boolean on any chore row, while no existing ~chores~ column is altered and ~completions~ is untouched — same table, same ~(chore_id, local_date)~ partial unique index. (SC-4, D80)
:LOGBOOK:
- Note taken on [2026-09-04 Fri 20:45] \\
  A before/after schema diff across the real migration reported no ~chores~ column removed or altered, three plain ~ADD COLUMN~ statements (no table rebuild, so the ~completions.chore_id~ FK and its cascade are untouched), ~completions~ identical, indexes identical, and the partial unique index still reading ~ON "completions" ("chore_id", "local_date") WHERE undone_at IS NULL~. In-suite: ~"archived_on takes a local date and is_recurring a boolean"~ round-trips both fields, ~"recurring? reads and writes the is_recurring column"~ writes the column by raw SQL and reads it back through the schema (pinning the ~:source~ mapping — making the field virtual fails it), ~"no pre-existing chores column is altered"~ pins the original columns' types, nullability and defaults, and ~"completions is untouched"~ pins its columns, its partial unique index and a live completion round-trip. No new index was added, per the excerpt.
:END:

* Definition of Done

** [X] All acceptance criteria have been either met or have had a documented removal or deferral
:LOGBOOK:
- Note taken on [2026-09-04 Fri 21:11] \\
  All five are ~[X]~; none was removed or deferred. Two were only met after audit findings forced real changes rather than a re-reading of the criterion: AC-1 failed outright on the excerpt's SQL backfill and was fixed by a human-ruled design correction, and AC-2 missed on ~priv/repo/seeds.exs~, which bypassed ~create_chore/3~ and stamped =1970-01-01= on a fresh ~mix ecto.setup~ deploy. A ~story-verifier~ subagent re-derived each criterion's substance independently — a real ~Ecto.Migrator~ run over hand-seeded pre-lifecycle rows, and a before/after ~sqlite_master~ diff — rather than reading the notes back, and checked the notes' concrete claims against the repo; two evidence numbers it found stale or undercounted (a test count and a mutation-failure count) are corrected above.
:END:
** [X] Tests written and passing for the acceptance criteria in this story
:LOGBOOK:
- Note taken on [2026-09-04 Fri 21:11] \\
  ~mix precommit~ → =Result: 495 passed=, up from 489 at the start of the story. Each criterion is backed by a test proven to fail on a plausible regression, not merely to pass: dropping the timezone conversion fails two backfill tests, collapsing the ~MIN~ fails one, a constant ~active_from~ in the context fails four, ~LocalTime.now()~ → ~DateTime.utc_now()~ in the admin form fails the new LiveView pin, the same swap in the seed script fails the seeding pin, a flipped ~is_recurring~ column default fails the DDL pin, and making ~recurring?~ virtual fails two column tests. \\
  An audit initially found this item *not* met: the web-edge half of AC-2 was unpinned, so mutating the form to a UTC clock read left the suite green — against the repo convention that a touched flow gets a LiveView test. Closed with ~chore_live_test.exs~'s form pin. Two limits are recorded rather than smoothed over: the two clock-bracket guards cannot bite before 17:00 local, when the local and UTC dates agree (see the departure note in Technical Notes), and AC-4's in-suite pin cannot fail on any mutation available today because nothing yet reads the new columns — its real evidence is the migrator run.
:END:
** [X] Code follows style guidelines in CLAUDE.md
:LOGBOOK:
- Note taken on [2026-09-04 Fri 21:11] \\
  ~mix format --check-formatted~ exits 0 standalone (run before ~precommit~, so its ~format~ step could not mask it), and ~mix compile --warnings-as-errors~ is clean — including on a dropped-and-recreated test database, where a module-redefinition warning had appeared until ~run_backfill/0~ gained a ~Code.ensure_loaded?/1~ guard. No comment, ~@doc~ or ~@moduledoc~ line in any file this story touches exceeds 80 columns. Programmatic fields stay out of ~cast/3~ and are set with ~put_change~; the predicate field ends in ~?~; the migration carries a generated timestamp; no new index, per the excerpt. Org conventions checked in this file by the subagent: unwrapped prose, one LOGBOOK drawer of one timestamped note per item, correct weekday, ~code~ for identifiers and =verbatim= for stored values and captured output, and no line-initial asterisk that would parse as a heading.
:END:
** [X] The repo's declared ~Verify command:~ (CLAUDE.md; default ~mix format --check-formatted~ and ~mix test~) passes without errors
:LOGBOOK:
- Note taken on [2026-09-04 Fri 21:11] \\
  This repo declares ~mix precommit~. Green at =Result: 495 passed=, exit 0, both on the working database and from a dropped-and-recreated one; ~deps.unlock --unused~ removed nothing and the ~format~ step rewrote nothing, so ~git status~ is unchanged by the run. The only remaining output noise is ~[debug]~ migration logging and the ~Exqlite.Connection … disconnected~ lines, both of which I confirmed against a stashed baseline are pre-existing and unrelated to this story. The CLAUDE.md default reading also passes: ~mix format --check-formatted~ exit 0, ~mix test~ 495 passed.
:END:

* Technical Notes

The Design excerpt above is the source of truth for this story; DESIGN.org is broader context only.

This story has no dependency on any other — it is the data model every later story in the batch builds on. It deliberately changes *no* behavior: nothing reads ~archived_on~ or ~is_recurring~ yet (Stories 02, 03 and 05 do), and nothing writes ~archived_on~ yet (Story 03 does).

Generate the migration with ~mix ecto.gen.migration add_chore_lifecycle~ so the timestamp and conventions are applied. Column order and the SQLite constant-default constraint are as the excerpt states; the backfill ~UPDATE~ runs in the same migration, after the ~add~s.

~BearCub.Chores.Chore~ gains ~:active_from~ (~:date~), ~:archived_on~ (~:date~) and ~field :recurring?, :boolean, source: :is_recurring, default: false~. The virtual ~shows_in~ field and the changeset derivation that drives it are Story 04's scope, not this story's — here the schema simply carries the columns.

Carry the excerpt's ~fragment~ hazard forward when touching ~lib/bear_cub/chores.ex~: ~extras_by_kid/1~ already contains a raw ~fragment(...)~, and any reference to the recurrence flag from inside one must spell ~is_recurring~, not ~recurring?~.

The rewards sibling settles half of this and not the other half. Lifecycle *markers* there are stamped by the context from a caller-supplied ~%DateTime{}~ through a private ~to_utc/1~ — ~Rewards.archive_reward(reward, LocalTime.now())~ at ~admin/reward_live/index.ex~ — which is the precedent ~archive_chore/2~ follows in Story 03, differing only in storing a local date rather than a UTC instant. *Creation* has no such precedent: ~create_reward/2~ takes no datetime at all, and rewards stamp nothing at creation beyond Ecto's UTC ~timestamps/1~. ~active_from~ is the first field in the repo needing a local date at creation time.

*Ruled [2026-09-04] — creation takes the caller's local datetime: ~create_chore/3~ ([[file:../../decisions.org::#d86][D86]]).* ~active_from~ is stamped by the context from a ~%DateTime{}~ the caller supplies, with the clock read staying at the web edge (~LocalTime.now()~ in ~admin/chore_live/form.ex~, as ~archive_reward/2~ is already called from ~admin/reward_live/index.ex~). ~active_from~ is then ~DateTime.to_date(local_now)~, set by the context and never cast from attrs, alongside ~kid_id~ and ~position~.

The reason is the standing "all time logic takes a local datetime argument" invariant, *not* the test suite: the argument carries no production information — a chore's ~active_from~ is always the day the row was made — so what earns it is that dropping it would make ~create_chore~ the only date-writing function in the repo reading the clock itself. An earlier draft of this story justified the argument by what the tests need; that reasoning was rejected. Rejected alternatives, recorded in D86 so they are not relitigated at implementation time: a clock read inside ~create_chore/2~; a ~local_now \\ LocalTime.now()~ default argument; casting ~active_from~ from attrs; a SQLite ~CURRENT_DATE~ default.

The test consequence follows rather than motivates: ~chore_fixture~ can express a chore that became live on a past day through the real create path, which every date-bounded criterion in Story 02 depends on.

Two call sites change: ~admin/chore_live/form.ex~'s ~save_chore(socket, :new, params)~, and ~chore_fixture/2~ in ~test/support/fixtures/chores_fixtures.ex~.

Test hygiene per the excerpt: fixed local dates, ~:routine_windows~ pinned, no wall-clock reads. The "identical before and after" criterion is naturally expressed as a derivation pin — build a fixture with completions across several days, record ~BearCub.Points~ output, and assert it is unchanged once the columns exist.

Deferred discovery: the backfill's ~local_date_of/2~ raises a bare ~FunctionClauseError~ naming ~Calendar.ISO.parse_utc_datetime/2~ on an ~inserted_at~ it cannot parse, without naming the offending chore. Unreachable with Ecto-written data (~chores.inserted_at~ is ~TEXT NOT NULL~ and ~strftime~ normalizes every spelling SQLite accepts), and failing loudly is the right direction, but a data-repair scenario would get an unhelpful abort.

Deferred discovery: the epoch floor does double duty as the "not yet backfilled" sentinel, and ~chore_fixture/3~'s default ~local_now~ is that same epoch — so a pre-lifecycle row and a row deliberately created with an epoch datetime are indistinguishable to the backfill's guard. Correct today because no production caller passes one; worth knowing before anything else keys off the value.

Deferred discovery: because ~chore_fixture/3~ defaults to the epoch, every existing fixture chore is live on every date. Story 02's date-bounded tests must opt into a real ~local_now~ or they will pass vacuously.

Deferred discovery: ~priv/repo/seeds.exs~ still sets ~position~ by hand rather than going through ~Chores.create_chore/3~, which would compute it. This story stamped ~active_from~ there the same way rather than reroute the script; the bypass predates this batch.

Deferred discovery: the migration depends on application code and on a named timezone backend — it calls ~BearCub.LocalTime.timezone()~ and ~Application.ensure_all_started(:tzdata)~. Migrations are historical artifacts that outlive the code around them, so renaming that module, changing the ~:bear_cub, :timezone~ config key, or swapping the ~Tzdata.TimeZoneDatabase~ backend would break a migration that has already run everywhere but must still replay on a rebuilt box.

Deferred discovery: a ~%Chore{}~ inserted without ~active_from~ silently takes the epoch floor rather than failing — Ecto omits the nil field and SQLite supplies the default, which is exactly how the ~seeds.exs~ gap above went unnoticed. Nothing at the schema level stands behind the field; the row is then indistinguishable from a genuinely pre-lifecycle one.

Deferred discovery (pre-existing, adjacent): ~mix precommit~ runs ~format~, which rewrites files, rather than ~format --check-formatted~, so the declared verify command cannot fail on formatting.

Departure from the excerpt, ruled at ~/workflow-kit:update-design~ as [[file:../../decisions.org::#d88][D88]] — §Testing now excepts a test whose subject is an edge that reads the clock, and the four unimplemented stories' excerpts carry the amended line. The finding as recorded: §Testing said no new test derives a date from the wall clock, and two tests added here do — ~chore_live_test.exs~'s form pin and ~seeds_test.exs~'s seeding pin both bracket the run with ~LocalTime.now()~. There is no alternative under [[file:../../decisions.org::#d86][D86]], which puts the clock read at the edge with no injection point, and both follow a precedent already in ~chore_live_test.exs~. The cost is that neither guard can catch a UTC read before 17:00 local, when the two dates agree — so a CI run in the morning would not notice a re-regression. Ruled in favour of amending the hygiene line; the live alternative, an injectable clock at the web edge, is recorded as rejected in [[file:../../decisions.org::#d88][D88]].

Deferred discovery: replaying ~down~ then ~up~ inside a single BEAM process raises =duplicate column name: is_recurring=, because the migration file is loaded twice in the same VM and ~up/0~ runs twice. Separate ~mix ecto.rollback~ and ~mix ecto.migrate~ invocations are unaffected, as is a from-scratch create-and-migrate, so this bites only someone who later writes a round-trip migration test in one process.
