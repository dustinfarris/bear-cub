#+title: PLAN — Bear Cub: Chore lifecycle
#+date: 2026-09-04

# Mutability: execution state. Story status and implementation observations are recorded here as LOGBOOK entries per the workflow-kit org-conventions skill. By phase close, this file should read as an accurate record of what was actually built.

Design: [[file:DESIGN.org]]

* Batch 1
:LOGBOOK:
- Note taken on [2026-09-04 Fri 19:55] \\
  *Pre-implementation design correction — not a gate record; Batch 1 remains open and its gate is still pending. No story implemented.* Writing the batch's acceptance criteria surfaced a conflict inside DESIGN.org: §Reclassification locked the whole "Shows in" select once a chore had any completion row, while §Recurring extras' "two exits" and [[file:../decisions.org::#d82][D82]]'s stated-as-intended resurrection of a retired extra both require the recurrence half of that same select to work on a chore that has completions. Ruled by the human at story-writing time: the lock guards a change of ~routine~ only. Landed as the full three-step — [[file:../decisions.org::#d85][D85]] minted in the shared log, body edits to §Screens (the "Shows in" select), §Key Behaviors (Reclassification, which gains a second paragraph stating the toggle is not locked) and the ~*Changeset*~ testing line, and an Advisory carrying the superseded blanket-lock position. Stories 04 and 05 were written to the ruling; their embedded excerpts are refreshed against the new body text and their ruling notes now point at D85 rather than flagging an outstanding obligation. \\
  Two further story-writing rulings: the balance comparison the design requires under §Testing becomes a batch-close gate item rather than a sixth story (recorded in the gate checklist below, so the requirement is not lost with the story) — no DESIGN pivot, the requirement is unchanged; and chore creation takes the caller's local datetime ([[file:../decisions.org::#d86][D86]]), which paired with a repair to §Data Model's "the caller already passes" wording and its own Advisory.
:END:

# Stories in this batch nest below as org TODOs, listed in implementation order; status updates get LOGBOOK entries under the story heading. At phase-close the gate record for this batch lands here as a LOGBOOK entry on the batch heading — its presence marks the batch closed. At most one batch is open (has no gate record) at a time; new batches are appended by later user-stories runs, never over an open one.

Ordering rationale: 01 lands the data model alone and changes no behaviour, so every later story reads columns that already exist and are already backfilled truthfully. 02 comes *before* 03 deliberately, inverting the order the design's Overview narrates: the date-bounded roster must be in place before anything can be archived, or the first archive would be scored under today's-roster derivation and would retroactively complete past routine-days — the exact defect this initiative exists to close. 03 then lands archive end to end (domain, today's-surface filtering, the admin button, the live kiosk drop) and removes ~delete_chore/1~ with it. 04 gives the parent the "Shows in" select — the fourth option and the reclassification lock — so that 05's kiosk behaviour can be exercised from the real create/edit path rather than from a fixture-written flag. The one-time balance shift the design requires be measured is not a story — it is a gate item, recorded below.

No questions remain open — the one Story 02 close-out raised was ruled on 2026-09-05 and is recorded at the end of this section. *Human ruling [2026-09-04] — chore creation takes the caller's local datetime: ~create_chore/3~ ([[file:../decisions.org::#d86][D86]]).* Ruled on the standing "all time logic takes a local datetime argument" invariant rather than on test convenience: the argument carries no production information, and what earns it is that dropping it would make ~create_chore~ the only date-writing function in the repo reading the clock itself. An earlier draft of Story 01 justified it by what the tests need; that reasoning was rejected and the alternatives it skipped past — an internal clock read, a default argument, casting from attrs, a SQLite ~CURRENT_DATE~ default — are recorded in D86. Landed as D86 plus a repair to §Data Model's "the caller already passes" wording and its Advisory.

*Human ruling [2026-09-04] — the reclassification lock guards ~routine~ changes only, now recorded in the design.* DESIGN §Reclassification previously locked the whole "Shows in" select once any completion row existed, which made DESIGN §Recurring extras' "two exits" and [[file:../decisions.org::#d82][D82]]'s intended resurrection of a retired extra both unreachable. Ruled: the lock refuses a change of routine bucket and permits the recurrence toggle within extras. Landed as [[file:../decisions.org::#d85][D85]] plus the paired body edits and Advisory (see the batch LOGBOOK above); Stories 04 and 05 carry the updated excerpts.

*Human ruling [2026-09-05] — Story 02 AC-7 is retired by removing its subject: the date bound no longer tolerates a null ~active_from~ ([[file:../decisions.org::#d90][D90]]).* The criterion asks that a chore row carrying a null ~active_from~ be counted as live, which is the null-tolerant direction §The liveness predicate specifies. It cannot be tested and the tolerance cannot be reached: Story 01's migration made the column ~NOT NULL~ with a constant epoch default, and SQLite enforces that on insert and update alike — a mutation deleting ~is_nil(active_from) or~ from all three bound sites leaves the whole 508-test suite green. Routes checked and rejected: ~INSERT OR REPLACE~ substitutes the column default rather than storing a null, ~INSERT OR IGNORE~ skips the row, and the only remaining route is ~PRAGMA writable_schema~ surgery on ~sqlite_master~ inside a sandboxed connection, a worse artifact than the gap it would close. Nothing about SC-5 was ever at risk — the bound is correct on every row that can exist — so this was never a PRD conflict and no Amendment is needed. Ruled: the tolerance is dropped from all three queries and the ~NOT NULL~ constraint stands as the single protection, with the constraint test rewritten as an explicit tripwire. Rejected — relaxing the column so the tolerance became real and the criterion literally testable: the epoch floor doubles as the backfill's "not yet backfilled" sentinel ([[file:../decisions.org::#d87][D87]]), so a nullable column would slip past that guard and trade a hypothetical hazard for a live one. Rejected — keeping the branch as documented defence in depth: a layer no row can reach cannot fail, only rot, and it left the repo carrying an impression of coverage it did not have. Story 02's AC-7 is ~[-]~ and its DoD items 1 and 2 are ~[X]~; nothing is carried to batch close.

Batch close — the standing on-device gate from the mvp PRD (mvp/§7), a manual human checklist per repo convention, never CI'd and never skipped: Fully Kiosk rendering on the tablet's own panel, the 5-chore no-scroll budget intact, and sleep/wake reconnect. Two items specific to this batch join it: an archived chore's card disappearing live on the tablet while it is watching, and a recurring extra reappearing uncompleted at the next morning's reveal on the device rather than in a test. The design's other verification-beyond-~mix precommit~ requirement joins the same checklist rather than becoming a story (human ruling [2026-09-04]): a *before/after per-kid balance comparison against a copy of the real database*, run before the deploy the gate covers, with every difference accounted for by routine bonuses actually earned or actually not earned, any downward move on a kid's total identified with the days that caused it and surfaced for a ruling, the comparison run against a copy and never the live database, and the figures recorded by kid id — never by name, since the repo is public.

** DONE [[file:stories/story-01-lifetime-columns.org][Story 01 — Chore lifetime columns: the ~add_chore_lifecycle~ migration and its backfill]]
:LOGBOOK:
- Note taken on [2026-09-04 Fri 21:22] \\
  Closed out with all five acceptance criteria and all four Definition of Done items ~[X]~; ~mix precommit~ green at =Result: 495 passed=, up from 489. Shipped: the ~add_chore_lifecycle~ migration, the three schema fields, ~create_chore/2~ → ~create_chore/3~, and the ~chore_fixture/3~ and ~admin/chore_live/form.ex~ call sites the story named. \\
  One design pivot, ruled by the human mid-story and landed here as the full three-step ([[file:../decisions.org::#d87][D87]] + §Migration and backfill + Advisory): the design's SQL backfill *cannot* satisfy its own acceptance criterion. ~DATE(inserted_at)~ is UTC, so an evening-created chore never completed was stamped a day late — shrinking that day's roster and fabricating a ~+R~, the exact hazard [[file:../decisions.org::#d80][D80]] exists to prevent — and the ~MIN~ rescues that only when a completion already exists on the earlier local day, so [[file:../decisions.org::#d80][D80]]'s "absorbs the UTC-versus-local skew" claim holds for completed chores only. Backfill is now per-row Elixir with a ~DateTime.shift_zone!~ conversion, guarded to rows still at the epoch floor. Running the real ~Ecto.Migrator~ then surfaced a second defect no unit test could reach: a migration runs with the application unstarted, so the conversion raised =:time_zone_not_found= until the backfill started ~:tzdata~ itself. \\
  A second pivot ([[file:../decisions.org::#d88][D88]] + §Testing + Advisory) settles the question Story 01 left open rather than deferring it: §Testing's blanket wall-clock ban now excepts a test whose subject is an edge that reads the clock, which brackets the action instead. Flagged for the human at gate if the ruling is unwelcome — the live alternative was an injectable clock at the web edge, which [[file:../decisions.org::#d86][D86]]'s recorded reasoning argues against. \\
  Two implementation notes that change no design text. Subagent audits found two genuine gaps the story's own tests had missed and both are now closed: ~priv/repo/seeds.exs~ bypassed the context and stamped the epoch floor on a fresh ~mix ecto.setup~ deploy, and the admin form's clock read plus the ~is_recurring~ column default were each passing without being pinned — mutating them left the suite green. Carry-forward for Story 02: ~chore_fixture/3~ defaults ~local_now~ to the epoch, so every existing fixture chore is live on every date and Story 02's date-bounded tests must opt into a real ~local_now~ or they will pass vacuously.
:END:
** DONE [[file:stories/story-02-date-bounded-roster.org][Story 02 — The date-bounded roster: a past routine-day is scored against the chores that were live that day]]
:LOGBOOK:
- Note taken on [2026-09-05 Sat 07:37] \\
  *Implemented and closed out, but deliberately not DONE: two Definition of Done items stay open pending a human ruling (below).* ~mix precommit~ green at =Result: 508 passed=, up from 495. Six of seven acceptance criteria ~[X]~, each proven discriminating rather than vacuous — a story-verifier subagent mutated the shipped bound and confirmed exactly the expected tests fail for each. Shipped: the liveness bound on ~routine_day_contribution/3~'s roster query, and ~routine_days_by_kid/1~'s per-~(kid_id, routine)~ chore-count subquery reshaped to per-~(kid_id, routine, local_date)~ via a new private ~live_chore_counts/1~. The grouped-query property is intact and now pinned absolutely rather than only for constancy: a whole-render read is two statements, one per leg, with the roster count nested as a subquery-in-subquery. \\
  One design pivot, landed here as the full three-step ([[file:../decisions.org::#d89][D89]] + §Which queries filter / §The date-bounded roster + Advisory): the design's site classification is wrong for one site. It listed ~routine_days_by_kid/1~'s main aggregate among the deliberately unfiltered, but that aggregate is where completed and required are *compared*, so it must compare like with like. Leaving it unfiltered makes the two derivations disagree on exactly the archive day, in an ordinary case — a chore finished in the morning and archived that afternoon costs the kid the ~+R~ they earned. Two sites became three, and the census fourteen became fifteen. \\
  *Follow-up [2026-09-05 Sat 08:17] — the open question below is ruled and the story is now fully closed: all seven acceptance criteria and all four Definition of Done items are marked, AC-7 as ~[-]~ superseded.* Landed as a second full three-step ([[file:../decisions.org::#d90][D90]] + §The liveness predicate / §The date-bounded roster + Advisory): the bound no longer tolerates a null ~active_from~. The tolerance could not be reached — the column is ~NOT NULL~ and deleting the clause from all three sites left the suite green — and the constraint is the stronger protection, since it makes the null impossible rather than survivable. The ~archived_on~ half keeps its ~is_nil~ check, which was never defensive: that column is nullable by design and nil means live. The safety argument [[file:../decisions.org::#d80][D80]] made is preserved as a tripwire rather than a branch — the test asserting the database rejects a null ~active_from~ now says in a comment that if it fails, the three bounds must regain their tolerance before the change ships. \\
  Two implementation notes that change no design text. The design's roster snippet in §The date-bounded roster showed the bound without its null tolerance, which §The liveness predicate one section earlier already specifies; the snippet now matches the shipped query — a corrected illustration, not a changed position. And close-out caught two weaknesses in this session's own test work that mutation testing exposed and assertion alone had not: a first attempt at the pointwise criterion added fixture days that changed no mutant outcome and was reverted for a real per-cell test, and the "single query" clause was unpinned until an absolute count assertion replaced pure constancy checks.
:END:
** DONE [[file:stories/story-03-archive-replaces-delete.org][Story 03 — Archive replaces delete: ~archive_chore/2~, today's surfaces, and the admin Archive button]]
:LOGBOOK:
- Note taken on [2026-09-05 Sat 10:45] \\
  Closed out with all nine acceptance criteria and all four Definition of Done items ~[X]~; ~mix precommit~ green at =Result: 521 passed=, up from 508. Shipped exactly what the design specified, with no pivot: ~archive_chore/2~ (stamps ~archived_on~ from the caller's local date, broadcasts ~:chores_changed~), ~delete_chore/1~ removed with its tests, ~is_nil(archived_on)~ added to all 8 today's-surfaces sites (~list_chores/2~, ~list_extras/2~, all four ~neighbor/2~ clauses, both ~next_position/2~ clauses), and the admin form's Delete button/handler became Archive with the design's exact confirm wording. Two independent ~story-verifier~ passes (one against Acceptance Criteria, one against Definition of Done) confirmed the implementation against the actual repo state rather than trusting test names. \\
  No design pivot — this is the first story in the batch to land without one. Two coverage gaps the first verifier pass flagged were closed inline during close-out rather than deferred, changing no design text: three of the four ~neighbor/2~ clauses initially had no dedicated archived-neighbor test (only the routine-bucket ~:down~ clause did) — now all four are independently exercised, both directions, both buckets; and AC-4's "a past earned day stays earned" case was proven only through Story 02's pre-existing ~stamp/2~-based test rather than through ~archive_chore/2~ itself — a new test now proves it through the shipped function. The FK-cascade safety-net test in ~test/bear_cub/chores/completion_test.exs~ was repointed from the removed ~delete_chore/1~ to ~Repo.delete!~ directly, since the design keeps the ~on_delete: :delete_all~ constraint as an untriggered safety net, not a deleted one.
:END:
** DONE [[file:stories/story-04-shows-in-select.org][Story 04 — The "Shows in" select: four options, the extras create path, and the reclassification lock]]
:LOGBOOK:
- Note taken on [2026-09-05 Sat 11:16] \\
  Closed out with all 8 acceptance criteria and all 4 Definition of Done items ~[X]~; ~mix precommit~ green at =Result: 540 passed=, up from 521. Two independent ~story-verifier~ passes (Acceptance Criteria, then Definition of Done) confirmed the implementation against the actual repo state. Shipped exactly what the design specified, with no pivot: a virtual ~shows_in~ field on ~Chore~ (~morning~/~evening~/~extra~/~extra_daily~) whose changeset derives ~routine~ and ~recurring?~ and forces ~recurring?: false~ whenever ~routine~ is non-nil; the admin select grew to four options and now shows on the Extras create path (hidden, as before, when Morning/Evening pin the bucket); and ~check_reclassification_lock/2~ refuses a ~routine~ change once any completion row exists (undone/failed included) while leaving the ~extra~ ↔ ~extra_daily~ toggle open, via a ~Repo.exists?~ check that runs only when the derived ~routine~ actually differs from the stored one. \\
  No design pivot — DESIGN §Screens/"the Shows in select", §Key Behaviors/Reclassification, and the ~*Changeset*~ testing line already matched the shipped behaviour verbatim (D83/D84/D85 were minted at story-writing time, before this story's implementation; see the batch LOGBOOK above). \\
  One implementation note that changes no design text: populating ~shows_in~'s display default (so the select preselects the stored bucket) must never touch the changeset ~create_chore/3~/~update_chore/2~ actually persist, or the virtual field's derived value leaks onto the returned struct and breaks the ~==~ comparisons several existing tests make between a freshly-created chore and one reloaded via a plain query (reloaded structs always carry ~shows_in: nil~, since virtual fields are never read back from the database). Fixed by confining the default-population (~Chore.with_default_shows_in/1~) to ~Chores.change_chore/2~ — the LiveView-facing, display-only path — and keeping ~Chore.changeset/2~ itself free of it. Worth remembering for any future virtual field on this schema: a display default belongs in the form-facing wrapper, never in the changeset the create/update path calls directly. Also noted by the verifier as informational, not a defect: ~Chore.changeset/2~ now carries a hard ~Repo~ dependency for the reclassification lock's existence check, which is exactly what the design's Technical Notes call for ("changeset-level... backed by a cheap existence check"). \\
  Also flagged by the verifier, not acted on (explicitly implementation latitude per the story's Technical Notes): the locked select isn't visually disabled ahead of submission — the parent learns of the lock only after submitting and hitting the changeset error; and the LiveView layer's preselection round trip is exercised only for ~extra_daily~, with ~morning~/~evening~/~extra~ covered at the changeset-unit layer instead.
:END:
** DONE [[file:stories/story-05-recurring-extras.org][Story 05 — Recurring extras: ~list_extras/2~ and the daily reveal]]
:LOGBOOK:
- Note taken on [2026-09-05 Sat 11:42] \\
  Closed out with all 10 acceptance criteria and all 4 Definition of Done items ~[X]~; ~mix precommit~ green at =Result: 548 passed=, up from 540. Two independent ~story-verifier~ passes (Acceptance Criteria, then Definition of Done) confirmed the implementation against the actual repo state. Shipped exactly what the design specified, with no pivot: ~list_extras/2~ split into two ~where~ clauses, the second being ~c.recurring? or c.id not in subquery(retired_ids)~ — the design's snippet quoted verbatim, landed verbatim. Nine lines of implementation across one file; the three surfaces that read the function (kiosk, admin Today, admin chore index) inherited the change untouched. \\
  No design pivot — DESIGN §Recurring extras and §Kiosk — extras already described the shipped behaviour exactly. The design's falsifiable claim that *points need no changes at all* was tested rather than assumed and held: ~lib/bear_cub/points.ex~, ~extra_contribution/2~, ~earnings/2~ and ~extras_by_kid/1~ are all untouched in the diff, and the two-days-equals-2× criterion passes against them unmodified. The ~fragment~ hazard the design flags did not materialise — ~extras_by_kid/1~'s raw fragment references only ~undone_at~/~failed_at~/~chore_id~, never the recurrence flag, so nothing needed to spell ~is_recurring~. \\
  Two implementation notes that change no design text. First, two of the ten criteria are *absence* properties on files this story never modified — no repeat marker on the kiosk card, and the admin Today view's on-behalf toggle still reaching a recurring extra on a day the reveal never happened. Both were verified by ~grep -rn "recurring" lib/bear_cub_web/~ returning zero matches and by an empty ~git diff~ on ~kiosk_live.ex~ and ~admin/today_live.ex~, not by new tests; a later reader looking for tests behind those two criteria will not find any, and that is correct rather than a gap. Second, the test-side half of Story 04's virtual-field lesson surfaced here: a test comparing ~list_extras/2~ output against a fixture struct must compare ids, not whole structs, whenever the fixture was created through ~shows_in~ — the freshly-created chore carries the virtual value while every reloaded row carries ~shows_in: nil~, so ~==~ on the structs fails for a reason that has nothing to do with the behaviour under test. Story 04 recorded the production-side half (keep ~with_default_shows_in/1~ out of the persisting changeset); together they are one lesson candidate for the gate's promotion review. \\
  One process note: the new kiosk test was checked for discrimination rather than assumed to be meaningful — reverting the ~list_extras/2~ change and re-running it confirmed it fails without the fix, so it is not vacuous.
:END:

* Deferred

# Out-of-scope discoveries land here during implementation instead of being fixed inline. Promoted to v2 candidates at initiative close.

From Story 01:

- The backfill's ~local_date_of/2~ raises a bare ~FunctionClauseError~ naming ~Calendar.ISO.parse_utc_datetime/2~ on an ~inserted_at~ it cannot parse, without naming the offending chore. Unreachable with Ecto-written data (~chores.inserted_at~ is ~TEXT NOT NULL~ and ~strftime~ normalizes every spelling SQLite accepts), and failing loudly is the right direction, but a data-repair scenario would get an unhelpful abort.
- The epoch floor does double duty as the "not yet backfilled" sentinel, and ~chore_fixture/3~'s default ~local_now~ is that same epoch — so a pre-lifecycle row and a row deliberately created with an epoch datetime are indistinguishable to the backfill's guard. Correct today because no production caller passes one.
- A ~%Chore{}~ inserted without ~active_from~ silently takes the epoch floor rather than failing: Ecto omits the nil field and SQLite supplies the default, which is exactly how the ~seeds.exs~ gap went unnoticed. Nothing at the schema level stands behind the field.
- The migration depends on application code and on a named timezone backend — it calls ~BearCub.LocalTime.timezone()~ and ~Application.ensure_all_started(:tzdata)~. Migrations outlive the code around them, so renaming that module, changing the ~:bear_cub, :timezone~ config key, or swapping the ~Tzdata.TimeZoneDatabase~ backend would break a migration that has already run everywhere but must still replay on a rebuilt box.
- Replaying ~down~ then ~up~ inside a single BEAM process raises =duplicate column name: is_recurring=, because the migration file is loaded twice in the same VM and ~up/0~ runs twice. Separate ~mix ecto.rollback~ and ~mix ecto.migrate~ invocations are unaffected, as is a from-scratch create-and-migrate; this bites only someone who later writes a round-trip migration test in one process.
- ~priv/repo/seeds.exs~ still sets ~position~ by hand rather than going through ~Chores.create_chore/3~, which would compute it. Story 01 stamped ~active_from~ there the same way rather than reroute the script; the bypass predates this batch.
- Pre-existing, adjacent: ~mix precommit~ runs ~format~, which rewrites files, rather than ~format --check-formatted~, so the declared verify command cannot fail on formatting.

From Story 02:

- The ~Chore~ query-site census moved from fourteen to fifteen: ~live_chore_counts/1~'s day-enumeration subquery introduces a ~Chore~ join the design's inventory did not account for. §Which queries filter now states fifteen, and Story 03's excerpt and scope note were refreshed to match — recorded here because any future story scoped as "N of the fourteen sites" is reading a stale census.
- The before/after per-kid balance comparison against a copy of the real database has no artifact in the tree, and the test suite structurally cannot produce one. Already a batch-close gate item by the [2026-09-04] ruling above; noted again because Story 02 is the story whose rule change makes the shift happen, and the comparison is the only thing standing between that shift and the fridge.
- The grouped derivation is observable only through per-kid totals: ~routine_days_by_kid/1~ is private and ~earnings_by_kid/1~ aggregates, so any perturbation of its per-cell values whose per-kid sum is zero at every cutoff date is invisible to any test. Two such classes were demonstrated by mutation and both survive the suite. Plausible implementation bugs are all caught; closing the residual would mean exposing the grouped query's rows purely for a test, which [[file:../decisions.org::#d86][D86]]'s recorded reasoning argues against. Stated in a comment above the per-cell test rather than left for someone to rediscover.
