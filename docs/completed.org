#+title: Completed
#+startup: showall
#+TAGS: oneshot(o) hand(h) promote(p)

Completed backlog items, moved here from docs/backlog.org when marked DONE (per the rule recorded in its preamble, [2026-07-24 Fri]). Entries keep their full capture/triage/done trail; new arrivals append at the end.
* DONE [#A] Gamification: Points system :promote:
:PROPERTIES:
:CREATED:  [2026-07-13 Mon 23:27]
:END:
Need a way to reward the kids for completing their tasks.  A couple thoughts:
- Completing an entire routine (morning or evening) is worth maybe 5 points
- Ad-hoc chores added by parents are worth 5 points by default but parent can adjust
- Need a points total readout for each child on the main page
- Parents need to be able to "fail" a chore upon inspection (i.e. the child tapped it done but did not actually do it)
  - This /deducts/ the points that would have been otherwise added, and the chore completion is undone so that the child can actually do it and mark it off and earn the points back (netting zero as a consequence for the lie).

Triage [2026-07-14 Tue]: promote — initiative-scale. Two things for the brainstorm:
- Consider claiming the collapse-delay and big-icon items below as batch members — same completion-reward theme.
- Settle the ledger fork early: the fail-on-inspection flow interacts with D10 (undo sets ~undone_at~, completion rows are never deleted, day state is derived). A points total derived from completions needs the failed completion to leave a trace that /counts against/ — that's a D27-style extend-vs-new-table fork and belongs in the PRD, not implementation.

Claimed [2026-07-15 Wed]: into initiative docs/2026-07-15-gamification-points/ (Gamification: Points).

Done [2026-07-24 Fri]: initiative shipped — all 10 stories closed, Batch 1 gate closed [2026-07-18]. Marked at backlog review.
* DONE [#A] Rewards / redemption system :promote:
:PROPERTIES:
:CREATED:  [2026-07-18 Sat 23:35]
:END:
Out of scope for the gamification-points initiative (DESIGN.org "Out of Scope for This Design"): no store, spending flow, or streaks were built. The points total is deliberately shaped as ~max(0, completions_earnings + other_signed_inputs)~, with ~other_signed_inputs~ empty today (D42) — so a future redemption spend (a negative, non-completion signed input) slots into the existing summation without reshaping the earnings derivation. The seam is real but unexercised; building redemption is future initiative-scale work.

Swept [2026-07-18 Sat 23:35] from docs/2026-07-15-gamification-points/DESIGN.org's Out of Scope section, per its initiative-close sweep instruction.

Claimed [2026-07-25 Sat]: into initiative docs/2026-07-25-rewards-redemption/ (Rewards / Redemption).

Done [2026-07-25 Sat]: initiative shipped — 9 stories closed (01 points foundation, 02 aggregate rewrite, 03 rewards domain, 04 admin catalog, 05 kiosk shop, 06 approval queue, 07 history view, 08 request lifecycle, 09 admin wording/balance — the last two inserted by a pre-gate localhost QA round that minted D75–D77), Batch 1 gate closed [2026-07-25 Sat] with a PASSED on-device checklist on the deployed kiosk hardware. Marked at initiative close.
* DONE Remove ~BearCub.MigrationTestRepo~ and the remaining migration-execution test :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-18 Sat 23:35]
:END:
Retro ruling ([2026-07-18]): migration mechanics are framework contract, not app behavior — the same judgment Story 01 already applied when it dropped its own throwaway ~MigrationTestRepo~-based backfill test (docs/2026-07-15-gamification-points/PLAN.org, Story 01 LOGBOOK). Remove the scratch ~BearCub.MigrationTestRepo~ pool (~test/support/migration_test_repo.ex~) and the one remaining migration-execution test that still exercises it, ~test/bear_cub/repo/migrations/relax_chores_routine_nullability_test.exs~ (its AC3 coverage — completion history surviving the ~chores.routine~ nullability rebuild — should be re-pointed to the standard ~mix test~ migration run, per Story 01's precedent).

Removing this retires one of the two documented triggers on the Exqlite teardown-noise entry in docs/backlog.org (noted there).

Done [2026-07-24 Fri]: both files removed (~test/support/migration_test_repo.ex~, ~test/bear_cub/repo/migrations/relax_chores_routine_nullability_test.exs~); no other references existed. AC3 coverage now rides the standard ~mix test~ migration run per Story 01's precedent. ~mix precommit~ clean, 282 passed.
* DONE Inline-markup normalization sweep across the org corpus :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-18 Sat 23:35]
:END:
Retro ruling ([2026-07-18]): enforce the org-conventions ~code~/=verbatim= distinction (~code~ for anything a language would parse — identifiers, module/function names, expressions, shell commands, file paths; =verbatim= only for literal non-code strings quoted exactly — stored values, config keys, log output) across the entire org corpus: all three initiative chains (mvp, extras, gamification-points — PRD.org/DESIGN.org/stories/ in each), ~docs/learnings.org~, ~docs/design-language.org~, and ~docs/backlog.org~ itself.

Repair-class neutral pass: no D-entries, no Advisories — the convention already stands and isn't changing, the corpus is only being brought into line with it. Land as one dedicated doc-only commit.

Done [2026-07-24 Fri]: ~1,700 span conversions across the scoped corpus (PRD/DESIGN/stories in all three chains, learnings, design-language, backlog); also repaired ~25 lines of author-broken org markup found along the way (early-closing = spans like ~value=""~, doubled ~~ around sigil-bearing code, pluralized =EXDATE=s). Approximation tildes (~15 minutes), ICS property literals (=FREQ=MONTHLY=), and quoted stored values stayed verbatim. The mvp PRD's 4 markup-only conversions were applied under a human-authorized one-time prd-lock bypass. PLAN.org files and docs/decisions.org were outside the item's stated scope and untouched.
* DONE I want the vertical height of a chore to shrink a little when it is completed :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-17 Fri 06:29]
:END:
Triage [2026-07-24 Fri]: oneshot. Kids' view visuals — docs/design-language.org read is binding; on-device gate applies (mind the 5-chore no-scroll check — shrinking completed rows should help it, never hurt it).

Done [2026-07-24 Fri]: done rows (chores and extras, via the shared ~chore_row~) render at ~h-20~ against the pending ~h-24~, with the row grids switched from fixed ~auto-rows-[6rem]~ tracks to content-sized ~auto-rows-min~ so the per-row height takes effect; ~transition-all~ animates the shrink. Only helps the no-scroll budget (rows only ever get shorter). Ruling recorded in docs/design-language.org. On-device look remains for the next gate's manual checklist.
* DONE [#B] need a darker screen for night mode (after 23:00) :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-13 Mon 23:28]
:END:
Should just be a dark screen with a evening-colored large moon in the center, and nothing else.

Triage [2026-07-14 Tue]: oneshot. Kids' view visuals — docs/design-language.org read is binding; on-device gate applies. Independent of gamification.

Done [2026-07-24 Fri]: the night window now renders a single ~#night-screen~ (dark ~bg-stone-950~, one large ~hero-moon-solid~ colored ~var(--routine-evening)~) replacing the per-kid columns entirely — D56 minted (supersedes D32's Good Night message, scopes D43's always-visible badge to waking windows); design-language ruling recorded; ~Messages.good_night/0~ retired. TDD at the LiveView layer, ~mix precommit~ clean. On-device check (dark render on the tablet, no light bleed) still owed at the next gate.
* DONE [#B] Need a short delay between completing the last routine task and collapsing the routine list :oneshot:
:PROPERTIES:
:ID:       90b89296-d77e-468b-9ad1-c8f4261cee70
:CREATED:  [2026-07-12 Sun 23:04]
:END:

So the child can get the quick "I completed that task" visual reward before seeing that they've completed the entire routine.

Triage [2026-07-14 Tue]: oneshot; candidate for absorption into the gamification initiative if promoted — hold until that brainstorm rules.

Claimed [2026-07-15 Wed]: into initiative docs/2026-07-15-gamification-points/ (Gamification: Points) as a batch member (collapse-delay, §6.3).

Done [2026-07-24 Fri]: shipped as gamification-points Story 07 (400ms ~@collapse_delay_ms~). Marked at backlog review. A deferred discovery from that story lives in docs/backlog.org as the double-schedule entry.
* DONE [#B] extra-large completed icon for routines in the top bar where their names are :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-12 Sun 23:01]
:END:

- Big sun icon for completing morning routine
- Big moon icon for completing evening routine
- Maybe a green checkmark in the upper-right corner of both

This would then make the routine's persistent header bar obsolete.  The kid can tap the big icon to restore the list instead.

Triage [2026-07-14 Tue]: oneshot; candidate for absorption into the gamification initiative if promoted. Retiring the persistent header bar is a design-language ruling, not just implementation — record it in docs/design-language.org when done.

Claimed [2026-07-15 Wed]: into initiative docs/2026-07-15-gamification-points/ (Gamification: Points) as a batch member (big-icon, §6.3).

Done [2026-07-24 Fri]: shipped as gamification-points Story 08 (completion icon + tap-toggle, header bar retired; refined by D51/D52). Design-language recordings landed. Marked at backlog review.
* DONE Roster drift (D72) — ~chores.inserted_at~-bounded roster handles additions but not deletions
:PROPERTIES:
:CREATED:  [2026-07-25 Sat 22:40]
:END:
~routine_day_contribution/3~ (now composed into ~Chores.routine_days_by_kid/1~, the rewards/redemption Story 02 grouped-query rewrite) evaluates historical routine-days against the kid's *current* chore roster: adding a morning chore today retroactively makes every past morning-day incomplete, wiping historical ~+R~; deleting one can retroactively complete past days and grant points never earned. Pinned as-is by Story 01 and preserved unchanged by Story 02 (D72) — this ships today and predates the redemption work, which only raises the stakes (a roster edit can now move a balance after a spend, potentially into debt with no fail involved). Needs its own ruling: a ~chores.inserted_at~-bounded roster (scoring a historical day against the roster live *on that day*, not today's) would handle additions correctly but not deletions, since a deleted chore leaves no row left to bound against. Not designed for in the rewards/redemption initiative.

Swept [2026-07-25 Sat 22:40] from docs/2026-07-25-rewards-redemption/PLAN.org's Deferred section (Story 01/02 discovery, D72).

Claimed [2026-09-04 Fri]: into initiative docs/2026-09-04-chore-lifecycle/ (Chore lifecycle), whose §Overview names closing this entry as its third change. The objection recorded above — that a date-bounded roster cannot handle deletions, since a deleted chore leaves no row to bound against — was removed rather than solved: [[file:decisions.org::#d78][D78]] replaces deletion with archive, so every chore keeps its row and its one lifetime interval ([[file:decisions.org::#d79][D79]]).

Done [2026-09-05 Sat 12:12]: closed in both directions by [[file:decisions.org::#d81][D81]] — the roster bound landed on three query sites ([[file:decisions.org::#d89][D89]] corrected the classification from two), shipped as chore-lifecycle Story 02, with [[file:decisions.org::#d90][D90]] settling the bound's final form. Moved here at the Batch 1 gate per the initiative DESIGN's §Overview instruction. *Caveat carried forward:* the gate recorded the before/after per-kid balance comparison against a copy of the real database as a documented deferral, so the one-time balance shift this fix causes is still unverified against production data and is owed before the deploy.
* DONE "Good standing" indicator
:PROPERTIES:
:CREATED:  [2026-09-01 Tue 17:07]
:END:
Need a highly visible indicator showing that a child has both
1. Completed the previous evening's routine
2. Completed this morning's routine

This will be used to allow or restrict certain activities like screen time or other treats.  It should be shown upon completion of the morning routine, if the conditions are met (previous evening's routine was also completed).  It needs to be large and visible from a distance on the kiosk.

Picked up [2026-09-05 Fri] directly as initiative docs/2026-09-05-good-standing/ (Good standing) — no separate claim step recorded in this file.

Done [2026-09-06 Sat]: initiative Batch 1 gate closed ([[file:decisions.org::#d91][D91]]–[[file:decisions.org::#d97][D97]] in ~docs/decisions.org~), all 5 stories shipped — ~Chores.routine_day_status/3~ and ~Chores.standing/2~ derive the two-half rule ([[file:decisions.org::#d91][D91]], [[file:decisions.org::#d92][D92]], [[file:decisions.org::#d94][D94]]); ~Admin.TodayLive~ shows the verdict and which half broke it ([[file:decisions.org::#d96][D96]]); the kiosk shows a ~bg-success~ band of ~hero-star-solid~ glyphs (three at ship, grown to thirteen by [[file:decisions.org::#d98][D98]]/[[file:decisions.org::#d99][D99]] hands-on refinement) plus a column ring, morning window only ([[file:decisions.org::#d95][D95]], [[file:decisions.org::#d97][D97]]). ~mix precommit~ clean, 577 passed. *Caveat carried forward, tracked as its own backlog item rather than repeated here:* the on-device gate (Story 05 AC8 — Fully Kiosk rendering, band/ring legibility, five-chore no-scroll fit, 17:00 disappearance, sleep/wake reconnect) is a documented deferral pending deploy.
* DONE [#B] parent notifications when kids complete chores :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-24 Fri 15:04]
:END:
Triage [2026-07-24 Fri]: hold, deliberately untagged — needs a delivery-channel ruling before sizing: admin is phone-first over Tailscale, so the candidates (ntfy/Pushover-style push, email, web push) differ in reach off the tailnet, and the choice touches NixOS deployment, not just app code. Granularity also needs a ruling (per chore vs per routine-complete vs daily digest; quiet hours). Brainstorm first; promote if it grows a settings surface, otherwise it may collapse to a oneshot (a Req POST on ~:chores_changed~).

Ruled [2026-07-24 Fri]: ntfy push, per-chore granularity for now — customization (batching, quiet hours, per-kid settings) possible later, not in scope. Collapses to oneshot, #B: a Req POST per completion event, topic/server via ~runtime.exs~ env config. Treat the topic URL like the ICS URLs — never in git, never logged. Self-hosted ntfy vs ntfy.sh is an implementation-time deployment choice (NixOS module side).

Done [2026-09-06 Sun 13:05]: shipped as a oneshot, scope widened in the session's brainstorm beyond the per-chore ruling above: (1) a per-chore *Notify when complete* flag (~chores.notify_on_complete~, default off, a checkbox on the admin chore form) gates the per-chore push rather than every completion pushing; (2) a routine-complete push fires regardless of flags when a completion finishes the kid's routine for the day (~Chores.routine_day_status/3~, D94); (3) a kiosk reward ask pushes too (~Rewards.request_redemption/3~); (4) admin on-behalf completions push like kiosk ones, chosen to keep the rule simple; undo, fail, and parent direct redeems stay silent. Deployment choice landed on ntfy.sh with a long random topic as the secret (~BEAR_CUB_NTFY_URL~, full topic URL, unset = off). A first cut delivered it through a hand-placed ~environmentFile~; that was the box's first hand-placed secret, against the zero-hand-placed-secrets principle the module already states (D9, design §7), so it was replaced the same session: the NixOS module generates the topic into the state directory on first boot exactly like ~SECRET_KEY_BASE~ (~services.bear-cub.ntfyServer~, default ntfy.sh, null = off), and a new ~/admin/notifications~ page, linked from the Chores header, shows the topic for subscribing — the only place it is ever displayed. ~BearCub.Notifications~ POSTs from a ~Task.Supervisor~ child so a tap never waits on the network; failures log message and status only. Web push was weighed and set aside as its own backlog entry. The settings-surface concern in the triage above was accepted as bounded (one boolean on an existing form) rather than promoted.

* DONE [#B] Early bird bonus! :oneshot:
:PROPERTIES:
:CREATED:  [2026-09-06 Sun 09:34]
:END:

When a kid completes their morning routing before a certain time (e.g. 7:45 AM) they get an Early Bird bonus, perhaps indicated by a bird emoji/icon next to the sun icon with another configurable bonus, e.g. +2.

Triage [2026-09-06 Sun]: oneshot, #B — smaller than it looks: ~completions.completed_at~ already exists, so "last morning chore tapped before 7:45" is a plain query, no new column. Config-only threshold/bonus, mirroring ~:routine_bonus~/~:routine_windows~ (~config/runtime.exs~, env-overridable) rather than an admin form. One ruling to settle before coding, not after: where the bonus composes into ~Chores.earnings/2~. D42's =other_signed_inputs= seam is already occupied by ~Rewards.spend_total~ (see ~lib/bear_cub/points.ex~), so this is a third term, not that seam — and whichever term it becomes must also land in ~earnings_by_kid/1~'s grouped rewrite, not just the per-kid path, or the two derivations disagree the way [[file:decisions.org::#d89][D89]] found them disagree once already. Kids' view visual (bird icon beside the sun icon) — ~docs/design-language.org~ read is binding; on-device gate applies.

Done [2026-09-06 Sun 14:13]: shipped as a oneshot on the composition ruling above — a term *inside* the routine-day contribution (~R + E~ for an earned, unfailed morning whose last live tap landed strictly before the cutoff), landed in both ~routine_day_contribution/3~ and the grouped ~routine_days_by_kid/1~ through one shared ~contribution/4~, so the pointwise-agreement tests cover it (D100). Config-only as triaged: ~BEAR_CUB_EARLY_BIRD_CUTOFF~ (=07:45=, must fall inside the morning window, boot-checked) and ~BEAR_CUB_EARLY_BIRD_BONUS~ (=2=, =0= off). Kiosk: a =🐦= beside the sun with its own ~+E~ badge, forfeited whole on a fail (D101, design-language ruling). Set aside into its own backlog entry on the human's call: config-drift snapshotting, so a seasonal cutoff change does not rewrite points history. On-device gate still owed for the bird's size and badge spacing.
