#+title: PLAN — Bear Cub: Extras (one-off to-dos)
#+date: 2026-07-12
:LOGBOOK:
- Note taken on [2026-07-12 Sun 16:27] \\
  Initiative closed — extraction pass complete. Lessons a (pinned-nil Ecto query comparisons raise, don't match) and b (D27's nullable-discriminator pattern confirmed across all six stories) promoted to docs/learnings.org. All 7 Deferred items swept to docs/backlog.org. CLAUDE.md's Active initiative cleared. Nothing else in this chain changes after this note.
:END:

# Mutability: execution state. Story status and implementation observations are recorded here as LOGBOOK entries per the workflow-kit org-conventions skill. By phase close, this file should read as an accurate record of what was actually built.

Design: [[file:DESIGN.org]]

* Batch 1 — Extras
:LOGBOOK:
- Note taken on [2026-07-12 Sun 16:17] \\
  Gate record. All 6 stories (01-06) done. Checks run: ~mix precommit~ (compile --warnings-as-errors, format --check-formatted, deps.unlock --unused, test) — clean, 216 passed, per the personal-mvp weight class and CLAUDE.md's naming of ~mix precommit~ as the verification gate. DESIGN.org's Key Behaviors and Screens/Interfaces sections diffed against the implementation across all six stories' files (~kiosk_live.ex~, ~admin/today_live.ex~, ~admin/chore_live/index.ex~, ~admin/chore_live/form.ex~, ~chores.ex~, ~messages.ex~, ~routines.ex~, ~chores/chore.ex~) — no divergences found. PLAN reconciliation — all six stories' LOGBOOK entries independently re-verified true against current code and the 216-test suite total. On-device QA (Fully Kiosk WebView + phone/Tailscale admin, per CLAUDE.md's "on-device QA gates each batch" invariant: Good Night lockdown, morning band-collapse + extras reveal, band re-expand/undo, admin Extras CRUD/reclassify and Today on-behalf correction, reset controls confirmed gone, 5-chore no-scroll) — human-confirmed passed. Advisories review: all three existing entries (D38 Good Night message, and the two repair-class next_position/2 / move_chore/2 nil-handling notes) withdrawn — the batch is complete, no unimplemented story excerpts remain that could reintroduce any of the three superseded positions, and DESIGN.org's body already states the corrected behavior in each case. Two of the three withdrawn advisories (the Ecto ~is_nil/1~-vs-~==~ nil-comparison bugs in ~next_position/2~ and ~move_chore/2~) are flagged as lesson candidates — a generalizable Ecto/Elixir gotcha (a pinned ~nil~ value in a query comparison raises ~ArgumentError~ rather than silently matching) — for possible promotion to =docs/learnings.org=, not yet promoted. Verdict: PASS. Advisories withdrawn: 3. Advisories promoted: 0 (2 lesson candidates noted for later). Batch 1 closed.
:END:

# Stories nest below as org TODOs in implementation order; status updates get LOGBOOK entries under the story heading. At phase-close the gate record for this batch lands here as a LOGBOOK entry on the batch heading — its presence marks the batch closed. At most one batch is open (has no gate record) at a time.

# Order rationale: Story 01 is the schema foundation (nullable routine) every other story rides on. 02 adds the pure context + Messages. 03 makes extras creatable/manageable in admin. 04 cleans the kiosk (retire flip/dimmed, add Good Night) as the base for 05, which delivers the morning reveal — the feature payoff. 06 completes the correction path and retires day-reset. The app runs after every story.

** DONE [[file:stories/story-01-nullable-routine.org][Story 01 — Nullable routine: migration + changeset]]
:LOGBOOK:
- Note taken on [2026-07-12 Sun 11:43] \\
  Closed out: all 4 ACs and all 4 DoD items =[X]=. =Chore.changeset/2= (=lib/bear_cub/chores/chore.ex=) drops =:routine= from ~validate_required/2~ while keeping ~validate_inclusion(:routine, @routines)~ (=nil= skips the check; ="morning"=/="evening"= pass; anything else non-nil is rejected). New migration =priv/repo/migrations/20260712180527_relax_chores_routine_nullability.exs= drops =NOT NULL= on =chores.routine= via a create-new/copy-rows/drop-old/rename table rebuild (SQLite has no =ALTER COLUMN=), preserving the =completions= → =chores= FK and the =[:kid_id, :routine, :position]= index. =list_chores/2= needed no change — its existing =routine in ~w(morning evening)~= guard already excludes =nil= rows. New tests: nil-routine create/update/list-filtering in =chores_test.exs=, plus a dedicated migration test (=test/bear_cub/repo/migrations/relax_chores_routine_nullability_test.exs= + a throwaway =BearCub.MigrationTestRepo=) that runs the real migration via =Ecto.Migrator= against a scratch DB and proves completion history and FK enforcement survive the rebuild. =mix precommit= clean, 189 passed. No DESIGN.org pivot: the migration fulfills §Data Model's stated approach (nullable =routine=, table-rebuild path, FK/index preservation) verbatim — additive confirmation only, not a design change. Two Deferred discoveries carried below.
:END:
** DONE [[file:stories/story-02-extras-context.org][Story 02 — Extras queries, reclassify position, and Messages]]
:LOGBOOK:
- Note taken on [2026-07-12 Sun 12:09] \\
  Closed out: all 6 ACs and all 4 DoD items =[X]=. Added ~BearCub.Chores.list_extras/2~ (kid + local_date, matching the design's =list_extras(kid)= shorthand per the CLAUDE.md local-time invariant): outstanding + done-today =nil=-routine chores via a =retired_ids= anti-join subquery on =is_nil(undone_at) and local_date < local_date=, ordered by position. ~update_chore/2~ now detects a =routine= change via ~Ecto.Changeset.fetch_change/2~ and re-appends =position= to =max+1= of the target bucket (D35); =position= stays out of =cast=. New ~BearCub.Messages~ module with =morning_complete/0=, =evening_complete/0=, =good_night/0= (D37, D38). Found and fixed a pre-existing bug while implementing reclassify: ~next_position/2='s =nil= clause hardcoded =0= instead of querying =max(position)=, so multiple extras (or a second reclassify into the bucket) would collide at position 0 — now queries =is_nil(c.routine)= like every other bucket. DESIGN.org body updated (Data Model) with a paired Advisory (repair-class, no D-entry — see DESIGN.org "§Data Model next_position/2's nil clause previously hardcoded 0, not queried"). =mix precommit= clean, 200 passed. Two Deferred discoveries carried below.
:END:
** DONE [[file:stories/story-03-admin-extras-crud.org][Story 03 — Admin: create, reclassify, and list extras]]
:LOGBOOK:
- Note taken on [2026-07-12 Sun 12:55] \\
  Closed out: all 6 ACs and all 4 DoD items =[X]=. =ChoreLive.Index= (=lib/bear_cub_web/live/admin/chore_live/index.ex=) gained a third Extras section (=id="routine-extras"=/=id="chores-extras"=) driven by =Chores.list_extras/2= (retired rows already filtered by Story 02), with its own "+ Add" link carrying no =routine= param; the chore =<li>= row was factored into a shared =chore_row/1= function component across all three sections. =ChoreLive.Form= (=lib/bear_cub_web/live/admin/chore_live/form.ex=): =apply_action(:new)='s fallback changed from ="morning"= to =nil= (D36's "no =extra= token" mechanism); the =routine= select is hidden on Create (=:if={@live_action == :edit}=) and relabeled "Shows in" on Edit with the three-option mapping; =save_chore(:new, params)= now force-sets =params["routine"]= from the URL-derived =socket.assigns.chore.routine= rather than trusting the submitted form, since the field is hidden and would otherwise be forgeable via a raw event payload. Found and fixed a real bug flagged by the story's own Technical Notes "confirm" prompt: =Chores.neighbor/2= (used by =move_chore/2=) compared =c.routine == ^chore.routine=, which Ecto forbids/raises on when pinned to =nil= — added =is_nil(c.routine)=-specific clauses for the extras bucket, mirroring Story 02's =next_position/2= fix. DESIGN.org body updated (Data Model) with a paired Advisory (repair-class, no D-entry — see DESIGN.org "§Data Model =move_chore/2='s neighbor lookup previously assumed no code change was needed for the extras bucket"). Two independent =story-verifier= passes (AC, then DoD) closed two minor test-coverage gaps (evening-link creation, extra→evening reclassify) and caught three stale LOGBOOK line-number citations from a mid-file test insertion, corrected in place. =mix precommit= clean, 209 passed. One Deferred discovery carried below.
:END:
** DONE [[file:stories/story-04-kiosk-cleanup-goodnight.org][Story 04 — Kiosk: retire flip/dimmed, add Good Night mode]]
:LOGBOOK:
- Note taken on [2026-07-12 Sun 13:17] \\
  Closed out: all 6 ACs and all 4 DoD items =[X]=. =KioskLive= (=lib/bear_cub_web/live/kiosk_live.ex=): removed the =flipped= assign, ~handle_event("flip", ...)~, and the =#routine-flip= button; =load/2= now derives chores from the auto-selected routine only. Replaced the =dimmed?= assign and its =opacity-40= paths with =night? = state == :upcoming=: when true, the chores =1fr= region renders a =#goodnight-<kid.id>= band (=Messages.good_night()=, no =phx-click=, no rows) instead of the chore =<ul>=; the events strip is unaffected (only the chores region is state 5's concern). The boundary timer (=:boundary= handler) needed no new logic — it already reloads on every window edge, and =night?= just falls out of =Routines.current(now)='s existing ={:upcoming, _}= branch. New "Good Night mode" describe block in =kiosk_live_test.exs= tests both boundary transitions (evening-close with the evening routine complete /and/ incomplete; night→05:00 morning reopen) by reconfiguring =Application.put_env(:bear_cub, :routine_windows, ...)= relative to the real =LocalTime.now()= between a mount and a =:boundary= send — this manufactures a deterministic boundary crossing without mocking the clock itself (CLAUDE.md invariant), matching the story's "driven by local-datetime args" testing intent. No DESIGN.org pivot: the implementation followed the story's D30/D32/D38 excerpts verbatim — additive confirmation only. =mix precommit= clean, 210 passed. One Deferred discovery carried below.
:END:
** DONE [[file:stories/story-05-kiosk-reveal.org][Story 05 — Kiosk: collapse band, reveal gating, extras reveal]]
:LOGBOOK:
- Note taken on [2026-07-12 Sun 15:44] \\
  Closed out: all 7 ACs and all 4 DoD items =[X]=. =KioskLive='s =load/2= (=lib/bear_cub_web/live/kiosk_live.ex=) gained a =build_column/6= helper that derives one of three render states per kid column — =:night=, =:rows=, =:band= — from =Routines.current(now)=, the completions map, and a new ephemeral =:expanded= =MapSet= assign. This collapses the design's five conceptual states into three render paths: spec states 1 (normal) and 4 (manually re-expanded) render identically (rows, tap-to-undo) exactly as DESIGN.org already describes state 4, so they share one =:rows= branch rather than needing a fourth distinct state value. =reveal?= (=not night? and complete?=, where =complete? = chores != [] and Enum.all?(&done?/1)=) gates the =:band= state on both the active window and completion (D33) — structurally, since =chores= is only ever fetched for the currently-=auto= routine, a completed-but-not-shown routine can never leak into the reveal. Extras are computed only =if state == :band and auto == :morning=, matching the morning-only asymmetry. A new ~handle_event("toggle-band", ...)~ toggles =kid_id= into =:expanded=; =load/2='s =still_expanded= intersects it against kids whose =reveal?= is still true on each reload, clearing the entry the moment a routine's completion state flips (D34) — mirroring, not reusing, =Admin.TodayLive='s comparable =expanded= pattern. Extra rows reuse a new shared =chore_row/1= function component (also used for regular chore rows), since extras are chores per D27. Two pre-existing tests needed adaptation, not correction: the "tap to complete and undo" describe block's single-chore setup and =Admin.TodayLiveTest='s admin→kiosk sync test both completed a kid's *only* active-routine chore, which now correctly auto-collapses the kiosk into the reveal band (new behavior, not a regression) — fixed by adding a companion chore in the former and asserting the band in the latter. Two independent =story-verifier= passes (AC, then DoD) closed a real coverage gap before marking AC6 done: no test exercised the empty-evening-column case DESIGN.org explicitly names, and the D33 (window-gating) test conflated with the zero-chore guard (AC6) rather than isolating it — both fixed with additional/strengthened tests. No DESIGN.org pivot: the three-state collapse is an implementation-level realization of the design's own "state 4 renders like state 1" description, not a reinterpretation of it — additive confirmation only. =mix precommit= clean, 218 passed. No new Deferred discoveries.
:END:
** DONE [[file:stories/story-06-admin-today-extras.org][Story 06 — Admin Today: extras correction, remove day reset]]
:LOGBOOK:
- Note taken on [2026-07-12 Sun 15:58] \\
  Closed out: all 5 ACs and all 4 DoD items =[X]=. =Admin.TodayLive= (=lib/bear_cub_web/live/admin/today_live.ex=): =load/2= gained a per-kid =extras= list sourced from =Chores.list_extras(kid, today)= (Story 02 — retired rows already filtered), rendered unconditionally per kid card (=id="today-extras-#{kid.id}"=, no new =expanded= key, matching the story's own suggested pattern). Extracted the chore =<li>= row markup into a shared =chore_row/1= function component (mirroring the pattern already used in =ChoreLive.Index= and =KioskLive=), reused for both the morning/evening sections and the new Extras section — extras fire the existing =phx-click="toggle-chore"= handler into =Chores.toggle_completion/3= with no new event handler needed, since extras are chores per D27. Removed the =#reset-kid-{id}= button, the =#reset-day= button, and both =handle_event("reset-kid", ...)=/=handle_event("reset-day", ...)= clauses (D31). =BearCub.Chores=: removed =reset_day/1=, =reset_kid_day/2=, and the shared private =reset/2= helper; =current_of_day/1= correctly retained since =current_completions/1= still depends on it. Deleted the four reset-specific tests from =chores_test.exs= and renamed its describe block from "on-behalf toggling and day resets" to "on-behalf toggling"; in =today_live_test.exs=, replaced the reset-flow tests with a "reset controls are gone" assertion and added new Extras coverage plus a dedicated cross-process (admin + kiosk) PubSub test that pins =routine_windows= to keep morning deterministically active. Two independent =story-verifier= passes (AC, then DoD) found no gaps. No DESIGN.org pivot: the story implements D30/D31 verbatim (both minted at design-time) — additive confirmation only. =mix precommit= clean, 216 passed. One Deferred discovery carried below.
:END:

* Deferred

# Out-of-scope discoveries land here during implementation instead of being fixed inline. Promoted to v2 candidates at initiative close.

- From Story 01: a full =mix test= run occasionally logs a non-fatal =Exqlite.Connection ... disconnected: ** (DBConnection.ConnectionError) client ... exited= line during teardown (no test failures either way). Traced to the scratch =BearCub.MigrationTestRepo= pool (=test/support/migration_test_repo.ex=), started per-test via =start_supervised!= with the default (non-Sandbox) pool, shutting down at test-process exit — the same class of benign teardown race the MVP chain's PLAN.org already noted for Story 03 of that initiative. Never affected =mix precommit='s exit code across many repeated runs; worth a look only if fully log-clean =mix precommit= output becomes a goal.
- From Story 01: the DDL-transaction/connection-pool hazard the nullability migration works around — =Ecto.Migration.Runner= dispatches =execute=/=create table=/=drop table= through a separate Agent process, so with =@disable_ddl_transaction true= a pooled (non-Sandbox) connection can schedule statements on different physical SQLite connections, letting a later statement observe a stale pre-drop schema — was promoted directly to =docs/learnings.org= (2026-07-12 entry) rather than waiting for a second occurrence, per human ruling: transferable craft knowledge proven by reproduction on two paths (an isolated test repo and the real =mix ecto.migrate= path), not design-silence material. No further action pending here.
- From Story 02: =list_extras/2= (=lib/bear_cub/chores.ex=) references =BearCub.Chores.Completion= fully-qualified rather than the =alias BearCub.Chores.Completion= declared later in the file — correct given Elixir's lexical alias scoping (the alias isn't in scope yet at that point in the file), but a future pass could hoist the alias earlier for consistency.
- From Story 02: =update_chore/2= builds a partial =%Kid{id: chore.kid_id}= struct purely to satisfy =next_position/2='s =%Kid{}= guard clause, avoiding an extra query. Works correctly and is covered by tests, but is a slightly unusual pattern — a future pass could change =next_position/2='s signature to take a bare =kid_id= instead.
- From Story 03: =ChoreLive.Index='s three list sections (Morning/Evening/Extras) render plain =:for= lists rather than LiveView streams, per CLAUDE.md's "always use streams" guideline. Pre-existing pattern — Morning/Evening already did this before Story 03; the new Extras section just mirrors it. Consistent with the rest of the admin/kiosk codebase at family-scale list sizes (single-digit item counts); revisit only if that scale assumption changes.
- From Story 04: =BearCub.Routines.other/1='s doc comment (=lib/bear_cub/routines.ex=) still describes it as "the manual flip target (FR-4)" — stale now that the kiosk flip is removed. The function itself is correctly retained (still used by =Admin.TodayLive= to list both routine sections); only the comment is out of date. Out of Story 04's file-touch list; a docs-only fix for a future pass.
- From Story 06: the new admin→kiosk cross-process PubSub test in =today_live_test.exs= (two independent =live/2= processes) reliably logs a benign =Exqlite.Connection ... disconnected: DBConnection.ConnectionError= line during teardown — the same class of non-fatal sandbox-connection race already noted for Story 01's migration test repo, just triggered here by a second LiveView process outliving the first. Never affects =mix precommit='s exit code across repeated runs with different seeds. Worth a look only if fully log-clean test output becomes a goal.
