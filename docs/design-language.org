#+title: Design Language — Kids' View
#+startup: showall

One-off visual redesign, backlog-tracked (no chain documents). Future kiosk styling work builds on this file rather than re-litigating it.

* Semantics
A child-color left border (~--child-border-width~) means chore ownership — whose card this is. A routine color (~--routine-morning~, ~--routine-evening~) means temporal context — which part of the day this card belongs to. A routine tint (~--routine-morning-tint~, ~--routine-evening-tint~) means pending within that routine — it disappears the moment a card is done.

* Invariants
Collapsed routine cards never carry a child-color border — collapse is a routine-level state, not any one child's. Extras render on a fixed neutral surface (~--extra-card-background~, ~--extra-card-content~) independent of routine and theme, so they never inherit tint meaning.

* Rulings
The collapsed-routine message renders inside the collapsed card — do not re-litigate.

The persistent routine header bar (the routine-colored title bar above the chore rows, and its twin atop the collapsed band) is retired [2026-07-16]; the banner completion icon beside the child's name is now the routine card's collapse/expand affordance. Do not reintroduce a routine-region header without a ruling.

Completion badge (sun/moon) considered and deferred [2026-07-12] — collapse + message were the only completion signals. Superseded [2026-07-16]: a large sun (morning) / moon (evening) completion icon now renders in the child's banner whenever their routine is complete. Refined [2026-07-17]: the icon carries no separate checkmark glyph — a single green bonus badge is the completion/reward signal, shown only when the routine bonus is not forfeited (a redo-after-fail completion shows the icon alone, no badge). Do not add further indicators without a ruling.

A green +N chip on a completed extra's card shows its earned value (its own ~chore.points~); routine chores carry no per-chore number — a routine earns a single +R no matter how many chores it holds, so a per-chore number there would misstate that. Added [2026-07-17]. The exact positive mirror of the failed extra's warning + −N chip.

Completed rows shrink [2026-07-24]: a done chore/extra row renders at ~h-20~ against the pending ~h-24~ — a quiet in-row completion signal that also refunds vertical space toward the 5-chore no-scroll budget (FR-6). Row tracks are content-sized (~auto-rows-min~), never equal-fill. The shrink stays subtle: a done row must remain a comfortable undo tap target — do not shrink further without a ruling.

Night screen [2026-07-24]: the 23:00–05:00 gap is a single dark screen (~bg-stone-950~) with one large centered moon (~hero-moon-solid~, colored ~var(--routine-evening)~ — the moon is temporal context, evening family) and nothing else: no columns, banners, points badges, events, or copy, and nothing tappable. Minimum emitted light is the point — do not add content or brighten the surface without a ruling. Rationale: [[file:decisions.org::#d56][D56]].

Reward card surface [2026-07-25]: a reward card reuses the extras tokens (~--extra-card-background~, ~--extra-card-content~) rather than a new pair — like extras, a reward carries no routine or chore-ownership meaning, so it takes the same fixed neutral surface and never a child-color border. Availability/verdict state is expressed by dimming (~opacity-45~) plus a glyph, never by a second surface color. Rationale: [[file:decisions.org::#d66][D66]].

Reward card dim+glyph vocabulary [2026-07-25]: rows 4–6 of the kiosk's seven-row card precedence table (declined/claimed/locked) share the one dim treatment (~opacity-45~) above and differ only by glyph — declined is ~hero-x-mark~, claimed is ~hero-check~ (the same check used by a done chore row), locked is ~hero-lock-closed~. Row 3 (pending) is untappable like rows 4–6 but is deliberately *not* dimmed with them — it carries the literal =⏳= character at full color, identical to the banner gift button's own pending glyph, not a fourth hero icon added to the dim group: a request awaiting an answer is not yet a "not available" reason. The lock glyph is the pre-reader-legible explanation for unaffordable (D63/D64): no number, especially no negative number, is ever shown on a reward card. Do not add a fourth dim-state glyph, dim the pending row, or split the shared dim treatment, without a ruling. Rationale: [[file:decisions.org::#d66][D66]].

Banner gift button [2026-07-25]: the banner's right-hand group is the points badge plus a gift button (=🎁=), grouped together because the badge and the shop are the same economy. The button has exactly two glyphs — =🎁= idle, =⏳= while this kid has an open request today — and is always tappable regardless of which glyph it shows; there is no third (e.g. declined) banner state; a decline is visible only on the card that carries it (D67). The points badge's own tap stays unbound, reserved for a possible future affordance. Do not add a third banner glyph or bind the badge's tap without a ruling. Rationale: [[file:decisions.org::#d65][D65]], [[file:decisions.org::#d67][D67]].

Tokens live in assets/css/app.css, in the block above the LiveView class variants.
