#+title: Backlog
#+startup: showall
#+TAGS: oneshot(o) hand(h) promote(p)

Repo-level backlog, cumulative across initiatives. Items land here from PLAN Deferred sections at initiative close, from ad-hoc discovery (org-capture or mid-session), and from work surfaced out of scope during execution. Items leave by being done, claimed by an initiative, or deliberately dropped. Rule [2026-07-24 Fri]: DONE entries do not stay here — move each to docs/completed.org (heading, properties, and full capture/triage/done trail intact) at the review that marks it, so this file only ever holds open work.

Size tags: oneshot (compiled single-session prompt), hand (inert edit, no session), promote (candidate for initiative-scale work). Untagged means untriaged — sizing happens at review or pull time, not capture time.
* TODO [#C] revisit web push for parent notifications
:PROPERTIES:
:CREATED:  [2026-09-06 Sun 13:05]
:END:
Set aside [2026-09-06 Sun] while implementing the ntfy oneshot (now in docs/completed.org): web push would keep notifications first-party with no relay and no extra phone app, and delivery reaches the phone anywhere via Apple's/Google's push relays. What ruled it out for that session was the machinery, not the outcome: a secure context (TLS for the admin — a Tailscale cert for the MagicDNS name plus reverse proxy or Bandit TLS in the NixOS module), a service worker plus a per-phone subscribe flow in the admin UI, a subscriptions table and a VAPID keypair as a new server secret, a web-push encryption dep (~web_push_elixir~ or ~web_push_encryption~), and iOS only delivering to a Home Screen PWA (so a manifest too). Untagged deliberately: this is initiative-scale (promote) if picked up, and would replace rather than sit beside the ntfy path. Trigger to revisit: the public-relay trade ("<kid> finished <chore>" through ntfy.sh) stops being acceptable, or TLS lands for another reason.
* TODO On-device gate for the good-standing band and ring is still owed :hand:
:PROPERTIES:
:CREATED:  [2026-09-06 Sun 09:08]
:END:

Swept [2026-09-06 Sun 09:08] from the good-standing Batch 1 gate, where the human ruled it a documented deferral. Story 05's AC8 stays ~[-]~: the tablet cannot be exercised without a deploy, so nothing on this machine can close it.

Still owed, on the tablet in Fully Kiosk, during the morning window, for a kid in good standing:

1. band and ring render as designed;
2. *both* read from across the room — the ring is a 4px inset ring against ~bg-base-100~ and read as a thin line in the desktop spot-check, so it needs the legibility check as much as the band does;
3. band plus five manually re-expanded done morning chore rows fit without scrolling;
4. band and ring disappear at 17:00 without anyone touching the tablet;
5. standing still renders correctly after sleep/wake reconnect.

Local dev recipe, verified [2026-09-06 Sun 09:07] — it does *not* substitute for the tablet, but it makes items 3 and 4 checkable before a deploy, which is what was missing:

- Force the morning window to cover the current time and push evening past it: ~BEAR_CUB_MORNING_WINDOW=05:00-22:00 BEAR_CUB_EVENING_WINDOW=22:00-23:00 mix phx.server~. ~config/runtime.exs~ is not guarded by ~config_env()~, so both vars apply in dev; confirmed by reading ~:routine_windows~ back after an override. On an overlap ~Routines.current/2~ takes the first matching window in config order, which is morning.
- Raise the band without backdating anything: use a kid whose *evening* roster is empty. An empty roster is vacuously satisfied ([[file:decisions.org::#d92][D92]]), so last night's half passes for free and simply completing today's morning chores on the kiosk is enough. Any other kid needs yesterday-dated evening completions inserted by hand in IEx.
- Watch the disappearance without waiting for 17:00: set the morning end a few minutes ahead of now, e.g. ~BEAR_CUB_MORNING_WINDOW=05:00-09:25 BEAR_CUB_EVENING_WINDOW=09:25-23:00~. ~Routines.next_boundary/2~ schedules a re-render at every window edge, so the band and ring vanish on their own at that edge.

What the recipe cannot cover, and why the tablet items survive it: Fully Kiosk's Android WebView rendering, across-the-room legibility, and sleep/wake reconnect. Desktop Chrome at a tablet viewport approximates item 3 only.

* TODO snapshot the routine windows and early bird cutoff so a config change does not rewrite points history
:PROPERTIES:
:CREATED:  [2026-09-06 Sun 14:10]
:END:
Set aside [2026-09-06 Sun] while implementing the early bird oneshot (now in docs/completed.org), on the human's own call. Every routine-day contribution is re-derived from ~completions~ against the *current* config on every read (D10, D72): ~Routines.bonus/0~, ~Routines.early_bird_cutoff/0~, ~Routines.early_bird_bonus/0~, and ~Routines.windows/0~ all come from ~config/runtime.exs~ with no history. Bumping ~BEAR_CUB_EARLY_BIRD_CUTOFF~ from =07:45= to =09:00= for the summer therefore retroactively awards the early bird to every past morning finished between 7:45 and 9:00, and moving it back in September takes those points away again — the same class of drift as the roster drift D81 closed for chores, now on the config axis. The bonus amounts (~R~, ~E~) drift identically, and the windows would too if any derivation ever read them (none does today; ~completed_at~ is compared only to the cutoff). Untagged deliberately: the fix needs a ruling on shape before sizing — a dated effective-from table for the constants (config becomes data, seeded from env on first boot, edited in admin), a per-completion snapshot of the values in force at tap time (denormalizes onto the row and still misses ~R~, which is a routine-day fact, not a row fact), or a stated policy that re-derivation against current config is intended and the family accepts the drift. Trigger: the first seasonal cutoff change, or any bonus amount change with meaningful history behind it.
* TODO [#B] The extra chores need to show how many points they're worth. :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-26 Sun 14:18]
:END:

Triage [2026-09-06 Sun]: oneshot, #B — today an extra's ~+N~ chip (~lib/bear_cub_web/live/kiosk_live.ex~'s ~chore_row/1~, D54) renders only once the chore is done, so a kid can't see the payoff before tapping. Extend the same row to show ~chore.points~ on the *pending* card too — the value is already loaded (~@chore.points~), no new query. Kids' view visual — ~docs/design-language.org~ read is binding (a number on every pending row is more chrome than D54 shipped with; get a ruling on placement/weight before implementing, not after); on-device gate applies.
* TODO [#B] parents can give points to a kid for some reason on the fly :promote:
:PROPERTIES:
:CREATED:  [2026-07-26 Sun 13:09]
:END:

Triage [2026-09-06 Sun]: promote — not a clean oneshot. ~Points.balance/2~ is ~Chores.earnings + Rewards.spend_total~ (D42, D57); an ad hoc award is neither — it's a third signed-input source with no completion row and no redemption behind it, so it needs its own ruling on where it lives (a new context, or a new signed-event table under an existing one), an admin surface (amount + reason, at minimum), and how it composes into ~balance/2~ and the grouped ~balances/1~ without breaking the D72-style pointwise-agreement requirement between the per-kid and grouped derivations. Candidate for its own brainstorm rather than absorption into an existing initiative.
* Fulfillment marker on redemptions — act if double-buy or forgot-to-buy occurs in family use
:PROPERTIES:
:CREATED:  [2026-07-25 Sat 22:40]
:END:
Considered and deliberately not designed for in rewards/redemption (DESIGN.org "Out of Scope for This Design"): the app records that a redemption *happened*, not that the parent actually delivered it. At two-kid scale the verbal channel covers it, and deferral is structurally free — a nullable ~fulfilled_at~ marker plus one admin control later is a trivially additive oneshot. Act only if field evidence surfaces (a double-buy or a forgot-to-buy) that the verbal channel isn't catching.

Swept [2026-07-25 Sat 22:40] from docs/2026-07-25-rewards-redemption/PLAN.org's Deferred section and DESIGN.org's Out of Scope section.
* History pagination (D70) — backlog seed if the 50-row bound ever bites
:PROPERTIES:
:CREATED:  [2026-07-25 Sat 22:40]
:END:
~Rewards.list_redemption_history/1~ (~/admin/rewards/history~) is bounded at a default ~limit~ of 50, with the bound stated on screen (D70) — a deliberate v1 simplicity choice, not an oversight. Act if the flat unpaginated list stops being enough at family scale.

Swept [2026-07-25 Sat 22:40] from docs/2026-07-25-rewards-redemption/PLAN.org's Deferred section.
* Wall-clock-derived date argument in ~today_live_test.exs~ (rewards/redemption Story 01)
:PROPERTIES:
:CREATED:  [2026-07-25 Sat 22:40]
:END:
A pre-existing test in ~test/bear_cub_web/live/admin/today_live_test.exs~ (/"a fail live-updates the kiosk: the chore reverts and the points badge drops"/) passes ~DateTime.to_date(LocalTime.now())~ as its local date rather than a fixed value — Story 01 only swapped the module name reference on that line, without changing the wall-clock read. Not a ~docs/learnings.org~ [2026-07-17] violation — the test does pin ~:routine_windows~, which is what that learning requires — but the date argument itself still tracks whichever local date the run happens to land on. A test-hygiene seed: apply the [2026-07-17] discipline to the date argument the learning didn't originally reach.

Swept [2026-07-25 Sat 22:40] from docs/2026-07-25-rewards-redemption/PLAN.org's Deferred section (Story 01 discovery).
* No automated guard on the ~Chores~ ⇏ ~Rewards~ dependency direction (rewards/redemption Story 01)
:PROPERTIES:
:CREATED:  [2026-07-25 Sat 22:40]
:END:
CLAUDE.md treats ~Chores~ ⇏ ~Rewards~ as a design invariant alongside the older ~Chores~ ⇏ ~Calendars~ one, and D57 rests on it: the moment ~Chores~ imports ~Rewards~, a rewards failure can reach the chore path. Today it holds by convention and by ~grep~ at review time only — nothing fails automatically if a future session wires the two together. A durable guard (a boundary test, or a compile-time check over the module's references) needs its own ruling on instrument and placement, and it applies equally to the older ~Calendars~ invariant, not just this one.

Swept [2026-07-25 Sat 22:40] from docs/2026-07-25-rewards-redemption/PLAN.org's Deferred section (Story 01 discovery).
* ~mix ecto.reset~ seeds the test database when run under ~MIX_ENV=test~ :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-25 Sat 22:40]
:END:
The ~ecto.reset~ alias (~mix.exs~) chains ~ecto.drop~ → ~ecto.setup~, and ~ecto.setup~ unconditionally runs ~priv/repo/seeds.exs~ — there is no ~MIX_ENV~ guard distinguishing "reset my dev sandbox" from "reset the test database a suite is about to run against." Run against ~MIX_ENV=test~, it permanently inserts the placeholder kid/chore rows directly into the test db file, outside any ~Ecto.Adapters.SQL.Sandbox~ transaction — the seeded rows survive every subsequent ~mix test~ invocation until the file is manually dropped and recreated without seeding (~MIX_ENV=test mix ecto.drop && MIX_ENV=test mix ecto.create && MIX_ENV=test mix ecto.migrate~, skipping ~ecto.setup~'s seed step). Discovered when rewards/redemption Story 02's own verification loop hit this by accident (a run of failures — leaked kid/chore rows, id drift, a "fresh production database" test finding kids present — reproduced identically against a ~git stash~ of pre-story code, ruling out the rewrite as cause). A repo-tooling oneshot: guard ~ecto.setup~'s seed step on ~Mix.env() == :dev~, or split ~ecto.setup~ into seeding and non-seeding variants. Touches shared tooling (~mix.exs~), not any one story's code.

Swept [2026-07-25 Sat 22:40] from docs/2026-07-25-rewards-redemption/PLAN.org's Deferred section (Story 02 discovery).
* Late-approval same-day balance-read characteristic (D72-class, narrowed by D73)
:PROPERTIES:
:CREATED:  [2026-07-25 Sat 22:40]
:END:
~Rewards.spend_totals_by_kid/1~ filters on a redemption row's ~local_date~ (request time) combined with its *current* ~approved_at~/~reversed_at~ state, not the state as of the date being queried, so a marker stamped after the fact changes what an earlier "as of" read reports — the same D10/D72-class characteristic as the roster-drift entry above, not a bug. D73's day-scoped ~check_open/2~ closes the original multi-day form (request day 1, an intermediate ~Points.balance~ read on day 2, approval on day 3 moving that day-2 read after the fact) — ~approve_redemption/3~ now refuses a request once its ~local_date~ is before today. What remains, still accepted under D72's reasoning: (a) the *same-day* case — a request and its approval both dated today, with an intermediate ~Points.balance~ read taken between the two, still moves after the fact, since ~check_open/2~ only excludes prior days, not same-day timing; (b) direct data manipulation (IEx, or any future admin action stamping ~approved_at~ outside the guarded write paths) can still produce a lapsed-then-approved row, bypassing the guard entirely.

Swept [2026-07-25 Sat 22:40] from docs/2026-07-25-rewards-redemption/PLAN.org's Deferred section (Story 03 discovery, narrowed by D73).
* TODO [#B] Ollie idea: "Streaks" like completing your Morning Routine every day :promote:
:PROPERTIES:
:CREATED:  [2026-07-25 Sat 20:44]
:END:

e.g. "You've completed your morning routine 7 days in a row!"

Triage [2026-09-06 Sun]: promote — a streak is a consecutive-day scan over ~routine_day_status/3~ (D94) rather than a single derived value, with its own edge-case set (does an empty roster's vacuous D92 pass count toward a streak? does a failed-then-redone day? where does it reset — midnight, or the window boundary ~Routines.next_boundary/2~ already tracks?) and a kiosk display surface of its own. Same brainstorm-first shape as the skip-chore and points-stats ideas below; consider claiming together if one initiative ends up covering "things Ollie asked for about the points/routine surface."
* TODO [#B] admin "quick add" extra chore from Today view :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-24 Fri 15:06]
:END:
Triage [2026-07-24 Fri]: oneshot, #B — the Today view already renders a per-kid Extras section (~lib/bear_cub_web/live/admin/today_live.ex~), and extras are just chores with ~routine: nil~, so quick add is an inline minimal create landing there. A plain link to the existing form isn't enough: ~ChoreLive.Form~ hard-navigates to the chores index after save. Depends on the default-emoji item below (a quick add shouldn't demand icon picking) — do that first or in the same session. If the Today redesign (next entry) is also claimed, sequence it first: it owns the layout this lands in.
* TODO [#B] redesign admin "Today" view put each kid on their own tab and collapse completed routines :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-24 Fri 15:05]
:END:
Triage [2026-07-24 Fri]: oneshot, #B — large but single-view: all the state already lives in ~today_live.ex~ (per-kid cards, the ~expanded~ MapSet), so tabs replace the stacked sections and auto-collapsing completed routines extends the existing ~toggle-section~ derivation. Admin-only — no design-language or on-device-gate concerns. Sequence before the quick-add entry above if both are claimed.
* TODO [#B] ~Chores.earnings/2~'s N+1 query shape — the kiosk's per-kid live badge still has it; the grouped batch path does not
:PROPERTIES:
:CREATED:  [2026-07-18 Sat 23:35]
:END:
~points_total/2~ (~lib/bear_cub/chores.ex~, Story 02) issues one query for distinct ~(routine, local_date)~ pairs, then one ~routine_day_contribution/3~ call — two more queries — per pair, an N+1 shape. Correctness is unaffected and all tests pass, but the total is cumulative all-time and recomputed on every ~:chores_changed~ re-fetch (Story 03 wires the kiosk badge to it), so this could become a real cost as history grows. Swept [2026-07-18 Sat 23:35] from docs/2026-07-15-gamification-points/PLAN.org's Deferred section (Story 02 discovery). Triage [2026-07-24 Fri]: hold, deliberately untagged — conditional on points becoming load-bearing. Claim candidate for the rewards/redemption initiative (the Rewards entry above), which is exactly what would make the total load-bearing; size it there, not here.

Retriage [2026-09-06 Sun]: the condition landed — rewards/redemption shipped and points balances are now spent against — and that initiative's Story 02 (D72) did rewrite the whole-render path: ~Chores.earnings_by_kid/1~ (two grouped queries, independent of kid/history count) backs ~Points.balances/1~ and ~totals/1~. The function itself was renamed ~points_total/2~ → ~earnings/2~ along the way but its shape didn't change: ~Points.balance/2~ (~lib/bear_cub/points.ex~) still calls the per-kid ~Chores.earnings/2~, and ~KioskLive.build_column~ (~lib/bear_cub_web/live/kiosk_live.ex:382~) still calls ~Points.total(kid, today)~ once per kid per render — the N+1 shape this entry names is exactly there today, just narrower in scope than originally written (the kiosk badge only, not every reader). Still hold: two kids is still cheap, and the fix is now well-scoped — swap ~build_column~'s per-kid call for a whole-render ~earnings_by_kid/1~ lookup passed in once, the same move Story 02 already made for ~balances/1~.
* TODO [#B] Ollie idea: admin option to "skip" a routine chore -- marking complete without failing it
:PROPERTIES:
:CREATED:  [2026-07-17 Fri 07:05]
:END:
Triage [2026-07-24 Fri]: rewards-brainstorm candidate, deliberately untagged — not a clean oneshot because it needs a points-semantics ruling first (does a skipped chore earn its points? count toward the routine bonus? interacts with D10 derived day state and the D40/D41 contribution shapes). Settle the ruling in the rewards/redemption brainstorm, then size.

Retriage [2026-09-06 Sun]: that brainstorm concluded without it — rewards/redemption's DESIGN.org names this explicitly: "not claimed, and not designed for. Neither is precluded: ... nothing in the redemption derivation constrains what a 'skipped' chore could contribute, since redemptions sum into the signed-inputs term independently of how earnings are derived." Still deliberately untagged, still needs the same points-semantics ruling — just needs a fresh brainstorm to settle it, not the one that already passed on it.
* TODO [#B] Ollie idea: tapping on the points should show you daily points progress and stats on your points
:PROPERTIES:
:CREATED:  [2026-07-17 Fri 07:02]
:END:
Triage [2026-07-24 Fri]: rewards-brainstorm candidate, deliberately untagged — same points-UI theme as the rewards/redemption initiative; a stats view would also make ~points_total/2~'s N+1 shape (entry above) load-bearing, so consider claiming both together.

Retriage [2026-09-06 Sun]: rewards/redemption shipped without claiming this — DESIGN.org: "A points-stats view for the kids ... not claimed, and not designed for. Neither is precluded: the points badge's own tap gesture stays unbound." Points did become load-bearing (balances are spent against, D57), but through ~earnings_by_kid/1~'s grouped rewrite rather than through this surface, so the N+1 entry above is now only about the kiosk's per-kid live badge, not this one. Still open, still untagged — the tap gesture is free and waiting.
* TODO [#B] Need a default emoji for extra chores -- parents may not have time or desire to pick one. :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-12 Sun 23:06]
:END:
Triage [2026-07-14 Tue]: oneshot — default in the changeset/form plus a test; too much code for a hand edit.
* Test-teardown Exqlite disconnect log noise — act only if log-clean output becomes a goal
Merged [2026-07-14 Tue] from extras stories 01 and 06 (both 2026-07-12) —
same class of non-fatal sandbox-connection race, two known triggers:
- The scratch ~BearCub.MigrationTestRepo~ pool
  (~test/support/migration_test_repo.ex~), started per-test via
  ~start_supervised!~ with the default (non-Sandbox) pool, shutting down at
  test-process exit.
- The admin→kiosk cross-process PubSub test in ~today_live_test.exs~ (two
  independent ~live/2~ processes), where the second LiveView process
  outlives the first.
Occasionally logs a =Exqlite.Connection ... disconnected:
(DBConnection.ConnectionError)= line during teardown; no test failures,
never affects ~mix precommit~'s exit code.

Also observed from the gamification-points initiative (Story 10, PLAN.org
Deferred, 2026-07-17) — same class, no new trigger; folded in here rather
than duplicated (a rough capture of this had briefly existed as its own
backlog entry — merged in during the 2026-07-18 post-retro sweep).

Retro ruling ([2026-07-18]): removing ~BearCub.MigrationTestRepo~ (see the
dedicated backlog item above) retires the first of the two triggers above,
leaving only the ~today_live_test.exs~ cross-process PubSub trigger.

Update [2026-07-24 Fri]: ~BearCub.MigrationTestRepo~ removed — first
trigger retired. The remaining ~today_live_test.exs~ trigger still fires
(two disconnect lines observed in the same ~mix precommit~ run); no test
failures, entry stands as act-only-if-log-clean-becomes-a-goal.

* From the extras initiative (docs/2026-07-11-extras/), story 03, 2026-07-12
~ChoreLive.Index~'s three list sections (Morning/Evening/Extras)
(~lib/bear_cub_web/live/admin/chore_live/index.ex~) render plain ~:for~ lists
rather than LiveView streams, per CLAUDE.md's "always use streams"
guideline. Pre-existing pattern — Morning/Evening already did this before
Story 03; Extras just mirrors it. Consistent with the rest of the
admin/kiosk codebase at family-scale list sizes (single-digit item counts).
Act only if that scale assumption changes.

Also observed from the rewards/redemption initiative (Story 05,
2026-07-25) — ~KioskLive~'s reward list, and the sibling chore/extras/events
lists it's modeled after (~lib/bear_cub_web/live/kiosk_live.ex~), render the
same way. Same class, a different LiveView; not introduced by Story 05,
which only carried the existing pattern forward. Folded in here rather
than duplicated, per the same discipline the Exqlite teardown-noise entry
above already follows.

Swept [2026-07-25 Sat 22:40] from docs/2026-07-25-rewards-redemption/PLAN.org's Deferred section (Story 05 discovery).
* TODO [#C] ~mix precommit~'s ~format~ step can't fail the declared verify command :hand:
:PROPERTIES:
:CREATED:  [2026-09-06 Sun 10:00]
:END:
~mix precommit~ (~mix.exs~) runs plain ~format~, which rewrites files in place, rather than ~format --check-formatted~, which would exit non-zero on unformatted code. The repo's declared Verify command (CLAUDE.md) is therefore unable to catch a formatting regression — it silently fixes and reports success either way.

Swept [2026-09-06 Sun] from docs/2026-09-04-chore-lifecycle/PLAN.org's Deferred section (Story 01 discovery, recorded there as "pre-existing, adjacent" rather than in scope for that story). Triage [2026-09-06 Sun]: hand — a one-line alias change (~mix.exs~), no session needed.
* TODO [#C] ~priv/repo/seeds.exs~ sets chore ~position~ by hand instead of through ~Chores.create_chore/3~ :hand:
:PROPERTIES:
:CREATED:  [2026-09-06 Sun 10:00]
:END:
The seed script computes and assigns ~position~ itself rather than calling ~create_chore/3~, which derives it (D86). Predates the chore-lifecycle batch; that batch's Story 01 stamped ~active_from~ the same bypassed way rather than reroute the script.

Swept [2026-09-06 Sun] from docs/2026-09-04-chore-lifecycle/PLAN.org's Deferred section (Story 01 discovery). Triage [2026-09-06 Sun]: hand — route the script through ~create_chore/3~, or confirm why it can't and drop this entry.
* TODO [#C] Recurring-extra "off" exit — retiring naturally after completion history — has no test :oneshot:
:PROPERTIES:
:CREATED:  [2026-09-06 Sun 10:00]
:END:
DESIGN §Recurring extras names two exits for a recurring extra: archive, and flipping recurrence off so it retires naturally once its last completion is a prior day. Every existing test moves the recurrence toggle *on*; nothing turns it off on a recurring extra with completion history and then asserts the natural retirement. Each half is covered separately (the reclassification lock inspects only ~:routine~, and one-off retirement is pinned elsewhere), but the composed behavior the design asserts is unpinned — a regression making ~extra_daily~ → ~extra~ sticky would leave the suite green.

Swept [2026-09-06 Sun] from docs/2026-09-04-chore-lifecycle/PLAN.org's Deferred section (Story 05 discovery, surfaced by the update-design pass rather than the story itself — no design text is wrong, this is a coverage gap against correct text). Triage [2026-09-06 Sun]: oneshot, #C — one test, per the Deferred note's own sizing.
* TODO [#C] Payout animation when the last routine chore is tapped :promote:
:PROPERTIES:
:CREATED:  [2026-09-06 Sun 18:08]
:END:
Set aside [2026-09-06 Sun] while recreating the Claude Design chore-card handoff (D105): its payout sequence is fully specified and was prototyped in the handoff's =Payout Animation.dc.html=, but choreographing client beats across LiveView patches — the row moves stacks on the server render, and the collapse delay (~pending_collapse~ in ~BearCubWeb.KioskLive~) then swaps the rows for the band — needs a JS hook that owns the transition rather than CSS on the patched DOM. What shipped is ~transition-colors~ on the stake segments and chip. Narrowed [2026-09-07 Mon]: beats 1–3 shipped in D106 (press-in, fill, check pop, on a 700ms in-place hold) and beat 4, the sink, is its own entry above — this entry is now beats 5–11 only, the sequence that runs when the *last* chore of a routine is tapped. The handoff's timings, authoritative if picked up (~2.4s total from the tap, no text at any point); beats 1–4 are listed for the timeline only:
| Time   | Beat    | Detail                                                                                                                             |
|--------+---------+------------------------------------------------------------------------------------------------------------------------------------|
| 0ms    | Press   | /Shipped, D106./ Row scales to 0.97, 100ms ease-out. Haptic tap — not shipped, no haptics exist.                                  |
| 120ms  | Fill    | /Shipped, D106./ Row background → kid color, dashed border → solid, label → white. 260ms ease.                                     |
| 200ms  | Check   | /Shipped, D106./ Check disc scales 0 → 1.28 → 1 over 320ms, ~cubic-bezier(.34,1.56,.64,1)~.                                         |
| 440ms  | Sink    | /Own entry above./ Slot collapses while an identical row expands at the top of the done stack. 300ms ease-in-out.                 |
| 720ms  | Segment | Final stake segment fills kid color, 200ms.                                                                                        |
| 920ms  | Sweep   | All segments → success, left to right, 70ms stagger per segment, 200ms each.                                                       |
| 1200ms | Ground  | Stake bar ground routine tint → paid tint, edge likewise, 260ms.                                                                    |
| 1300ms | Chip    | ~+R~ chip → success fill / white text, pops to 1.28 over 380ms.                                                                    |
| 1560ms | Fly     | A duplicate chip travels from the stake chip to the header star pill, 620ms, ~cubic-bezier(.5,-0.2,.3,1.4)~ so it arcs; scales to 0.7 en route, fades over the last 160ms. Compute the delta from live element rects. |
| 2100ms | Count   | Star pill pulses to 1.16 (380ms) and the total rolls up at ~45ms per increment. Chime.                                             |
| 2300ms | Stamp   | Completion icon and its badge scale in together, 420ms, same spring curve. The routine is now marked paid.                        |
Beats overlap so it reads as one event. Interruptible: a tap elsewhere mid-run snaps every beat to its end state. ~prefers-reduced-motion~: skip to end state, keep the count roll. Neither a chime nor haptics exist in the app today, and the tablet's WebView is the bar for smoothness, not desktop Chrome. Promote-sized: it subsumes the hide/show animation entry below (same code path, the collapse), and the collapse-delay's 400ms would need to stretch to cover the sequence — and past the 700ms settle, so the last chore's held row is seen before the band replaces it.
* TODO [#C] animation for hiding/showing routine chores :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-24 Fri 15:58]
:END:

when the last chore is completed for the routine, have it animate away rather than abruptly disappearing (e.g. collapse or similar).  likewise, if it is toggled back into view, animate it into view rather than abruptly showing it

Triage [2026-07-24 Fri]: oneshot, #C — kids' view visuals: docs/design-language.org read is binding; on-device gate applies, and animation smoothness on Fully Kiosk's WebView is the bar, not desktop Chrome. This is the Story 07/08 collapse path (delay → band, big-icon tap restores), so claim it together with the collapse-timer double-schedule entry below — same code, one session.
* TODO [#C] Kiosk collapse-delay can double-schedule its collapse timer on a rapid undo/redo :oneshot:
:PROPERTIES:
:CREATED:  [2026-07-18 Sat 23:35]
:END:
~BearCubWeb.KioskLive~'s collapse-delay (~lib/bear_cub_web/live/kiosk_live.ex~, Story 07): a rapid undo/redo of a routine's last chore within the ~@collapse_delay_ms~ (400ms) window can schedule more than one ~Process.send_after(self(), {:collapse_ready, kid_id}, ...)~ for the same kid — the first timer's reference is never stored or cancelled on undo. The extra message is a harmless no-op (~MapSet.delete~ on an already-absent key, then an idempotent reload), so no incorrect UI state results, but a very fast undo→redo could make the band appear slightly earlier than a full fresh delay after the final completion. Not covered by any AC — the design's "short delay" is explicitly imprecise at this level.

Swept [2026-07-18 Sat 23:35] from docs/2026-07-15-gamification-points/PLAN.org's Deferred section (Story 07 discovery).

Triage [2026-07-24 Fri]: oneshot, #C — store the timer reference and cancel it on undo. Behavior today is a harmless no-op, so this is polish, not a bug fix.
* TODO [#C] Change ~next_position/2~ to take a bare ~kid_id~ :oneshot:
From the extras initiative (docs/2026-07-11-extras/), story 02, 2026-07-12:
~update_chore/2~ (~lib/bear_cub/chores.ex~) builds a partial
~%Kid{id: chore.kid_id}~ struct purely to satisfy ~next_position/2~'s
~%Kid{}~ guard clause, avoiding an extra query. Works correctly and is
covered by tests, but is a slightly unusual pattern — change
~next_position/2~'s signature to take a bare ~kid_id~ instead and update
callers and tests.
